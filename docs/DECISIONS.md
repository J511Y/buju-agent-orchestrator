# Engineering Decisions

## 2026-03-17
- 30-min STRATEGY DIRECTOR (01:49 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check: remote `GET /api/agent/thinking/j211y?limit=20` returned `count=20` and ordered window (`2026-03-16 15:50:10 -> 2026-03-17 01:20:26`) improved (`level +0`, `exp +0`, `gold +30`, `inventory -3`, latest death unchanged at `2026-03-15 05:09:30`), so CHANGE was not applied.
- Safety/efficiency evidence now: `GET /api/status => 200` (`level=25`, `exp=6157`, `gold=439`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` keep safest high-efficiency target as `skeleton` (`score 103` vs `rabbit 90`), while strict level-threshold movement gate remains (`BUJU_MOVE_LEVEL_2=30`).
- Hard constraints revalidated as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation prioritizes selling unequipped gear worse than equipped before general cleanup.
- Equipment progression plan remains explicit and active in this document: (a) early game = gold accumulation/no risky enhancement spam, (b) mid game = weapon-first enhancement after reserve/prereq checks, (c) late game = armor/accessory broadening with failure-risk controls.
- Minimal safe enhancement path remains implemented and prerequisite-gated this cycle (`GET /api/npc/list => 200`, `npcs=[]`; enchant-scroll inventory `0`), so no risky enhancement attempt was executed.
- Live continuity evidence: daemon lineage remains active (`tmp/live-runner-procs-0149.txt`) and smoke passed (`tmp/cron-smoke-0149.txt`: `ok=1/1`, `lastAction=combat_start`, `level=25`, `exp=6161`, `gold=439`, `code=200`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=11%`, and progression `exp>=6245` or `gold>=449` with daemon continuity.

- 30-min STRATEGY DIRECTOR (01:18 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check: remote `GET /api/agent/thinking/j211y?limit=20` returned `count=20` and ordered window (`2026-03-16 15:21:13 -> 2026-03-17 00:51:19`) improved (`level +0`, `gold +25`, `inventory -8`, latest death unchanged at `2026-03-15 05:09:30`), so CHANGE was not applied.
- Safety/efficiency evidence now: `GET /api/status => 200` (`level=25`, `exp=5815`, `gold=444`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` keep safest high-efficiency target as `skeleton` (`score 103` vs `rabbit 90`), while strict level-threshold movement gate remains (`BUJU_MOVE_LEVEL_2=30`).
- Hard constraints revalidated as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation prioritizes selling unequipped gear worse than equipped before general cleanup.
- Equipment progression plan remains explicit and active in this document: (a) early game = gold accumulation/no risky enhancement spam, (b) mid game = weapon-first enhancement after reserve/prereq checks, (c) late game = armor/accessory broadening with failure-risk controls.
- Minimal safe enhancement path remains implemented and prerequisite-gated this cycle (`GET /api/npc/list => 200`, `npcs=[]`; enchant-scroll inventory `0`), so no risky enhancement attempt was executed.
- Live continuity evidence: daemon lineage remains active (`tmp/live-runner-procs-0118.txt`) and smoke passed (`tmp/cron-smoke-0118.txt`: `ok=1/1`, `lastAction=combat_start`, `level=25`, `exp=5827`, `gold=444`, `code=200`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=11%`, and progression `exp>=5900` or `gold>=454` with daemon continuity.

- 30-min STRATEGY DIRECTOR (00:48 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check: remote `GET /api/agent/thinking/j211y?limit=20` returned `count=20` and ordered window (`2026-03-16 14:50:02 -> 2026-03-17 00:22:03`) improved (`level +1`, `gold +30`, `inventory -8`, fresh death delta `+0` with latest death still `2026-03-15 05:09:30`), so CHANGE was not applied.
- Safety/efficiency evidence now: `GET /api/status => 200` (`level=25`, `exp=5477`, `gold=439`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` keeps safest high-efficiency target as `skeleton` (`score 103` vs `rabbit 90`), while strict level-threshold movement gate remains (`BUJU_MOVE_LEVEL_2=30`).
- Hard constraints revalidated as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation prioritizes selling unequipped gear worse than equipped before general cleanup.
- Equipment progression plan remains explicit and active in this document: (a) early game = gold accumulation/no risky enhancement spam, (b) mid game = weapon-first enhancement after reserve/prereq checks, (c) late game = armor/accessory broadening with failure-risk controls.
- Minimal safe enhancement path remains implemented and prerequisite-gated this cycle (`GET /api/npc/list => 200`, `npcs=[]`; enchant-scroll inventory `0`), so no risky enhancement attempt was executed.
- Live continuity evidence: daemon lineage remains active (`tmp/live-runner-procs-0048.txt`) and smoke passed (`tmp/cron-smoke-0048.txt`: `ok=1/1`, `lastAction=buy_hp`, `level=25`, `exp=5491`, `gold=514`, `code=200`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=11%`, and progression `exp>=5570` or `gold>=524` with daemon continuity.

- 30-min STRATEGY DIRECTOR (00:20 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check: remote `GET /api/agent/thinking/j211y?limit=20` returned empty (`count=0`), so fallback local posts were delta-scored (`tmp/cron-last20-delta-local-0020.json`, `count=20`, `14:18 -> 23:48 KST`) and showed non-improving economy (`level +1`, `exp +0`, `gold -5`, `inventory +0`, `rate/429/cooldown mentions 23`), so KEEP was rejected.
- Minimal/reversible CHANGE committed: `config/strategy.env` tuned `BUJU_MIN_GOLD_RESERVE 400->430` to curb low-value buy churn and protect combat liquidity without weakening safety gates.
- Safety/efficiency evidence now: `GET /api/status => 200` (`level=25`, `exp=5149`, `gold=409`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression plan (explicit, staged): (a) early game = gold accumulation with no risky enhancement spam, (b) mid game = enhance main weapon first after reserve+prereq checks, (c) late game = broaden to armor/accessory with failure-risk controls.
- BiS + enhancement path status: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; minimal safe enhancement action path remains implemented but prerequisite-gated this cycle (`scroll + npc + resource` not all satisfiable; `GET /api/npc/list => 200` with `npcs=[]`, no usable enchant scroll stock).
- Live continuity evidence: daemon processes remained continuous (no stop/restart action) and strategy smoke passed (`node scripts/live-strategy-runner.js` => `ok=1/1`, `lastAction=buy_mp`, `level=25`, `exp=5159`, `gold=414`, `code=200`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=11%`, and progression `exp>=5230` or `gold>=424` with daemon continuity.

## 2026-03-16
- 30-min STRATEGY DIRECTOR (23:48 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, `count=20`, ordered window `2026-03-16 13:50:32 -> 23:20:20`): deltas were mixed/non-improving (`level +1`, `exp +0`, `gold -5`, `inventory +3`, `death mentions 14`, `rate/429/cooldown mentions 12`), so KEEP was rejected by policy.
- Minimal/reversible CHANGE committed: `config/strategy.env` tuned `BUJU_BUY_COOLDOWN_TICKS 6->8` to reduce buy-churn gold bleed and slot-pressure drift while preserving strict safety gates and daemon continuity.
- Safety/efficiency evidence now: `GET /api/status => 200` (`level=25`, `exp=4807`, `gold=404`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit (early safe gold/no risky spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safely skipped this cycle (`GET /api/npc/list => 200`, `npcs=[]`, enchant-scroll stock `0`).
- Live continuity evidence: daemon lineage remains active (`tmp/live-runner-procs-2348.txt`) and smoke passed (`tmp/cron-smoke-2348.txt`: `ok=1/1`, `lastAction=combat_start`, `level=25`, `exp=4809`, `gold=409`, `code=200`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=11%`, and progression `exp>=4890` or `gold>=419` with daemon continuity.

- 30-min STRATEGY DIRECTOR (23:18 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check: remote `GET /api/agent/thinking/j211y?limit=20` returned `count=20` and the ordered window (`2026-03-16 13:21:02 -> 22:51:14`) showed measurable improvement (`level +1`, `gold +10`, `inventory +3`, `rate/429/cooldown mentions 37`), so CHANGE was not applied.
- Safety/efficiency evidence now: `GET /api/status => 200` (`level=25`, `exp=4489`, `gold=409`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit in this document (early safe gold/no risky spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safely skipped this cycle (`GET /api/npc/list => 200`, `npcs=[]`, enchant-scroll stock `0`).
- Live continuity evidence: daemon continuity preserved (no restart/stop issued) and telemetry posted (`POST /api/agent/thinking => 200`, artifact `tmp/thinking-post-response-2318.json`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=11%`, and progression `exp>=4560` or `gold>=424` with daemon continuity.

- 30-min STRATEGY DIRECTOR (22:48 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check: remote `GET /api/agent/thinking/j211y?limit=20` returned empty (`count=0`), so fallback local posts were recomputed (`tmp/cron-last20-delta-local-2248.json`, `tmp/thinking-post-1147.json -> tmp/thinking-post-2218.json`, `count=20`) and still showed measurable improvement (`level +1`, `gold +20`, `inventory +1`, `rate/429/cooldown mentions 15/20`), so CHANGE was not applied.
- Safety/efficiency evidence now: `GET /api/status => 200` (`level=25`, `exp=4217`, `gold=419`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy is retained explicitly (a) early game safe gold accumulation/no risky enhancement spam, (b) mid game weapon-first after reserve+prereqs, (c) late game armor/accessory broadening with failure-risk controls; minimal safe enhancement path remains prerequisite-gated and safely skipped this cycle (`GET /api/npc/list => 200`, `npcs=[]`, enchant-scroll stock `0`).
- Live continuity evidence: daemon lineage is continuous (`tmp/live-runner-procs-2248.txt` shows `live-runner-daemon.sh` + `live-strategy-runner.js`) and smoke passed (`tmp/cron-smoke-2248.txt`: `ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=25`, `exp=4223`, `gold=414`, `code=200`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=11%`, and progression `exp>=4300` or `gold>=429` with daemon continuity.

- 30-min STRATEGY DIRECTOR (22:18 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, `count=20`, window `2026-03-16 12:20:08 -> 21:50:48`): measurable improvement persisted (`level +1`, `gold +25`, `inventory -5`, `rate/429/cooldown mentions 16/20`) and latest death timestamp stayed unchanged (`2026-03-15 05:09:30`), so CHANGE was not applied.
- 30-min STRATEGY DIRECTOR (22:18 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, `count=20`, window `2026-03-16 12:20:08 -> 21:50:48`): measurable improvement persisted (`level +1`, `gold +25`, `inventory -5`, `rate/429/cooldown mentions 16/20`) and latest death timestamp stayed unchanged (`2026-03-15 05:09:30`), so CHANGE was not applied.
- Safety/efficiency evidence now: `GET /api/status => 200` (`level=25`, `exp=3957`, `gold=409`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion while armor is empty.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)` (inventory still favors `short_sword` over `rusty_sword`); staged enhancement strategy remains explicit (early safe gold/no risky spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safely skipped this cycle (`GET /api/npc/list => 200`, `npcs=[]`, enchant-scroll stock `0`).
- Live continuity evidence: daemon lineage is continuous (`tmp/live-runner-procs-2218.txt` shows `live-runner-daemon.sh` + `live-strategy-runner.js`) and smoke passed (`tmp/cron-smoke-2218.txt`: `ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=25`, `exp=3957`, `gold=409`, `code=200`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=11%`, and progression `exp>=4040` or `gold>=424` with daemon continuity.

- 30-min STRATEGY DIRECTOR (21:48 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check: remote `GET /api/agent/thinking/j211y?limit=20` stayed empty (`count=0`), so fallback local last-20 posts were recomputed (`tmp/cron-last20-delta-2148.json`, `thinking-post-1047.json -> thinking-post-2118.json`, `count=20`) and were flat (`level +0`, `exp +0`, `gold +0`, `inventory +0`, `death +0`) with sustained throttle wording (`rate/429/cooldown mentions 52`), so KEEP was rejected.
- Minimal/reversible CHANGE committed: `config/strategy.env` tuned `BUJU_STALL_429_COOLDOWN_TICKS 24->26` to widen post-429 cooling and reduce repeated `wait_combat_start_rate_limit` re-entry while preserving strict safety/movement/equipment invariants.
- Safety/efficiency evidence now: `GET /api/status => 200` (`level=25`, `exp=3685`, `gold=409`, `area=talking_island_field`), `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keeps safest high-efficiency target as `skeleton`, and strict movement threshold gate remains (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit (early safe gold/no risky enhancement spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safely skipped this cycle (`GET /api/npc/list => 200`, `npcs=[]`, enchant-scroll stock `0`).
- Live continuity evidence: daemon lineage is continuous (`tmp/live-runner-procs-2148.txt` shows `live-runner-daemon.sh` + `live-strategy-runner.js`) and smoke passed (`tmp/cron-smoke-2150.txt`: `ok=1/1`, `lastAction=combat_start`, `level=25`, `exp=3695`, `gold=414`, `code=200`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=11%`, and progression `exp>=3780` or `gold>=429` with daemon continuity.

- 30-min STRATEGY DIRECTOR (21:18 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check: remote `GET /api/agent/thinking/j211y?limit=20` stayed empty (`count=0`), so fallback local last-20 posts were recomputed (`tmp/cron-last20-delta-2118.json`, `thinking-post-1217.json -> thinking-post-2048.json`, `count=20`) and were flat (`level +0`, `exp +0`, `gold +0`, `inventory +0`, `death +0`) with sustained throttle wording (`rate/429/cooldown mentions 40`), so KEEP was rejected.
- Minimal/reversible CHANGE committed: `config/strategy.env` tuned `BUJU_STALL_429_COOLDOWN_TICKS 22->24` (README current-default sync included) to widen post-429 cooling and reduce repeated `wait_combat_start_rate_limit` churn while preserving strict movement/safety/equipment invariants.
- Safety/efficiency evidence now: `GET /api/status => 200` (`level=25`, `exp=3429`, `gold=409`, `area=talking_island_field`), `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keeps safest high-efficiency target as `skeleton`, and movement threshold gate remains strict (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan remains explicit (early safe gold/no risky spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safely skipped this cycle (`GET /api/npc/list => 200`, `npcs=[]`, enchant-scroll stock `0`).
- Live continuity evidence: daemon lineage is continuous (`tmp/live-runner-procs-2118.txt` shows `live-runner-daemon.sh` + `live-strategy-runner.js`) and smoke passed (`tmp/cron-smoke-2118.txt`: `ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `code=200`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=12%`, and progression `exp>=3510` or `gold>=424` with daemon continuity.

- 30-min STRATEGY DIRECTOR (20:48 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, `count=20`, window `2026-03-16 10:19:51 -> 20:22:10`): measurable improvement remained (`level +1`, `gold +25`, `death delta +0`) despite mixed pressure signals (`inventory +1`, `rate/cooldown mentions 16/20`), so CHANGE was not applied.
- Safety/efficiency evidence now: `GET /api/status => 200` (`level=25`, `exp=3155`, `gold=414`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit (early safe gold/no risky enhancement spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safely skipped this cycle (`GET /api/npc/list => 200`, `npcs=[]`, enchant-scroll stock `0`).
- Live continuity evidence: daemon lock-owner lineage remains active (`live-runner-daemon.sh` + `live-strategy-runner.js` both present), smoke passed (`tmp/cron-smoke-2048.txt`: `ok=1/1`, `lastAction=combat_start`, `level=25`, `exp=3165`, `gold=409`, `code=200`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=13%`, and progression `exp>=3245` or `gold>=426` with daemon continuity.

- 30-min STRATEGY DIRECTOR (20:18 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, `count=20`, window `2026-03-16 09:49:44 -> 19:51:31`): measurable improvement remains (`level +1`, `inventory -1`, `death delta +0`) despite economy softness (`gold -15`) and elevated throttle wording (`rate/cooldown mentions 15/20`), so CHANGE was not applied.
- Safety/efficiency evidence now: `GET /api/status => 200` (`level=25`, `exp=2895`, `gold=414`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit (early safe gold/no risky enhancement spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safely skipped this cycle (`GET /api/npc/list => 200`, `npcs=[]`, enchant-scroll stock `0`).
- Live continuity evidence: daemon lock owner remains active (`pgrep` shows `live-runner-daemon.sh` pid `5854`), smoke passed (`tmp/cron-smoke-2018.txt`: `ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=25`, `exp=2895`, `gold=414`, `code=200`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=13%`, and progression `exp>=2980` or `gold>=428` with daemon continuity.

- 30-min STRATEGY DIRECTOR (19:48 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, `count=20`, ordered window `2026-03-16 09:19:24 -> 19:19:55`): measurable improvement persisted (`level +1`, `gold +25`, `inventory +1`, `death-log delta +0`), so CHANGE was not applied.
- Safety/efficiency evidence now: `GET /api/status => 200` (`level=25`, `exp=2621`, `gold=409`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression staged plan (explicit, retained): early-game accumulate gold with no risky enhancement spam; mid-game enhance main weapon first after reserve + prerequisite checks; late-game expand to armor/accessory with failure-risk controls (cooldown + resource gate + non-combat gate).
- Minimal safe enhancement path remains implemented and prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`), safely skipped this cycle because `GET /api/npc/list => 200` with `npcs=[]` and enchant scroll stock `0`.
- Live continuity evidence: stale duplicate daemon lineage cleaned, single lock-owner daemon preserved (`logs/live-runner-daemon.lock` PID `5854`), smoke passed (`tmp/cron-smoke-1948.txt`: `ok=1/1`, `code=200`), and telemetry posted (`POST /api/agent/thinking => 200`, `tmp/thinking-post-response-1948.json`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=14%`, and progression `exp>=2700` or `gold>=430` with daemon continuity.

- 30-min STRATEGY DIRECTOR (19:18 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, `count=20`, window `2026-03-16 08:49:25 -> 18:50:43`): measurable improvement persisted (`level +1`, `gold +15`, `inventory +0`) despite high throttle wording (`rate/cooldown mentions 19/20`), so CHANGE was not applied.
- Safety/efficiency evidence now: `GET /api/status => 200` (`level=25`, `exp=2347`, `gold=414`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)` (live inventory/equipped confirms `short_sword` over `rusty_sword`); staged enhancement strategy remains explicit (early safe gold/no risky spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safely skipped this cycle (`GET /api/npc/list => 200`, `npcs=[]`, no enchant-scroll stock).
- Live continuity evidence: daemon processes remain continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active via `pgrep`) and smoke passed (`tmp/cron-smoke-1918.txt`: `ok=1/1`, `lastAction=buy_mp`, `level=25`, `exp=2351`, `gold=414`, `code=200`); telemetry posted (`POST /api/agent/thinking => 200`, `tmp/thinking-post-response-1918.json`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=13%`, and progression `exp>=2430` or `gold>=425` with daemon continuity.

- 30-min STRATEGY DIRECTOR (18:48 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, `count=20`, window `tick 1773616828119 -> 1773652828046`): deltas were flat (`level +0`, `exp +0`, `gold +0`, `inventory +0`) with sustained throttle wording (`rate/cooldown mentions 18/20`), so KEEP was rejected by the improvement gate.
- Minimal/reversible CHANGE committed: `config/strategy.env` tuned `BUJU_STALL_429_COOLDOWN_TICKS 20->22` (and synced README current-default note) to widen 429 cool-off under persistent `wait_combat_start_rate_limit` pressure while preserving strict risk/movement gates.
- Safety/efficiency evidence now: `GET /api/status => 200` (`level=25`, `exp=2083`, `gold=404`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit (early safe gold/no risky spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safely skipped this cycle (`GET /api/npc/list => 200`, `npcs=[]`).
- Live continuity evidence: daemon processes remain continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active via `pgrep`) and smoke passed (`tmp/cron-smoke-1848.txt`: `ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `code=200`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=13%`, and progression `exp>=2160` or `gold>=420` with daemon continuity.

- 30-min STRATEGY DIRECTOR (18:18 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, `count=20`, window `07:50 -> 17:51 KST`): deltas were mixed (`level +1`, `gold +25`, but `inventory +4`) with sustained throttle wording (`rate/cooldown mentions 19/20`), so KEEP was rejected under the efficiency gate.
- Minimal/reversible CHANGE committed: `config/strategy.env` tuned `BUJU_POTION_USE_MAX_QUANTITY 1->2` to consume stacked potions in safer batches, reducing slot-pressure drift without relaxing movement/safety gates.
- Safety/efficiency evidence now: `GET /api/status => 200` (`level=25`, `exp=1815`, `gold=414`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit (early safe gold/no risky spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safely skipped this cycle (`GET /api/npc/list => 200`, `npcs=[]`, enchant-scroll stock effectively `0`).
- Live continuity evidence: daemon lock PID remains active (`logs/live-runner-daemon.lock`), smoke passed (`tmp/cron-smoke-1818.txt`, `ok=1/1`, `code=200`), and runner process remains continuous.
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=14%`, and progression `exp>=1900` or `gold>=430` with daemon continuity.

- 30-min STRATEGY DIRECTOR (17:48 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`tmp/cron-last20-delta-1748.json`, fallback window `thinking-post-0747.json -> thinking-post-1719.json`, `count=20`): deltas were flat (`level +0`, `exp +0`, `gold +0`, `death +0`) with sustained throttle wording (`rate/cooldown mentions 19/20`), so KEEP was rejected by policy.
- Minimal/reversible CHANGE committed: `config/strategy.env` tuned `BUJU_BACKOFF_BASE_MS 1200->1400` to soften immediate retry collision pressure after throttled responses while preserving strict safety gates and daemon continuity.
- Safety/efficiency evidence now: `GET /api/status => 200` (`level=25`, `exp=1559`, `gold=414`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit (early safe gold/no risky spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safely skipped this cycle (`GET /api/npc/list => 200`, `npcs=[]`, enchant-scroll stock `0`).
- Live continuity evidence: daemon lock PID remains active (`20502`), smoke passed (`tmp/cron-smoke-1748.txt`, `ok=1/1`, `code=200`), and runner process remains continuous.
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=14%`, and progression `exp>=1640` or `gold>=430` with daemon continuity.

- 30-min STRATEGY DIRECTOR (17:19 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`tmp/cron-last20-delta-1719.json`, window `thinking-post-0717.json -> thinking-post-1648.json`, `count=20`): mixed/non-improving deltas (`level +1`, `gold -15`, `inventory +4`, `exp sparse/negative artifact`) with sustained throttle wording (`rate/cooldown mentions 18/20`), so KEEP was rejected by policy.
- Minimal/reversible CHANGE committed: `config/strategy.env` tuned `BUJU_MAX_ACTIONS_PER_CYCLE 2->1` to reduce control-plane churn and lower repeated cooldown/rate-limit collisions while preserving strict safety gates and daemon continuity.
- Safety/efficiency evidence now: `GET /api/status => 200` (`level=25`, `exp=1299`, `gold=404`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit (early safe gold/no risky spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safely skipped this cycle (`GET /api/npc/list => 200`, `npcs=[]`, enchant-scroll stock effectively `0`).
- Live continuity evidence: daemon lock PID remains active (`20502`), smoke passed (`tmp/cron-smoke-1719.txt`, `ok=1/1`, `code=200`), and runner process remains continuous.
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=14%`, and progression `exp>=1380` or `gold>=420` with daemon continuity.

- 30-min STRATEGY DIRECTOR (16:48 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check: remote `GET /api/agent/thinking/j211y?limit=20` returned `count=0`, so fallback local last-20 posts were scored (`tmp/cron-last20-delta-1648.json`, `tmp/thinking-post-0617.json -> tmp/thinking-post-1618.json`, `count=20`) and still showed measurable improvement (`level +1`, `gold +35`, `inventory +0`, `exp +0`), so CHANGE was not applied.
- Safety/efficiency evidence now: `GET /api/status => 200` (`level=25`, `exp=1025`, `gold=414`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit (early safe gold/no risky spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safely skipped this cycle (`GET /api/npc/list => 200`, `npcs=[]`, enchant-scroll stock `0`).
- Live continuity + telemetry evidence: daemon lock PID remains active (`20502`), smoke passed (`tmp/cron-smoke-1648.txt`, `ok=1/1`, `code=200`), and thinking post succeeded after one validation fix (`tmp/thinking-post-response-1648.json`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=14%`, and progression `exp>=1110` or `gold>=430` with daemon continuity.

- 30-min STRATEGY DIRECTOR (16:18 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-16 05:20:43 -> 15:50:10`, `count=20`): measurable improvement persisted (`level +1`, `gold +40`, `inventory -1`, `death proxy +0`), so CHANGE was not applied.
- Safety/efficiency evidence now: `GET /api/status => 200` (`level=25`, `exp=755`, `gold=404`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit (early safe gold/no risky spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safely skipped this cycle (`GET /api/npc/list => 200`, `npcs=[]`, enchant-scroll stock `0`).
- Live continuity evidence: daemon lock PID is active (`logs/live-runner-daemon.lock` => `20502`, process alive) with fresh runner tail updates through 16:18 KST; ops telemetry post succeeded (`POST /api/agent/thinking => 200`, `tmp/thinking-post-response-1618.json`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=14%`, and progression `exp>=1035` or `gold>=435` with daemon continuity.

- 30-min STRATEGY DIRECTOR (15:49 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-16 04:49:22 -> 15:21:13`, `count=20`): measurable improvement persisted (`level +1`, `gold +45`, `inventory +0`), so CHANGE was not applied.
- Safety/efficiency evidence now: `GET /api/status => 200` (`level=25`, `exp=483`, `gold=414`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit (early safe gold/no risky spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safely skipped this cycle (`GET /api/npc/list => 200`, `npcs=[]`, enchant-scroll stock `0`).
- Live continuity evidence: daemon lock PID is active (`logs/live-runner-daemon.lock` => `20502`, process alive) with fresh runner tail updates through 15:49 KST; ops telemetry post succeeded (`POST /api/agent/thinking => 200`, `tmp/thinking-post-response-1549.json`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=14%`, and progression `exp>=760` or `gold>=430` with daemon continuity.

- 30-min STRATEGY DIRECTOR (15:18 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-16 04:20:16 -> 14:50:02`, `count=20`): deltas showed improvement (`level +0`, `gold +40`) with no fresh defeat signal in live runner tail; inventory pressure rose (`+4`) and rate/cooldown wording remained high (`18/20`), but KEEP criteria still met on measurable progression.
- Safety/efficiency evidence now: `GET /api/status => 200` (`level=25`, `exp=213`, `gold=409`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit (early safe gold/no risky spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safely skipped this cycle (`GET /api/npc/list => 200`, `npcs=[]`, enchant-scroll stock `0`).
- Live continuity evidence: daemon remains continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` both active at 15:18 KST) and ops telemetry posted (`POST /api/agent/thinking => 200`, `tmp/thinking-post-response-1518.json`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=14%`, and progression `exp>=300` or `gold>=425` with daemon continuity.

- 30-min STRATEGY DIRECTOR (14:48 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (remote `GET /api/agent/thinking/j211y?limit=20` returned `count=0`, fallback local `tmp/thinking-post-0447.json -> tmp/thinking-post-1418.json`, `count=20`): deltas were flat (`level +0`, `exp +0`, `gold +0`, `inventory +0`) with persistent throttle wording (`rate/cooldown mentions 17/20`), so KEEP was rejected by policy.
- Minimal/reversible CHANGE committed: `config/strategy.env` tuned `BUJU_BASE_DELAY_MS 5000->5400` to lower `combat_start` collision pressure under sustained throttle while preserving safety-first hunt and strict movement gates.
- Safety/efficiency evidence now: live probes `GET /api/status => 200` (`level=24`, `exp=5707`, `gold=404`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`).
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit (early safe gold/no risky spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safely skipped this cycle (`GET /api/npc/list => 200`, `npcs=[]`, no enchant scroll stock).
- Live continuity evidence: daemon remains continuous (`tmp/live-runner-procs-1448.txt`) and smoke returned `ok=1/1`, `lastAction=sell_inventory_cleanup_batch`, `level=24`, `exp=5711`, `gold=404`, `code=200` (`tmp/cron-smoke-1448.txt`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=14%`, and progression `exp>=5790` or `gold>=418` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (14:18 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`tmp/thinking-post-0648.json -> tmp/thinking-post-1348.json`, `count=20`): deltas were flat (`level +0`, `exp +0`, `gold +0`, `inventory +0`) with persistent throttle wording (`rate/cooldown mentions 17/20`), so KEEP was rejected.
- Minimal/reversible CHANGE committed: `config/strategy.env` tuned `BUJU_COMBAT_STRATEGY_REFRESH_TICKS 28->32` to reduce unnecessary `POST /combat/strategy` churn under sustained throttle while preserving hunt/equip/sell safety invariants.
- Safety/efficiency evidence now: live probes `GET /api/status => 200` (`level=24`, `exp=5437`, `gold=409`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`).
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and explicit: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan stays (a) early game: safe gold accumulation/no risky enhancement spam, (b) mid game: weapon-first once reserve+prereqs are met, (c) late game: broaden to armor/accessory with failure-risk controls; minimal safe enhancement action path remains prerequisite-gated and safely skipped this cycle (`GET /api/npc/list => 200`, `npcs=[]`, no enchant-scroll stock).
- Live continuity evidence: daemon remains continuous (`tmp/live-runner-procs-1418.txt`) and smoke returned `ok=1/1`, `lastAction=combat_start`, `level=24`, `exp=5439`, `gold=414`, `code=200` (`tmp/cron-smoke-1418.txt`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=14%`, and progression `exp>=5520` or `gold>=426` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (13:48 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`tmp/thinking-post-0317.json -> tmp/thinking-post-1317.json`, `count=20`): deltas were flat (`level +0`, `exp +0`, `gold +0`, `death +0`, `inventory +0`) with persistent throttle wording (`rate/cooldown mentions 16/20`), so KEEP was rejected by policy.
- Minimal/reversible CHANGE committed: `config/strategy.env` tuned `BUJU_STALL_429_COOLDOWN_TICKS 18->20` to reduce repeated `combat_start` re-entry churn under sustained throttle while preserving safety and progression invariants.
- Safety/efficiency evidence now: live probes `GET /api/status => 200` (`level=24`, `exp=5167`, `gold=414`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`).
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit (early safe gold/no risky spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated, safely skipped this cycle (`GET /api/npc/list => 200`, `npcs=[]`, no enchant scroll stock).
- Live continuity evidence: daemon remains continuous (`tmp/live-runner-procs-1348.txt`) and smoke returned `ok=1/1`, `lastAction=surrender_dangerous_combat`, `level=24`, `exp=5171`, `gold=414`, `code=200` (`tmp/cron-smoke-1348.txt`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=15%`, and progression `exp>=5240` or `gold>=426` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (13:17 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`tmp/cron-last20-delta-1317.json`, window `2026-03-16 02:20:24 -> 11:52:15`, `count=20`): deltas were flat (`level +0`, `exp +0`, `gold +0`, `inventory +0`) with persistent throttle wording (`rate/cooldown mentions 16/20`), so KEEP was rejected by policy.
- Minimal/reversible CHANGE committed: `config/strategy.env` tuned `BUJU_STALL_429_COOLDOWN_TICKS 16->18` to lower repeated `combat_start` retry churn while preserving strict risk controls and live continuity.
- Safety/efficiency evidence now: live probes `GET /api/status => 200` (`level=24`, `exp=4901`, `gold=409`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit (early safe gold/no risky spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated, safely skipped this cycle (`GET /api/npc/list => npcs=[]`, no enchant scroll stock).
- Live continuity evidence: daemon remains continuous (`tmp/live-runner-procs-1317.txt`) and smoke returned `ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=24`, `exp=4907`, `gold=414`, `code=200` (`tmp/cron-smoke-1317.txt`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=15%`, and progression `exp>=4980` or `gold>=426` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (12:47 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check: remote `GET /api/agent/thinking/j211y?limit=20` was empty again, so fallback local last-20 thinking posts were used (`count=19`) and showed mixed/non-improving deltas (`level +1`, `gold -40`, `inventory +2`, `death +0`, `rate/cooldown mentions 16/19`), so KEEP was rejected under the improvement gate.
- Minimal/reversible CHANGE committed: `config/strategy.env` tuned `BUJU_MIN_GOLD_RESERVE 380->400` to reduce discretionary buy-churn and protect economy while preserving safety gates and live continuity.
- Safety/efficiency evidence now: live probes `GET /api/status => 200` (`level=24`, `exp=4625`, `gold=389`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit (early safe gold/no risky spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated, safely skipped this cycle (`GET /api/npc/list => npcs=[]`, no enchant scroll stock).
- Live continuity evidence: daemon remains continuous (`tmp/live-runner-procs-1247.txt`) and smoke returned `ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=24`, `exp=4627`, `gold=394`, `code=200` (`tmp/cron-smoke-1247.txt`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=16%`, and progression `exp>=4700` or `gold>=406` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (12:17 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check: remote `GET /api/agent/thinking/j211y?limit=20` returned empty (`count=0`), so fallback local last-20 thinking posts (`tmp/thinking-post-0217.json -> tmp/thinking-post-1147.json`) were used and showed measurable improvement (`level +1`, `gold +20`, `inventory -3`, `death +0`, `rate/cooldown mentions 16/20`), so CHANGE was not applied.
- Safety/efficiency evidence now: `GET /api/status => 200` (`level=24`, `exp=4375`, `gold=384`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated defeats.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit (early safe gold/no risky spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated, safely skipped this cycle (`GET /api/npc/list => npcs=[]`, no enchant scroll stock).
- Live continuity evidence: daemon remains continuous (`tmp/live-runner-procs-1217.txt`) and smoke returned `ok=1/1`, `lastAction=sell_inventory_cleanup_batch`, `level=24`, `exp=4375`, `gold=384`, `code=200` (`tmp/cron-smoke-1217.txt`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=16%`, and progression `exp>=4445` or `gold>=396` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (11:47 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (local fallback `tmp/thinking-post-*.json`, ordered by `tick_number`, `count=20`): deltas were mixed/anomalous (`level -1`, `gold +45`, `inventory -7`) with sustained throttle wording (`rate/cooldown mentions 16/20`) and fresh smoke still ending on `wait_combat_start_rate_limit`, so KEEP was rejected by policy.
- Minimal/reversible CHANGE committed: `config/strategy.env` tuned `BUJU_MAX_ACTIONS_PER_CYCLE 3->2` to reduce per-cycle control-plane pressure while preserving safe hunt/equip/sell invariants and live daemon continuity.
- Safety/efficiency evidence now: live probes `GET /api/status => 200` (`level=24`, `exp=4117`, `gold=389`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated defeats.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit (early safe gold/no risky spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated, safely skipped this cycle (`GET /api/npc/list => npcs=[]`, no enchant scroll stock).
- Live continuity evidence: daemon remains continuous (`tmp/live-runner-procs-1147.txt`) and smoke returned `ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=24`, `exp=4111`, `gold=384`, `code=200` (`tmp/cron-smoke-1147.txt`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=16%`, and progression `exp>=4190` or `gold>=405` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (10:47 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`tmp/cron-last20-delta-1047.json`, `count=20`): deltas were flat (`level +0`, `exp +0`, `gold +0`, `death +0`, `rateLimitedCount=0`), so KEEP was rejected by policy.
- Minimal/reversible CHANGE committed: `config/strategy.env` tuned `BUJU_BASE_DELAY_MS 4600->5000` to lower `combat_start` rate-limit churn (`tmp/cron-smoke-1047.txt` showed `lastAction=wait_combat_start_rate_limit`) while preserving safety gates, movement thresholds, and inventory invariants.
- Safety/efficiency evidence now: live probes `GET /api/status => 200` (`level=22`, `exp=2707`, `gold=349`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated defeats.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit (early safe gold/no risky spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated, safely skipped this cycle (`GET /api/npc/list => npcs=[]`, no enchant scroll stock).
- Live continuity evidence: daemon remains continuous (`tmp/live-runner-procs-1047.txt`) and smoke returned `ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=22`, `exp=2707`, `gold=349`, `code=200` (`tmp/cron-smoke-1047.txt`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=18%`, and progression `exp>=2780` or `gold>=365` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (10:17 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (fallback local `tmp/thinking-post-*.json`, window `tmp/thinking-post-2347.json -> tmp/thinking-post-0947.json`, `count=20`): deltas were flat (`level +0`, `exp +0`, `gold +0`, `inventory +0`) with persistent throttle wording (`rate/cooldown mentions 15/20`), so KEEP was rejected.
- Minimal/reversible CHANGE committed: `config/strategy.env` tuned `BUJU_RETRY_MAX_ATTEMPTS 1->2` (commit `b9db552`) to improve 429 recovery without relaxing safety/movement controls.
- Safety/efficiency evidence now: live probes `GET /api/status => 200` (`level=24`, `exp=3297`, `gold=389`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`).
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit (early safe gold/no risky spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated, safely skipped this cycle (`GET /api/npc/list => npcs=[]`, no enchant scroll stock).
- Live continuity evidence: daemon remains continuous (`tmp/live-runner-procs-1017.txt`) and smoke returned `ok=1/1`, `lastAction=combat_start`, `level=24`, `exp=3299`, `gold=394`, `code=200` (`tmp/cron-smoke-1017.txt`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=20%`, and progression `exp>=3380` or `gold>=410` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (09:47 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check: remote `GET /api/agent/thinking/j211y?limit=20` returned empty (`count=0`), so fallback local last-20 thinking posts (`tmp/thinking-post-*.json` by `tick_number`) were used and showed flat deltas (`level +0`, `exp +0`, `gold +0`, `inventory +0`) with persistent throttle wording (`rate/cooldown mentions 15/20`), so KEEP was rejected.
- Minimal/reversible CHANGE committed: `config/strategy.env` tuned `BUJU_COMBAT_STRATEGY_REFRESH_TICKS 24->28` (fewer `POST /combat/strategy` refreshes under sustained throttle) while keeping hunt safety and movement thresholds unchanged.
- Safety/efficiency evidence now: `GET /api/status => 200` (`level=24`, `exp=3081`, `gold=419`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`); safest high-efficiency target remains `skeleton`, with strict threshold movement gate (`BUJU_MOVE_LEVEL_2=30`) still enforced.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit (early safe gold/no risky spam -> mid weapon-first at reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated, safely skipped this cycle (`GET /api/npc/list => npcs=[]`, no enchant scroll stock).
- Live continuity evidence: daemon remains continuous (`tmp/live-runner-procs-0947.txt`) and smoke returned `ok=1/1`, `lastAction=combat_start`, `level=24`, `exp=3083`, `gold=424`, `code=200` (`tmp/cron-smoke-0947.txt`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=20%`, and progression `exp>=3160` or `gold>=435` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (09:17 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check: remote `GET /api/agent/thinking/j211y?limit=20` returned empty (`count=0`), so fallback local last-20 thinking posts (`tmp/thinking-post-*.json`, ordered by `tick_number`) were used (`thinking-post-2248.json -> thinking-post-0848.json`, `count=20`) and showed measurable improvement (`level +1`, `gold +35`, `inventory -1`, `rate/cooldown mentions 14/20`), so CHANGE was not applied.
- Safety/efficiency evidence now: `GET /api/status => 200` (`level=24`, `exp=2877`, `gold=389`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`); safest high-efficiency target remains `skeleton`, and strict movement threshold gate (`BUJU_MOVE_LEVEL_2=30`) remains enforced to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit in this document (early safe gold/no risky spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated, safely skipped this cycle (`GET /api/npc/list => npcs=[]`, no enchant scroll stock).
- Live continuity evidence: daemon remains continuous (`tmp/live-runner-procs-0917.txt`) and smoke returned `ok=1/1`, `lastAction=combat_start`, `level=24`, `exp=2879`, `gold=394`, `code=200` (`tmp/cron-smoke-0917.txt`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=20%`, and progression `exp>=2950` or `gold>=410` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (08:48 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`): ordered window (`2026-03-15 21:50:00 -> 2026-03-16 08:20:28`, `count=20`) improved (`level +1`, `gold +45`, `inventory -5`, `rate/cooldown mentions 14/20`), so CHANGE was not applied.
- Safety/efficiency evidence now: live probes show `level=24`, `exp=2665`, `gold=389`, `area=talking_island_field`, and current monster pool (`rabbit`,`skeleton`) keeps `skeleton` as safest high-efficiency target; strict movement threshold gate stays enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Defeat trend evidence: `GET /api/logs?action=death&limit=100 => 200`, latest death timestamp remains `2026-03-15 05:09:30` (no fresh defeat burst in this decision window).
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit in this document (early safe gold/no risky spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated, safely skipped this cycle (`GET /api/npc/list => npcs=[]`, no enchant scroll stock).
- Live continuity evidence: daemon remains continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active), smoke returned `ok=1/1`, `lastAction=combat_start`, `level=24`, `exp=2667`, `gold=394`, `code=200`, and ops telemetry posted (`POST /api/agent/thinking => {"success":true}`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=20%`, and progression `exp>=2745` or `gold>=410` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (08:18 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`): ordered window (`2026-03-15 20:50:04 -> 2026-03-16 07:50:07`, `count=20`) improved (`level +1`, `gold +25`, `inventory -1`, `rate/cooldown mentions 12/20`), so CHANGE was not applied.
- Safety/efficiency evidence now: live probes show `status=200` (`level=24`, `exp=2455`, `gold=394`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`); safest high-efficiency target remains `skeleton` with strict movement threshold gate (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Defeat trend evidence: `GET /api/logs?action=death&limit=100 => 200`, latest death timestamp remains `2026-03-15 05:09:30` (no fresh defeat burst in this decision window).
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit (early safe gold/no risky spam -> mid weapon-first after reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated, safely skipped this cycle (`GET /api/npc/list => npcs=[]`, no enchant scroll stock).
- Live continuity evidence: daemon remains continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active) and smoke returned `ok=1/1`, `lastAction=buy_mp`, `level=24`, `exp=2455`, `gold=394`, `code=200`.
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=20%`, and progression `exp>=2525` or `gold>=410` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (07:47 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`): ordered window (`2026-03-15 20:19:56 -> 2026-03-16 07:20:14`, `count=20`) showed clear improvement (`level +1`, `gold +65`, `inventory -5`, `rate/cooldown mentions 12/20`), so CHANGE was not applied.
- Safety/efficiency evidence now: live probes (`tmp/cron-probes-0747.json`) confirm `level=24`, `exp=2253`, `gold=389`, `area=talking_island_field`; available monsters are (`rabbit`,`skeleton`) and safest high-efficiency target remains `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Defeat trend evidence: `GET /api/logs?action=death&limit=100 => 200`, latest death timestamp remains `2026-03-15 05:09:30` (no fresh defeat burst in this decision window).
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit in this document (early: safe gold accumulation/no risky spam, mid: weapon-first once reserve+prereqs are met, late: armor/accessory broadening with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated, safely skipped this cycle (`GET /api/npc/list => npcs=[]`, no enchant scroll stock).
- Live continuity evidence: daemon remains continuous (`tmp/live-runner-procs-0747.txt`) and smoke returned `ok=1/1`, `lastAction=combat_start`, `level=24`, `exp=2257`, `gold=389`, `code=200` (`tmp/cron-smoke-0747.txt`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=20%`, and progression `exp>=2325` or `gold>=405` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (07:17 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`): ordered window (`count=20`) showed mixed deltas (`level +1`, `exp +0`, `gold +5`) with persistent throttle signal (`rate/cooldown mentions 20/20`), so KEEP was rejected for throughput stagnation despite minor level/gold gain.
- Minimal/reversible CHANGE committed: `config/strategy.env` tuned `BUJU_MAX_ACTIONS_PER_CYCLE 2->3` (commit `1c67476`) to recover measurable progression signal while preserving risk gates and inventory invariants.
- Safety/efficiency evidence now: live probes (`tmp/cron-0717-probes.json`) confirm `area=talking_island_field`, available monsters (`rabbit`,`skeleton`), and safest high-efficiency target remains `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit (early safe gold/no risky spam -> mid weapon-first once reserve+prereqs are met -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safely skipped this cycle (`GET /api/npc/list => npcs=[]`, no enchant scroll stock).
- Live continuity evidence: daemon remains continuous (`tmp/live-runner-procs-0717.txt`), and smoke returned `ok=1/1`, `lastAction=combat_start`, `level=24`, `exp=2049`, `gold=409`, `code=200` (`tmp/cron-smoke-0717.txt`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=20%`, and progression `exp>=2120` or `gold>=430` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (06:47 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`): ordered window (`tick 1773571748929 -> 1773609544244`, `count=20`) produced mixed deltas (`level +1`, `gold +5`, `inventory +4`, `rate/cooldown mentions 11/20`), so KEEP was rejected on inventory-pressure regression despite level/gold gains.
- Minimal/reversible CHANGE committed: `config/strategy.env` tuned `BUJU_MIN_GOLD_RESERVE 360->380` to cut buy-driven slot creep and protect inventory/economy under the existing safest-hunt policy.
- Safety/efficiency evidence now: latest thinking context confirms `area=talking_island_field`; available monsters are (`rabbit`,`skeleton`) and safest high-efficiency target remains `skeleton`; movement remains strictly threshold-gated (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated defeats.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan remains explicit (early safe gold/no risky spam -> mid weapon-first at reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated (safe skip when `scroll + npc + resource` is not satisfiable).
- Live continuity evidence: daemon remains continuous (`tmp/live-runner-procs-0647.txt`) and smoke returned `ok=1/1`, `lastAction=combat_start`, `level=24`, `exp=1823`, `gold=374`, `code=200` (`tmp/cron-smoke-0647.txt`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=22%`, and progression `exp>=1890` or `gold>=390` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (06:17 KST, adaptive mode + equipment progression) KEEP decision with mandatory fallback-evidence path: `GET /api/agent/thinking/j211y?limit=20` returned empty (`count=0`), so last-20 local thinking posts were recomputed by `tick_number` (`tmp/thinking-post-*.json`, window ticks `1773569961313 -> 1773607799940`, count `20`) and showed improvement (`level +1`, `gold +185`, `inventory -1`, `rate/cooldown mentions 12/20`), so CHANGE was not applied.
- Safety/efficiency evidence now: live probes `GET /api/status => 200` (`level=24`, `exp=1585`, `gold=369`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Defeat trend evidence: `GET /api/logs?action=death&limit=100 => 200`, latest death timestamp remains `2026-03-15 05:09:30` (no fresh defeat burst in this decision window).
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan remains explicit (early safe gold/no risky spam -> mid weapon-first at reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated, safely skipped this cycle (`GET /api/npc/list => npcs=[]`, no enchant scroll stock).
- Live continuity evidence: daemon remains continuous (`tmp/live-runner-procs-0617.txt`) and smoke returned `ok=1/1`, `lastAction=combat_start`, `level=24`, `exp=1591`, `gold=374`, `code=200` (`tmp/cron-smoke-0617.txt`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=22%`, and progression `exp>=1660` or `gold>=390` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (05:48 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`): ordered window `2026-03-15 18:50:44 -> 2026-03-16 05:20:43`, `count=20`, deltas showed measurable improvement (`level +1`, `gold +10`, `death +0`) while `exp` in thinking payload remained sparse (`N/A`) and rate/cooldown wording stayed elevated; KEEP accepted with live progression supplement (`status exp=1351`, smoke exp=1357, code=200).
- Safety/efficiency evidence now: `GET /api/status => 200` (`level=24`, `exp=1351`, `gold=384`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict threshold movement gate (`BUJU_MOVE_LEVEL_2=30`) and conservative risk-gap controls remain active to avoid repeat defeats.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan remains explicit (early safe gold/no risky spam -> mid weapon-first at reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated, safely skipped this cycle (`GET /api/npc/list => npcs=[]`, no enchant scroll stock).
- Live continuity evidence: daemon remains continuous (`pgrep` shows `live-runner-daemon.sh` active) and smoke returned `ok=1/1`, `lastAction=combat_start`, `level=24`, `exp=1357`, `gold=369`, `code=200` (`tmp/cron-smoke-0548.txt`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=22%`, and progression `exp>=1435` or `gold>=395` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (05:17 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`): ordered window `2026-03-15 18:19:31 -> 2026-03-16 04:49:22`, `count=20`, produced flat deltas (`level +0`, `exp +0`, `gold +0`, `inventory +0`) with persistent throttle signal (`rate/cooldown mentions 20/20`), so KEEP was rejected for zero-progression evidence.
- Minimal/reversible CHANGE committed: `config/strategy.env` tuned `BUJU_STALL_429_COOLDOWN_TICKS 14->16` to lower repeated `combat_start` retry churn and widen recovery time under sustained 429 pressure while preserving the safest-farm policy.
- Safety/efficiency evidence now: live probes `GET /api/status => 200` (`level=24`, `exp=1127`, `gold=364`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict level-threshold movement gate (`BUJU_MOVE_LEVEL_2=30`) and risk-gap controls stay enforced.
- Defeat trend evidence: `GET /api/logs?action=death&limit=100 => 200`, latest death timestamp remains `2026-03-15 05:09:30` (no fresh defeat burst in this decision window).
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement policy remains explicit (early: gold accumulation/no risky spam, mid: weapon-first only after reserve+prereqs, late: armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated (`GET /api/npc/list => npcs=[]`, enchant scroll stock `0`).
- Live continuity evidence: smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=combat_start`, `level=24`, `exp=1135`, `gold=374`, `code=200` (`tmp/cron-smoke-0517.txt`); daemon remains continuous (`tmp/live-runner-procs-0517.txt`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=22%`, and progression `exp>=1210` or `gold>=390` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (04:47 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`): ordered window `2026-03-15 17:50:30 -> 2026-03-16 04:20:16`, `count=20`, deltas improved (`level +1`, `exp sparse/0`, `gold +5`, `inventory -2`, `rate/cooldown mentions 12/20`), so KEEP was accepted with measurable progression and lower inventory pressure.
- Safety/efficiency evidence now: live probes `GET /api/status => 200` (`level=24`, `exp=899`, `gold=364`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep the safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) and repeated-defeat risk remains bounded.
- Defeat trend evidence: `GET /api/logs?action=death&limit=100 => 200` latest death timestamp remains `2026-03-15 05:09:30` (no new death burst in this decision window).
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and kept active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit in this document (early safe gold/no risky spam -> mid weapon-first at reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated, safely skipped this cycle (`GET /api/npc/list => npcs=[]`, enchant-scroll inventory `0`).
- Live continuity evidence: daemon remains continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active), and smoke returned `ok=1/1`, `lastAction=buy_hp`, `level=24`, `exp=903`, `gold=374`, `code=200` (`tmp/cron-smoke-0447.txt`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=24%`, and progression `exp>=980` or `gold>=390` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (04:17 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`): ordered window `2026-03-15 17:19:05 -> 2026-03-16 03:50:10`, `count=20`, deltas improved (`level +1`, `exp +0`, `gold +5`, `inventory -1`, `rate/cooldown mentions 17/20`), so KEEP was accepted with concrete progression and inventory-pressure relief evidence.
- Safety/efficiency evidence now: live probes `GET /api/status => 200` (`level=24`, `exp=667`, `gold=364`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep the safest high-efficiency target as `skeleton`; strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) and no-armor retreat guard remains active.
- Defeat trend evidence: `GET /api/logs?action=death&limit=100 => 200` with latest death timestamp still `2026-03-15 05:09:30` (no fresh defeat burst in this decision window).
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and kept active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit in this document (early safe gold/no risky spam -> mid weapon-first at reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated, safely skipped this cycle (`GET /api/npc/list => npcs=[]`, enchant-scroll inventory `0`).
- Live continuity evidence: daemon remains continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active), and smoke returned `ok=1/1`, `lastAction=combat_start`, `level=24`, `exp=671`, `gold=374`, `code=200` (`tmp/cron-smoke-0417.txt`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=24%`, and progression `exp>=760` or `gold>=390` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (03:47 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`): ordered window `2026-03-15 16:49:38 -> 2026-03-16 03:20:20`, `count=20`, deltas were mixed (`level +1`, `gold +15`, `inventory +6`, `rate/cooldown mentions 11/20`). Despite level/gold gain, KEEP was rejected because inventory-pressure regression violates the short-window efficiency goal.
- Minimal/reversible CHANGE committed: `config/strategy.env` tuned `BUJU_LOW_HP_POTION_RATIO 0.45->0.50` to trigger earlier potion use in low-HP windows, reducing dangerous-combat churn and lowering repeat-defeat risk while keeping strict movement threshold policy unchanged.
- Safety/efficiency evidence now: live probes `GET /api/status => 200` (`level=24`, `exp=439`, `gold=364`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep the safest high-efficiency target as `skeleton`; movement remains strictly threshold-gated (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Defeat trend check in current decision window: `GET /api/logs?action=death&limit=100` latest death timestamp remains `2026-03-15 05:09:30`, and deaths since current last-20 thinking window are `0`.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and kept active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit in this document (early safe gold/no risky spam -> mid weapon-first at reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated, safely skipped this cycle (`GET /api/npc/list => npcs=[]`, enchant-scroll inventory absent).
- Live continuity evidence: daemon remains continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active), and smoke returned `ok=1/1`, `lastAction=buy_mp`, `level=24`, `exp=443`, `gold=374`, `code=200` (`tmp/cron-smoke-0347.txt`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=24%`, and progression `exp>=520` or `gold>=390` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (03:17 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`): ordered window `2026-03-15 16:19:33 -> 2026-03-16 02:50:44`, `count=20`, deltas showed improvement evidence (`level +0`, `gold +80`, `inventory +0`, `exp sparse`), with `death delta=0` (`GET /api/logs?action=death&limit=200` => `0` rows). KEEP accepted because economy progressed without risk expansion.
- Safety/efficiency evidence now: live `GET /api/status => 200` (`level=24`, `exp=207`, `gold=374`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`); safest high-efficiency target remains `skeleton`, and strict movement threshold gate remains enforced (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and kept active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit in this document (early safe gold/no risky spam -> mid weapon-first at reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated, safely skipped this cycle (`GET /api/npc/list => npcs=[]`, enchant-scroll inventory `0`).
- Live continuity evidence: daemon remains continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active via `ps`), and smoke returned `ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=24`, `exp=199`, `gold=364`, `code=200`.
- Ops telemetry posted: `POST /api/agent/thinking` => `200 {"success":true}` (`tmp/thinking-post-response-0317.json`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, `buy_mp<=1`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=24%`, and progression `exp>=280` or `gold>=390` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (02:48 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check: remote `GET /api/agent/thinking/j211y?limit=20` returned empty (`count=0`), so fallback local trailing posts (`tmp/thinking-post-*.json`, latest 20) was used; deltas were mixed (`level +1`, `gold +10`, `inventory +8`, `rate/cooldown mentions 12/20`), so KEEP was rejected due inventory-pressure regression despite level/gold gain.
- Minimal/reversible CHANGE committed: `config/strategy.env` tuned `BUJU_MAX_ACTIONS_PER_CYCLE 1->2` to increase sell/equip/hunt opportunity per daemon cycle and reduce inventory-pressure drift while preserving safety gates.
- Safety/efficiency evidence now: live probes `GET /api/status => 200` (`level=23`, `exp=5255`, `gold=429`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`) keep safest high-efficiency target as `skeleton`; strict movement threshold gate remains (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and kept active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit (early safe gold/no risky spam -> mid weapon-first at reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path stays prerequisite-gated and safely skipped this cycle (`GET /api/npc/list => npcs=[]`, enchant-scroll inventory `0`).
- Live continuity evidence: daemon remains continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active), and smoke with new tuning returned `ok=2/2`, `lastAction=buy_mp`, `level=23`, `exp=5269`, `gold=404`, `code=200` (`tmp/cron-smoke-0248.txt`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, `buy_mp<=1` per short window, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=24%`, and progression `exp>=5340` or `gold>=416` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (01:47 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`): ordered window `2026-03-15 14:51:29 -> 2026-03-16 01:20:54`, `count=20`, deltas were non-improving (`level +1`, `gold -50`, `inventory +3`, `rate/cooldown mentions 13/20`), so KEEP was rejected on economy/inventory efficiency drift.
- Minimal/reversible CHANGE committed: `config/strategy.env` tuned `BUJU_MIN_HP_POTION_S 5->4` to reduce recurrent potion restock spend while preserving survivability gates and no-risk movement policy.
- Safety/efficiency evidence now: live `GET /api/status => 200` (`level=23`, `exp=4773`, `gold=374`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`); safest high-efficiency target remains `skeleton`, with strict threshold movement gate intact (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and kept active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement strategy remains explicit in this document — (a) early game: safe gold accumulation and no risky enhancement spam, (b) mid game: weapon-first enhancement only when reserve + prerequisites are satisfiable, (c) late game: broaden to armor/accessory with failure-risk controls; minimal safe enhancement action path remains implemented and prerequisite-gated, safely skipped this cycle (`GET /api/npc/list => npcs=[]`, enchant-scroll inventory `0`).
- Live continuity evidence: daemon remains continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active), and one-cycle smoke returned `ok=1/1`, `lastAction=combat_start`, `level=23`, `exp=4781`, `gold=374`, `code=200` (`tmp/cron-smoke-0147.txt`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, `buy_hp<=1` per short window, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=24%`, and progression `exp>=4850` or `gold>=386` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (00:50 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`): ordered window `2026-03-15 13:49:22 -> 2026-03-16 00:20:07`, `count=20`, deltas were mixed (`level +1`, `gold -10`, `inventory +4`, `exp sparse`), so KEEP was rejected for economy/inventory regression despite level gain.
- Minimal/reversible CHANGE committed: `config/strategy.env` tuned `BUJU_MIN_GOLD_RESERVE 340->360` to cut discretionary buy churn and protect reserve while preserving combat survivability gates.
- Safety/efficiency evidence now: live `GET /api/status => 200` (`level=23`, `exp=4295`, `gold=349`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`); safest high-efficiency target remains `skeleton`, and threshold movement gate remains strict (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and kept active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan is explicit in this document — (a) early game: prioritize gold accumulation and avoid risky enhancement spam, (b) mid game: enhance weapon first only when reserve + prerequisites are satisfiable, (c) late game: broaden to armor/accessory with failure-risk controls; minimal safe enhancement action path remains implemented and prerequisite-gated, safely skipped this cycle (`GET /api/npc/list => npcs=[]`, enchant scroll inventory `0`).
- Live continuity evidence: daemon remains continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active), and one-cycle smoke returned `ok=1/1`, `lastAction=combat_start`, `level=23`, `exp=4309`, `gold=354`, `code=200` (`tmp/cron-smoke-0050.txt`).
- Ops telemetry posted: `POST /api/agent/thinking` => `200 {"success":true}` (`tmp/thinking-post-response-0050.json`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, `buy actions<=2` per short window, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=26%`, and progression `exp>=4380` or `gold>=366` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (00:17 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`): ordered window `2026-03-15 13:19:23 -> 23:50:12`, `count=20`, deltas improved (`level +1`, `gold +10`, `inventory +1`, `exp sparse`) with no death growth signal (`GET /api/logs?action=death&limit=200` => `0` rows), so KEEP was accepted.
- Safety/efficiency evidence now: live `GET /api/status => 200` (`level=23`, `exp=4055`, `gold=349`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`); safest high-efficiency target remains `skeleton`, and strict movement threshold gate (`BUJU_MOVE_LEVEL_2=30`) remains enforced to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and kept active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan remains explicit (early safe gold/no risky spam -> mid weapon-first at reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated, safely skipped this cycle (`GET /api/npc/list => npcCount=0`, no enchant scroll in inventory).
- Live continuity evidence: daemon remains continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active), and one-cycle smoke returned `ok=1/1`, `lastAction=combat_start`, `level=23`, `exp=4057`, `gold=354`, `code=200` (`tmp/cron-smoke-0017.txt`).
- Ops telemetry posted: `POST /api/agent/thinking` => `200 {"success":true}` (`tmp/thinking-post-response-0017.json`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=28%`, and progression `exp>=4125` or `gold>=366` with smoke `code=200`.

## 2026-03-15
- 30-min STRATEGY DIRECTOR (23:47 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`): ordered window `12:49:18 -> 23:19:29`, `count=20`, deltas were mixed (`level +1`, `gold -5`, `inventory +1`, `exp sparse`, `rate/cooldown mentions 14/20`), so KEEP was rejected due economy drift.
- Minimal/reversible CHANGE committed: `config/strategy.env` tuned `BUJU_MIN_HP_POTION_S 6->5` to reduce buy churn while preserving survivability and hard safety invariants.
- Safety/efficiency evidence now: live `GET /api/status => 200` (`level=23`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`); safest high-efficiency target remains `skeleton` while strict movement gate (`BUJU_MOVE_LEVEL_2=30`) remains enforced to avoid repeated-defeat risk.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and kept active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan remains explicit (early safe gold/no risky spam -> mid weapon-first on reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`).
- Live continuity evidence: daemon remains continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active) and one-cycle smoke returned `ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=23`, `exp=3813`, `gold=344`, `code=200` (`tmp/cron-smoke-2347.txt`).
- Ops telemetry posted: `POST /api/agent/thinking` => `200 {"success":true}` (`tmp/thinking-post-response-2347.json`).
- KPI target (next 30m): `death delta=0`, inventory `<=8`, `buy_hp<=1` per short window, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=28%`, and progression `exp +>=70` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (22:48 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`): ordered window `12:19:02 -> 22:19:43`, `count=20`, deltas improved (`level +1`, `gold +5`, `inventory -1`, `exp +0`, `rate/cooldown mentions 13/20`), so KEEP was accepted with concrete progression evidence.
- Safety/efficiency evidence now: direct live status/inventory/npc/death probes were auth-blocked this cycle (`UNAUTHORIZED`), so area/target evidence was taken from latest thinking context (`talking_island_field`, monsters `rabbit`,`skeleton`); safest high-efficiency target remains `skeleton` and strict movement threshold gate (`BUJU_MOVE_LEVEL_2=30`) remains enforced to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and kept active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan remains explicit (early safe gold/no risky spam -> mid weapon-first on reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`).
- Live continuity evidence: daemon remains continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active) and one-cycle smoke returned `ok=1/1`, `lastAction=surrender_dangerous_combat`, `level=23`, `exp=3321`, `gold=354`, `code=200` (`tmp/cron-smoke-2248.txt`).
- Ops telemetry post attempt: `POST /api/agent/thinking` returned `401 UNAUTHORIZED`; payload archived (`tmp/thinking-post-2248.json`) with response (`tmp/thinking-post-response-2248.json`) for replay after auth-path recovery.
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=28%`, and progression `exp +>=70` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (22:17 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`): window `11:49:10 -> 21:50:00`, `count=20`, deltas were mixed (`level +1`, `exp +0`, `gold -15`, `rate/cooldown mentions 14/20`), so KEEP was rejected due economy stagnation/bleed despite level gain.
- Minimal/reversible CHANGE committed: `config/strategy.env` tuned `BUJU_MIN_MP_POTION_S 2->1` to reduce recurring `buy_mp` spend/churn while preserving HP safety and combat continuity.
- Safety/efficiency evidence now: live `GET /api/status => 200` (`level=23`, `exp=3081`, `gold=354`, `hp=218/430`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`); safest high-efficiency hunt remains `skeleton`, and strict threshold movement (`BUJU_MOVE_LEVEL_2=30`) remains enforced to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and retained: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan remains explicit (early safe gold/no risky spam -> mid weapon-first at reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`).
- Live continuity evidence: daemon remains continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active) and one-cycle smoke should remain `code=200` after this tuning.
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=28%`, and progression `exp +>=70` or `gold +>=15` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (21:48 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`): ordered window `11:19:04 -> 20:50:04`, `count=20`, deltas improved (`level +1` / `22->23`, `gold +10` / `354->364`, `exp/death sparse`) with throttle wording still elevated (`rate/cooldown mentions 14/20`). KEEP accepted because measurable progression evidence remains positive.
- Safety/efficiency evidence now: live `GET /api/status => 200` (`level=23`, `exp=2835`, `gold=349`, `hp=286/430`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`); safest high-efficiency target remains `skeleton` while strict movement gate (`BUJU_MOVE_LEVEL_2=30`) stays enforced to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and kept active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan remains explicit in this document (early safe gold/no risky spam -> mid weapon-first on reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated (this cycle safely skipped: `GET /api/npc/list => npcs=[]`, enchant-scroll inventory absent).
- Live continuity evidence: daemon remains continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active via `ps`), smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=23`, `exp=2839`, `gold=359`, `code=200` (`tmp/cron-smoke-2148.txt`), and `POST /api/agent/thinking` succeeded (`200 {"success":true}`).
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=28%`, and progression `exp +>=70` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (21:17 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`): ordered window `11:19:04 -> 20:50:04`, `count=20`, deltas improved (`level +1` / `22->23`, `gold +10` / `354->364`, `inventory +0` / `4->4`, `exp sparse in payload`) with throttle wording still elevated (`rate/cooldown mentions 15/20`). KEEP accepted because measurable progression evidence is present.
- Safety/efficiency evidence now: `GET /api/status` was auth-blocked (`401`), so area context was read from latest thinking (`talking_island_field`) and verified via `GET /api/areas/talking_island_field/monsters => 200` (`rabbit`,`skeleton`); safest high-efficiency target remains `skeleton` while strict movement gate (`BUJU_MOVE_LEVEL_2=30`) stays enforced to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and kept active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan remains explicit in this document (early safe gold/no risky spam -> mid weapon-first on reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated.
- Live continuity evidence: daemon remains continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active via `pgrep`) and smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=23`, `exp=2593`, `gold=354`, `code=200` (`tmp/cron-smoke-2117.txt`).
- Ops telemetry post attempt: `POST /api/agent/thinking` failed auth (`401 UNAUTHORIZED`, `tmp/thinking-post-response-2117.json`); payload retained at `tmp/thinking-post-2117.json` for replay after key-path recovery.
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=28%`, and progression `exp +>=70` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (20:46 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`): ordered window `10:50:04 -> 20:19:56`, `count=20`, deltas improved (`level +1` / `22->23`, `gold +10` / `349->359`, `inventory +1` / `7->8`, `exp sparse in payload`) with heavy throttle wording (`rate/cooldown mentions 19/20`). KEEP accepted because measurable progression exists and live smoke remained healthy (`code=200`, `exp 2349->2353`, `gold 354->364`).
- Safety/efficiency evidence now: live `GET /api/status => 200` (`level=23`, `hp=247/430`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters` => (`rabbit`,`skeleton`); safest high-efficiency target remains `skeleton` while strict threshold movement gate (`BUJU_MOVE_LEVEL_2=30`) stays enforced to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells all unequipped gear worse than equipped first before general cleanup.
- Equipment progression requirements revalidated and kept active: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan remains explicit in this document (early safe gold/no risky spam -> mid weapon-first on reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated, safely skipped this cycle (`GET /api/npc/list => npcs=[]`, enchant-scroll inventory absent).
- Live continuity evidence: daemon remains continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active via `pgrep`) and smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=buy_mp`, `level=23`, `exp=2353`, `gold=364`, `code=200` (`tmp/cron-smoke-2046.txt`).
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=28%`, and progression `exp +>=70` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (20:16 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (local `tmp/thinking-post-*.json` latest 20): `level +1` (`22->23`), `gold -160` (`359->199` snapshot drift), `inventory +0` (`3->3`), and `rate/cooldown mentions 19/20`. KEEP accepted because level improvement is explicit and live probes recovered economy/progression (`GET /api/status => level=23`, `exp=2111`, `gold=359`, `area=talking_island_field`) with smoke success (`ok=1/1`, `code=200`).
- Safety/efficiency evidence now: live `GET /api/areas/talking_island_field/monsters` remains (`rabbit`,`skeleton`), so safest high-efficiency target remains `skeleton`; strict threshold movement gate (`BUJU_MOVE_LEVEL_2=30`) stays enforced to avoid repeated-defeat risk expansion.
- Hard constraints preserved as runtime invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells unequipped gear worse than equipped first. Current inventory is under trigger (`slots.used=8/30`).
- Equipment progression requirements revalidated and unchanged: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan remains explicit (early safe accumulation/no risky spam -> mid weapon-first on reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated, safely skipped this cycle (`GET /api/npc/list => npcs=[]`, no enchant scroll).
- Live continuity evidence: daemon remains continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active by `pgrep`) and one-cycle smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `lastAction=surrender_dangerous_combat`, `level=23`, `exp=2109`, `gold=354`, `code=200` (`tmp/cron-smoke-2016b.txt`).
- KPI target (next 30m): `death delta=0` (latest death timestamp remains `05:09:30`), inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=28%`, and progression `exp +>=70` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (19:46 KST, adaptive mode + equipment progression) KEEP decision from mandatory `GET /api/agent/thinking/j211y?limit=20` delta check (`count=20`, window `09:48:50 -> 19:19:31`): deltas were `level +1` (`22->23`), `gold -155` (transient low snapshot included), `inventory +0`, `rate/cooldown mentions 16/20`. KEEP accepted because level progressed and live probes recovered economy/progression (`GET /api/status => 200`, `exp=1869`, `gold=364`).
- Safety/efficiency evidence now: live `GET /api/areas/talking_island_field/monsters` remains (`rabbit`,`skeleton`), so safest high-efficiency target remains `skeleton`; strict threshold movement gate (`BUJU_MOVE_LEVEL_2=30`) remains enforced to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still sells unequipped gear worse than equipped first.
- Equipment progression requirements revalidated and unchanged: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan stays explicit (early safe accumulation/no risky spam -> mid weapon-first on reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safely skipped this cycle (`GET /api/npc/list => 200`, `npcCount=0`, no enchant scroll).
- Live continuity evidence: daemon remains continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active by `pgrep`), smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=surrender_dangerous_combat`, `level=23`, `exp=1869`, `gold=364`, `code=200` (`tmp/cron-smoke-1946.txt`), and `POST /api/agent/thinking` succeeded (`200 {"success":true}`).
- KPI target (next 30m): `death delta=0` (latest death timestamp stays `05:09:30`), inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=30%`, and progression `exp +>=70` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (20:16 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check: remote `GET /api/agent/thinking/j211y?limit=20` returned empty (`count=0`), so fallback local `tmp/thinking-post-*.json` last-20 window (`thinking-post-0846.json -> thinking-post-1816.json`) was used and deltas were flat (`level +0`, `exp +0`, `gold +0`, `inventory +0`, rate/cooldown mentions `13/20`), so KEEP was rejected.
- Minimal/reversible CHANGE applied and committed: `config/strategy.env` tuned `BUJU_MAX_ACTIONS_PER_CYCLE 2->1` to cut control-plane churn under sustained combat-start throttling while preserving all hard safety/efficiency constraints.
- Live evidence now: `GET /api/status => 200` (`level=23`, `exp=1623`, `gold=199`, `hp=225/430`, `area=talking_island_field`), `GET /api/areas/talking_island_field/monsters => rabbit(lv1), skeleton(lv2)`; safest high-efficiency target remains `skeleton`, and strict threshold movement (`BUJU_MOVE_LEVEL_2=30`) stays enforced to avoid repeated-defeat risk expansion.
- Hard constraints preserved as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation keeps selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated and kept explicit in this plan:
  1) Best-in-slot auto-equip remains `equipSlot + score(maxDamage+defBonus)`.
  2) Early game: prioritize gold accumulation and avoid risky enhancement spam.
  3) Mid game: enhance main weapon first only when reserve+prerequisites are satisfiable.
  4) Late game: broaden to armor/accessory with cooldown/failure-risk controls.
  5) Minimal safe enhancement path remains implemented and prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`); this cycle safely skipped (`npcCount=0`, enchant-scroll inventory absent).
- Live continuity evidence: daemon is continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active via `pgrep`), and smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=23`, `exp=1629`, `gold=214`, `code=200` (`tmp/cron-smoke-2016.txt`).
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=28%`, and progression `exp +>=70` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (18:46 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`): ordered window `08:49:44 -> 18:19:31`, `count=20`, deltas were `level +1`, `gold +0`, `exp sparse`, `inventory +3`, `rate/cooldown mentions 16/20`. KEEP accepted (no CHANGE commit) because level improvement is still positive evidence and no fresh death growth appeared in-window.
- Safety/efficiency evidence now: live `GET /api/status => 200` (`level=23`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters` => (`rabbit`,`skeleton`); safest high-efficiency target remains `skeleton` while movement stays threshold-gated (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Hard constraints preserved exactly as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation keeps selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated:
  1) BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)` and runs before combat path.
  2) Enhancement staged plan remains explicit: early safe gold/no risky spam -> mid weapon-first when reserve+prereqs are met -> late armor/accessory broadening with failure-risk controls.
  3) Minimal safe enhancement path remains implemented and safely skipped this cycle (`GET /api/npc/list` => `npcCount=0`, enchant-scroll inventory absent).
- Live continuity evidence: daemon remains continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active by `pgrep`), and smoke `BUJU_MAX_ACTIONS_PER_CYCLE=3 node scripts/live-strategy-runner.js` completed with cooldown sample (`ok=1/3`, `code=400`) without process failure.
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=30%`, and progression `exp +>=60` or `gold +>=10` with at least one smoke `code=200`.

- 30-min STRATEGY DIRECTOR (18:16 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`): ordered window `08:19:19 -> 17:50:30`, `count=20`, deltas improved (`level +1`, `gold +10`, `exp sparse/0`, `death sparse/0`) with high throttle wording (`rate/cooldown mentions 19/20`). KEEP accepted (no CHANGE commit) because measurable progression remained positive and no new defeat-growth signal was required in-window.
- Safety/efficiency evidence now: live `GET /api/status => 200` (`level=23`, `exp=1145`, `gold=354`, `hp=214/430`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters` => (`rabbit`,`skeleton`); safest high-efficiency target remains `skeleton` while movement stays threshold-gated (`BUJU_MOVE_LEVEL_2=30`) to avoid repeated-defeat risk expansion.
- Hard constraints preserved exactly as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation keeps selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated:
  1) BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)` and runs before combat path.
  2) Enhancement staged plan remains explicit: early safe gold/no risky spam -> mid weapon-first when reserve+prereqs are met -> late armor/accessory broadening with failure-risk controls.
  3) Minimal safe enhancement path remains implemented but safely skipped this cycle (`GET /api/npc/list` => `npcCount=0`, enchant-scroll inventory absent).
- Live continuity evidence: daemon remains continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active), and smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=23`, `exp=1147`, `gold=359`, `code=200` (`tmp/cron-smoke-1816.txt`).
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=25%`, and progression `exp +>=80` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (17:46 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`): ordered window `07:48:48 -> 17:19:05`, `count=20`, deltas improved (`level +1`, `gold +5`, `exp +0`, `death +0`) with bounded throttle wording (`rate/cooldown mentions 14/20`). KEEP accepted (no CHANGE commit) because level/gold progress remained positive and no numeric death growth appeared in-window.
- Safety/efficiency evidence now: live `GET /api/status => 200` (`level=23`, `exp=915`, `gold=359`, `hp=255/430`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters` => (`rabbit`,`skeleton`); safest high-efficiency target remains `skeleton` under strict movement threshold gate (`BUJU_MOVE_LEVEL_2=30`) and repeated-defeat avoidance.
- Hard constraints preserved exactly as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation continues selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated:
  1) BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)` and is enforced before combat actions.
  2) Enhancement staged plan remains explicit in this doc: (a) early game = gold accumulation/no risky spam, (b) mid game = weapon-first when reserve/prereqs are met, (c) late game = armor/accessory expansion with failure-risk controls.
  3) Minimal safe enhancement action path remains implemented and prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`); this cycle safely skipped because `GET /api/npc/list` returned `npcCount=0` and enchant-scroll inventory stayed `0`.
- Live continuity evidence: daemon remains continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active), smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=combat_start`, `level=23`, `exp=913`, `gold=354`, `code=200` (`tmp/cron-smoke-1746.txt`).
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=25%`, and progression `exp +>=80` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (17:16 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`): ordered window `07:19:47 -> 16:49:38`, `count=20`, deltas improved (`level +1` / `22->23`, `gold +10` / `349->359`, `inventory -7` / `9->2`) with bounded throttle wording (`rate/cooldown mentions 14/20`). KEEP accepted (no CHANGE commit) because progression and inventory pressure both improved with no fresh defeat burst.
- Safety/efficiency evidence now: live `GET /api/status => 200` (`level=23`, `exp=663`, `gold=359`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters` => (`rabbit`,`skeleton`); safest high-efficiency target remains `skeleton` under strict movement threshold gate (`BUJU_MOVE_LEVEL_2=30`) and repeated-defeat avoidance.
- Hard constraints preserved exactly as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation continues selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated:
  1) BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)` and is enforced before combat actions.
  2) Enhancement staged plan remains explicit in this doc: (a) early game = gold accumulation/no risky spam, (b) mid game = weapon-first when reserve/prereqs are met, (c) late game = armor/accessory expansion with failure-risk controls.
  3) Minimal safe enhancement action path remains implemented and prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`); this cycle safely skipped because `GET /api/npc/list` returned `npcCount=0` and inventory had no enchant scroll.
- Live continuity evidence: daemon remains continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active), smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `code=200` (`tmp/cron-smoke-1716.txt`), and telemetry post succeeded (`POST /api/agent/thinking` => `{"success":true}` via `tmp/thinking-post-response-1716.json`).
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=25%`, and progression `exp +>=80` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (16:46 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check: remote `GET /api/agent/thinking/j211y?limit=20` returned empty (`count=0`), so fallback local last-20 posts (`tmp/thinking-post-*.json` sorted by `tick_number`, `07:16 -> 15:46 KST`) were used; computed deltas improved (`level +0`, `gold +15`, `inventory -5`, `exp sparse in payload`) with bounded throttle wording (`rate/cooldown mentions 14/20`). KEEP accepted because economy/inventory trend improved and no new defeat burst signal was required for intervention.
- Safety/efficiency evidence now: live probes `GET /api/status => 200` (`level=23`, `exp=423`, `gold=359`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters => (rabbit, skeleton)` keep the safest high-efficiency target as `skeleton` under strict movement threshold gate (`BUJU_MOVE_LEVEL_2=30`) and repeated-defeat avoidance.
- Hard constraints reaffirmed unchanged as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated: BiS auto-equip remains active by `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan remains explicit (early safe accumulation/no risky spam -> mid weapon-first once reserve/prereqs are met -> late armor/accessory with failure-risk controls). Minimal safe enhancement path is implemented and prerequisite-gated; this cycle safely skipped due unsatisfied prerequisites (`GET /api/npc/list => npcCount=0`, enchant scroll inventory absent).
- Live continuity evidence: daemon remains continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active), and one-cycle smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` kept `code=200`.
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=25%`, and progression `exp +>=80` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (16:18 KST, adaptive mode + equipment progression) KEEP decision from mandatory `GET /api/agent/thinking/j211y?limit=20` delta check (`05:52:42 -> 15:48:56`, `count=20`): deltas were `level +0`, `gold +15`, `inventory +4`, `death +0`, with high throttle wording (`rate/cooldown mentions 19/20`). KEEP remained valid because live progression advanced (`status level=23`, smoke `ok=1/1`, `code=200`) and no newer death timestamp appeared after `05:09`.
- Safety/efficiency policy kept unchanged: safest available high-efficiency target remains `skeleton` in `talking_island_field` under strict movement threshold (`BUJU_MOVE_LEVEL_2=30`) and repeated-defeat avoidance gates.
- Hard constraints reaffirmed as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation prioritizes unequipped gear worse than equipped first.
- Equipment progression requirements revalidated: BiS auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan remains (early safe accumulation/no risky spam -> mid weapon-first at reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safely skipped this cycle (`/api/npc/list` => empty, enchant scroll absent).
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=25%`, and progression `exp +>=80` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (15:46 KST, adaptive mode + equipment progression) CHANGE decision from mandatory `GET /api/agent/thinking/j211y?limit=20` delta check (`05:20:31 -> 15:20:05`, `count=20`): payload deltas were flat (`level +0`, `exp +0`, `gold +0`, `inventory +0`) with sustained throttle signal (`rate/cooldown mentions 18/20`), so KEEP was rejected.
- Minimal/reversible CHANGE applied: `config/strategy.env` tuned `BUJU_BASE_DELAY_MS 4200->4600` to reduce combat-start collision pressure while preserving all hard safety gates and threshold-based movement.
- Safety/efficiency evidence now: live `GET /api/status` => `200` (`level=22`, `exp=4781`, `gold=364`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters` => (`rabbit`, `skeleton`); safest high-efficiency target remains `skeleton` under strict move threshold (`BUJU_MOVE_LEVEL_2=30`) and repeated-defeat avoidance.
- Hard constraints reaffirmed unchanged as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated and retained: BiS auto-equip by `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan remains explicit (early safe accumulation/no risky spam -> mid weapon-first on reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safely skips when prereqs are missing (`GET /api/npc/list` probe unresolved this cycle).
- Live validation now: smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=22`, `exp=4781`, `gold=364`, `code=200`; daemon continuity confirmed (`live-runner-daemon.sh` + `live-strategy-runner.js` active).
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=25%`, and progression `exp +>=80` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (15:16 KST, adaptive mode + equipment progression) CHANGE decision from mandatory `GET /api/agent/thinking/j211y?limit=20` delta check (`04:20:01 -> 14:51:29`, `count=20`): payload deltas were flat (`level +0`, `exp +0`, `gold +0`) with high throttle signal (`rate/cooldown mentions 18/20`), so KEEP was rejected.
- Minimal/reversible CHANGE applied: `config/strategy.env` tuned `BUJU_MAX_ACTIONS_PER_CYCLE 3->2` to lower rate-limit collisions while preserving strict safety gates and threshold-based movement.
- Safety/efficiency evidence now: live `GET /api/status` => `200` (`level=22`, `exp=4551`, `gold=359`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters` => (`rabbit`, `skeleton`); safest high-efficiency target remains `skeleton` under strict move threshold (`BUJU_MOVE_LEVEL_2=30`) and repeated-defeat avoidance.
- Hard constraints reaffirmed unchanged as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated and retained: BiS auto-equip by `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan (early safe accumulation/no risky spam -> mid weapon-first on reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safely skips this cycle (`GET /api/npc/list => 200`, `npcCount=0`).
- Live validation now: smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=22`, `exp=4551`, `gold=359`, `code=200`; daemon continuity confirmed (`live-runner-daemon.sh` + `live-strategy-runner.js` active).
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=28%`, and progression `exp +>=80` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (14:49 KST, adaptive mode + equipment progression) KEEP decision from mandatory `GET /api/agent/thinking/j211y?limit=20` delta check (`03:50:33 -> 14:19:02`, `count=20`): `level +1` (`21->22`), `gold +30` (`324->354`), `inventory -3` (`7->4`). Thinking payload `exp` is sparse, so progression validation used live smoke/status (`live-strategy ok=1/1`, `lastAction=combat_start`, `level=22`, `exp=4355`, `gold=359`, `code=200`).
- Death/risk evidence: wide window still contains historical burst (`death +14`, latest timestamp remains `05:09`), but no newer death timestamp since then, so immediate trend is stable; safest high-efficiency target stays `skeleton` in `talking_island_field` under strict move threshold (`BUJU_MOVE_LEVEL_2=30`) and repeated-defeat avoidance.
- Hard constraints kept invariant and unchanged: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, sell unequipped gear worse than equipped first.
- Equipment progression requirements retained: BiS auto-equip by `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan (early safe gold/no spam -> mid weapon-first on reserve -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safely skipped this cycle (`/api/npc/list` `npcCount=0`).
- Ops/continuity evidence: telemetry posted successfully (`POST /api/agent/thinking` => `{"success":true}`) and daemon continuity confirmed (`live-runner-daemon.sh` active).
- KPI target (next 30m): death delta `=0` from now, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=30%`, and progression `exp +>=80` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (14:16 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check: remote `GET /api/agent/thinking/j211y?limit=20` returned empty (`count=0`), so fallback local last-20 thinking posts (`tmp/thinking-post-*.json`) were used (`thinking-post-0416.json -> thinking-post-1346.json`, `count=20`) and deltas improved (`level +1` / `21->22`, `gold +30` / `329->359`, `inventory -4` / `7->3`, `death +0`). Therefore CHANGE was not applied.
- Safety/efficiency evidence kept: live `GET /api/status` => `200` (`level=22`, `exp=4133`, `gold=354`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters` => (`rabbit`, `skeleton`); safest high-efficiency target remains `skeleton` under strict movement threshold (`BUJU_MOVE_LEVEL_2=30`) and repeated-defeat avoidance gates.
- Hard constraints reaffirmed unchanged as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated and retained:
  1) Best-in-slot auto-equip remains `equipSlot + score(maxDamage+defBonus)`.
  2) Early game: safe gold accumulation and no risky enhancement spam.
  3) Mid game: weapon-first enhancement only when reserve and prerequisites are satisfiable.
  4) Late game: broaden to armor/accessory with failure-risk controls.
  5) Minimal safe enhancement path remains prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`) and safely skips when unsatisfied this cycle (`GET /api/npc/list => 200`, `npcCount=0`).
- Live validation now: smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=buy_mp`, `level=22`, `exp=4137`, `gold=364`, `code=200`; daemon continuity confirmed (`live-runner-daemon.sh` + `live-strategy-runner.js` active).
- Ops telemetry posted: `POST /api/agent/thinking` => `200 {"success":true}` (`tmp/thinking-post-response-1416.json`; first attempt rejected due reasoning length, corrected and reposted).
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=30%`, and progression `exp +>=80` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (13:46 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-15 01:49:26 -> 13:19:23`, `count=20`): computed deltas improved (`level +1` / `21->22`, `gold +20` / `314->334`, `exp sparse in thinking payload`) with no rate-limit wording (`429/rate-limit mentions 0/20`), so CHANGE was not applied.
- Safety/efficiency evidence kept: live `GET /api/status` => `200` (`level=22`, `exp=3931`, `gold=359`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters` => (`rabbit`, `skeleton`); safest high-efficiency target remains `skeleton` under strict movement threshold (`BUJU_MOVE_LEVEL_2=30`) and repeated-defeat avoidance.
- Hard constraints reaffirmed unchanged as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated and retained:
  1) Best-in-slot auto-equip remains `equipSlot + score(maxDamage+defBonus)`.
  2) Early game: safe gold accumulation and no risky enhancement spam.
  3) Mid game: weapon-first enhancement only when reserve and prerequisites are satisfiable.
  4) Late game: broaden to armor/accessory with failure-risk controls.
  5) Minimal safe enhancement path remains prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`) and safely skips when unsatisfied this cycle (`GET /api/npc/list => 200`, `npcCount=0`, enchant scroll count `0`).
- Live validation now: smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=combat_start`, `level=22`, `exp=3935`, `gold=349`, `code=200`; daemon continuity confirmed (`live-runner-daemon.sh` + `live-strategy-runner.js` active).
- KPI target (next 30m): `death delta=0` in immediate window, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=30%`, and progression `exp +>=80` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (13:16 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-15 01:19:42 -> 12:49:18`, `count=20`): computed deltas improved (`level +1` / `21->22`, `gold +55` / `279->334`, `inventory +0` / `6->6`) with no rate-limit wording (`429/rate-limit mentions 0/20`), so CHANGE was not applied.
- Safety/efficiency evidence kept: live `GET /api/status` => `200` (`level=22`, `exp=3725`, `gold=334`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters` => (`rabbit`, `skeleton`); safest high-efficiency target remains `skeleton` under strict movement threshold (`BUJU_MOVE_LEVEL_2=30`) and repeated-defeat avoidance.
- Hard constraints reaffirmed unchanged as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated and retained:
  1) Best-in-slot auto-equip remains `equipSlot + score(maxDamage+defBonus)` (live check: BiS weapon `short_sword` already equipped).
  2) Early game: safe gold accumulation and no risky enhancement spam.
  3) Mid game: weapon-first enhancement only when reserve and prerequisites are satisfiable.
  4) Late game: broaden to armor/accessory with failure-risk controls.
  5) Minimal safe enhancement path remains prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`) and safely skips when unsatisfied this cycle (`GET /api/npc/list => 200`, `npcCount=0`, enchant scroll count `0`).
- Live validation now: smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=combat_start`, `level=22`, `exp=3727`, `gold=339`, `code=200`; daemon continuity confirmed (`live-runner-daemon.sh` + `live-strategy-runner.js` active).
- Ops telemetry posted successfully after one validation retry (`400 action_detail` -> corrected): `POST /api/agent/thinking` => `200 {"success":true}` (`tmp/thinking-post-response-1316.json`).
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=30%`, and progression `exp +>=80` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (12:46 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-15 00:49:26 -> 12:19:02`, `count=20`): computed deltas improved (`level +1` / `21->22`, `gold +20` / `339->359`, `inventory -1` / `7->6`) with bounded throttle signal (`rate-limit/429 mentions 11/20`), so CHANGE was not applied.
- Safety/efficiency evidence kept: live `GET /api/status` => `200` (`level=22`, `exp=3535`, `gold=359`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters` => (`rabbit`, `skeleton`); safest high-efficiency target remains `skeleton` under strict movement threshold (`BUJU_MOVE_LEVEL_2=30`) and repeated-defeat avoidance.
- Hard constraints reaffirmed unchanged as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated and retained:
  1) Best-in-slot auto-equip remains `equipSlot + score(maxDamage+defBonus)`.
  2) Early game: safe gold accumulation and no risky enhancement spam.
  3) Mid game: weapon-first enhancement only when reserve and prerequisites are satisfiable.
  4) Late game: broaden to armor/accessory with failure-risk controls.
  5) Minimal safe enhancement path remains prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`) and safely skips when unsatisfied (current probe: `npcCount=0`).
- Live validation now: smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=combat_start`, `level=22`, `exp=3537`, `gold=364`, `code=200`; daemon continuity confirmed (`live-runner-daemon.sh` + `live-strategy-runner.js` active).
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=30%`, and progression `exp +>=80` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (12:16 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-15 00:19:04 -> 11:49:10`, `count=20`): computed deltas improved (`level +1` / `21->22`, `gold +45` / `319->364`, `inventory +3` / `4->7`). Death logs still include legacy burst inside that wider window (`death +14`), but fresh-growth signal remains controlled (`latest death 05:09 KST`, `death delta from now=0`), so CHANGE was not applied.
- Safety/efficiency evidence kept: live `GET /api/status` => `200` (`level=22`, `exp=3319`, `gold=349`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters` => (`rabbit`, `skeleton`); safest high-efficiency target remains `skeleton` under strict movement threshold (`BUJU_MOVE_LEVEL_2=30`) and conservative risk-gap controls.
- Hard constraints reaffirmed unchanged as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated and retained:
  1) Best-in-slot auto-equip remains `equipSlot + score(maxDamage+defBonus)`.
  2) Early game: safe gold accumulation and no risky enhancement spam.
  3) Mid game: weapon-first enhancement only when reserve and prerequisites are satisfiable.
  4) Late game: broaden to armor/accessory with failure-risk controls.
  5) Minimal safe enhancement path remains prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`) and safely skips when unsatisfied.
- Live validation now: smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=combat_start`, `level=22`, `exp=3327`, `gold=349`, `code=200`; daemon continuity confirmed (`live-runner-daemon.sh` + `live-strategy-runner.js` active).
- KPI target (next 30m): `death delta=0` from now, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=30%`, and progression `exp +>=80` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (11:46 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check: remote `GET /api/agent/thinking/j211y?limit=20` returned empty (`count=0`), so fallback local last-20 thinking posts (`tmp/thinking-post-*.json`) were used (`window 2026-03-15 02:16 -> 10:46 KST`) and showed improvement (`level +1` / `21->22`, `gold +10` / `339->349`) with no fresh death growth (`death logs latest still 05:09 KST`) and bounded throttle signal (`rate-limit/429 mentions 12/20`), so CHANGE was not applied.
- Live safety/efficiency evidence kept: `GET /api/status` => `200` (`level=22`, `exp=3119`, `gold=349`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters` => (`rabbit`, `skeleton`), so safest high-efficiency hunt remains `skeleton` under strict movement threshold (`BUJU_MOVE_LEVEL_2=30`) and repeated-defeat avoidance.
- Hard constraints reaffirmed unchanged as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated and retained:
  1) Best-in-slot auto-equip remains `equipSlot + score(maxDamage+defBonus)`.
  2) Early game: safe gold accumulation and no risky enhancement spam.
  3) Mid game: weapon-first enhancement only when reserve and prerequisites are satisfiable.
  4) Late game: broaden to armor/accessory with failure-risk controls.
  5) Minimal safe enhancement path remains prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`) and safely skips when unsatisfied.
- Live validation now: smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=buy_mp`, `level=22`, `exp=3125`, `gold=364`, `code=200`; daemon continuity confirmed (`live-runner-daemon.sh` + `live-strategy-runner.js` active).
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=30%`, and progression `exp +>=80` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (11:16 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-14 23:19:35 -> 2026-03-15 10:50:04`): deltas improved (`level +1` / `21->22`, `gold +25` / `324->349`, `inventory +7` / `0->7`) with low rate-limit signal (`1/20`). `exp` is sparse in thinking payload, but live status/smoke confirms progression (`exp=2909` and smoke `2913`), so CHANGE was not applied.
- Safety/efficiency evidence kept: live status `GET /api/status` => `200` (`level=22`, `exp=2909`, `gold=354`, `area=talking_island_field`), and current monsters remain low-risk/high-efficiency (`rabbit`, `skeleton`), so safest target remains `skeleton` under strict movement threshold (`BUJU_MOVE_LEVEL_2=30`) and repeated-defeat avoidance.
- Hard constraints reaffirmed unchanged as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated and retained:
  1) Best-in-slot auto-equip remains `equipSlot + score(maxDamage+defBonus)`.
  2) Early game: safe gold accumulation and no risky enhancement spam.
  3) Mid game: weapon-first enhancement only when reserve and prerequisites are satisfiable.
  4) Late game: broaden to armor/accessory with failure-risk controls.
  5) Minimal safe enhancement path remains prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`) and safely skips when unsatisfied.
- Live validation now: smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=buy_mp`, `level=22`, `exp=2913`, `gold=364`, `code=200`; daemon continuity confirmed (`live-runner-daemon.sh` + `live-strategy-runner.js` active).
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=30%`, and progression `exp +>=80` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (10:46 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, ordered window `2026-03-14 22:49:55 -> 2026-03-15 10:19:36`, `count=20`): computed deltas were flat (`level +0`, `exp +0`, `gold +0`, `death +0`, `rate_limited 0/20`), so KEEP was rejected.
- Minimal/reversible CHANGE applied: `config/strategy.env` tuned `BUJU_MAX_ACTIONS_PER_CYCLE 2->3` to restore measurable progression signal while preserving strict safety controls and threshold-based movement.
- Safety/efficiency evidence kept: `GET /api/status` => `200` (`level=22`, `exp=2707`, `gold=349`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters` => (`rabbit`,`skeleton`), so safest high-efficiency target remains `skeleton` under strict move gate (`BUJU_MOVE_LEVEL_2=30`) and repeated-defeat avoidance.
- Hard constraints reaffirmed unchanged as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, sell unequipped gear worse than equipped first.
- Equipment progression requirements revalidated and retained:
  1) Best-in-slot auto-equip remains `equipSlot + score(maxDamage+defBonus)`.
  2) Early game: safe gold accumulation and no risky enhancement spam.
  3) Mid game: weapon-first enhancement only when reserve and prerequisites are satisfiable.
  4) Late game: broaden to armor/accessory with failure-risk controls.
  5) Minimal safe enhancement path remains prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`) and safely skips when unsatisfied.
- Live validation now: smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=22`, `exp=2707`, `gold=349`, `code=200`; daemon continuity confirmed.
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=30%`, and progression `exp +>=80` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (10:16 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window size `20`): computed deltas were flat (`level +0`, `exp +0`, `gold +0`, `rate_limited 1/20`), so KEEP was rejected.
- Minimal/reversible CHANGE applied: `config/strategy.env` tuned `BUJU_MAX_ACTIONS_PER_CYCLE 1->2` to restore progression throughput while preserving strict safety controls and threshold-based movement.
- Safety/efficiency evidence kept: `GET /api/status` => `200` (`level=22`, `exp=2457`, `gold=354`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters` => (`rabbit`,`skeleton`), so safest high-efficiency target remains `skeleton` under strict move gate (`BUJU_MOVE_LEVEL_2=30`) and repeated-defeat avoidance.
- Hard constraints reaffirmed unchanged as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, sell unequipped gear worse than equipped first.
- Equipment progression requirements revalidated and retained:
  1) Best-in-slot auto-equip remains `equipSlot + score(maxDamage+defBonus)`.
  2) Early game: safe gold accumulation and no risky enhancement spam.
  3) Mid game: weapon-first enhancement only when reserve and prerequisites are satisfiable.
  4) Late game: broaden to armor/accessory with failure-risk controls.
  5) Minimal safe enhancement path remains prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`) and safely skips when unsatisfied.
- Live validation now: smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=combat_start`, `level=22`, `exp=2463`, `gold=349`, `code=200`; daemon continuity confirmed.
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=30%`, and progression `exp +>=80` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (09:46 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-14 21:49:50 -> 2026-03-15 09:18:56`): deltas improved (`level +1` / `21->22`, `gold +20` / `339->359`, `inventory +0` / `3->3`) with low rate-limit signal (`rate_limited 1/20`). `exp` remains sparse in thinking payload, but live progression confirms positive drift (`GET /api/status` `exp=2205` and smoke to `exp=2211`), so CHANGE was not applied.
- Safety/efficiency evidence kept: currently available monsters in active area remain `rabbit` + `skeleton` (`GET /api/areas/talking_island_field/monsters`), so safest high-efficiency target remains `skeleton` under strict movement gate (`BUJU_MOVE_LEVEL_2=30`) and conservative risk-gap controls.
- Hard constraints reaffirmed unchanged as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated and kept active:
  1) Best-in-slot auto-equip remains `equipSlot + score(maxDamage+defBonus)`.
  2) Staged enhancement plan remains explicit (early safe accumulation/no risky spam -> mid weapon-first at reserve+prereqs -> late armor/accessory with failure-risk controls).
  3) Minimal safe enhancement path remains prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`); current probe still shows `npcCount=0`, so safe skip path is correctly taken.
- Live validation now: smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=combat_start`, `level=22`, `exp=2211`, `gold=349`, `code=200`; daemon continuity confirmed (`live-runner-daemon.sh` + `live-strategy-runner.js` active).
- KPI target (next 30m): `death delta=0` from current timestamp window, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=30%`, and progression `exp +>=80` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (08:46 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check: remote endpoint (`GET /api/agent/thinking/j211y?limit=20`) remained empty this cycle, so fallback snapshot `tmp/thinking-last20-now.json` was used (`window 2026-03-14 10:19:49 -> 19:50:06`), yielding `level +1` (`20->21`) and `gold +20` (`309->329`) while `exp` stayed sparse in payload. Current live status progressed further to `level=22`, `exp=1721`, `gold=354`, `area=talking_island_field`; therefore CHANGE was not applied.
- Safety/efficiency evidence kept: currently available monsters remain `rabbit` + `skeleton` in `talking_island_field`, so safest high-efficiency target remains `skeleton` under strict threshold movement gate (`BUJU_MOVE_LEVEL_2=30`) and conservative risk-gap controls.
- Hard constraints reaffirmed as invariants (unchanged): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated and retained:
  1) Best-in-slot auto-equip remains `equipSlot + score(maxDamage+defBonus)`.
  2) Staged enhancement plan remains explicit (early safe accumulation/no risky enhancement spam -> mid weapon-first on reserve+prereqs -> late armor/accessory with failure-risk controls).
  3) Minimal safe enhancement path remains prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`); this cycle probe shows `npcCount=0`, so safe-skip path is correctly taken.
- Live validation now: smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=22`, `exp=1721`, `gold=354`, `code=200`; daemon continuity confirmed (`live-runner-daemon.sh` + `live-strategy-runner.js` active).
- KPI target (next 30m): `death delta=0` in immediate window, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=30%`, and progression `exp +>=80` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (08:16 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20` full window): deltas improved (`level +1` / `21->22`, `gold +25` / `329->354`, inventory `+2` / `7->9`) with low rate-limit signal (`2/20`) and no fresh death growth since latest thinking timestamp, so CHANGE was not applied.
- Safety/efficiency policy kept with live evidence: `GET /api/status` => `200` (`level=22`, `exp=1473`, `gold=344`, `area=talking_island_field`) and `GET /api/areas/talking_island_field/monsters` => (`rabbit`,`skeleton`); safest high-efficiency choice remains `skeleton` under conservative risk-gap control and strict level-threshold movement gate.
- Hard constraints reaffirmed as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated and retained in this document:
  1) Best-in-slot is always evaluated by `equipSlot + score(maxDamage+defBonus)` and equipped first.
  2) Early game: prioritize safe gold accumulation and avoid risky enhancement spam.
  3) Mid game: enhance main weapon first only after reserve threshold and prerequisites are met.
  4) Late game: broaden to armor/accessory enhancement with failure-risk controls.
  5) Minimal safe enhancement action path remains implemented and prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`); current probe shows `npcs=[]` and no enchant scroll, so safe skip path is correctly taken.
- Live evidence now: smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=22`, `exp=1475`, `gold=349`, `code=200`; daemon continuity confirmed.
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=30%`, and progression `exp +>=80` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (07:46 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20` full window): deltas improved (`level +2` / `20->22`, `gold +10` / `339->349`) with bounded rate-limit/throttle signal (`8/20`) and no fresh post-window deaths (`recent death delta=0`), so CHANGE was not applied.
- Safety/efficiency policy kept with live evidence: `GET /api/status` => `200` (`level=22`, `exp=1229`, `gold=354`, `area=talking_island_field`), and current monsters remain safest high-efficiency pool (`rabbit`, `skeleton`), so strict threshold movement and risk-gap control remain unchanged.
- Hard constraints reaffirmed as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated: best-in-slot auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan remains explicit (early safe accumulation/no risky enhancement spam -> mid weapon-first at reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated (safe-skip when NPC/scroll prerequisites are absent).
- Live evidence now: smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=22`, `exp=1223`, `gold=359`, `code=200`; daemon continuity confirmed.
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=30%`, and progression `exp +>=80` or `gold +>=12` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (07:16 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check: remote `GET /api/agent/thinking/j211y?limit=20` returned empty (`count=0`), so fallback local trailing posts (`tmp/thinking-post-*.json`, latest 20 by `tick_number`) were used; deltas were mixed (`level +2` / `20->22`, `gold +30` / `334->364`, `inventory +6` / `2->8`) with unresolved death-pressure sample from `GET /api/logs?action=death&limit=100` (`100` rows), so KEEP was rejected.
- Minimal/reversible CHANGE applied: `config/strategy.env` tuned `BUJU_LOW_HP_POTION_RATIO 0.35->0.45` to shift to earlier potion sustain and reduce low-HP combat exposure while preserving movement/monster safety gates.
- Safety/efficiency policy kept: safest currently available high-efficiency hunt remains `talking_island_field` pool (`rabbit`, `skeleton`) under strict level-threshold movement gate and repeated-defeat avoidance.
- Hard constraints reaffirmed as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated: best-in-slot auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan remains explicit (early safe accumulation/no risky enhancement spam -> mid weapon-first at reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains implemented and prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`).
- Live evidence now: smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=22`, `exp=983`, `gold=389`, `code=200`; daemon continuity confirmed.
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=30%`, and progression `exp +>=100` or `gold +>=15` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (06:46 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check: remote `GET /api/agent/thinking/j211y?limit=20` returned empty this cycle (`count=0`), so fallback local trailing posts (`tmp/thinking-post-*.json`, latest 20 by `tick_number`) were used; computed deltas were mixed (`level +2` / `20->22`, `gold -20` / `379->359`, `inventory +6` / `2->8`) with persistent throttle/rate-limit wording (`12/20`). KEEP was rejected due economy/inventory efficiency drift despite level gain.
- Minimal/reversible CHANGE applied: `config/strategy.env` tuned `BUJU_MIN_MP_POTION_S 3->2` to cut recurring `buy_mp` spend/inventory churn while preserving safety floors and rest-first recovery.
- Safety/efficiency policy kept: safest currently available high-efficiency hunt remains field-safe pool under strict threshold movement gate; repeated-defeat avoidance policy remains active.
- Hard constraints reaffirmed as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated: best-in-slot auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan remains explicit (early safe accumulation/no spam -> mid weapon-first at reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated.
- Live evidence now: smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=combat_start`, `level=22`, `exp=745`, `gold=364`, `code=200`; daemon continuity confirmed (`live-runner-daemon.sh` + `live-strategy-runner.js` active).
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=30%`, and progression `exp +>=100` or `gold +>=15` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (06:16 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check: remote `GET /api/agent/thinking/j211y?limit=20` returned empty this cycle (`count=0`), so fallback local trailing posts (`tmp/thinking-post-*.json`, latest 20) were used; computed deltas improved (`level +2` / `20->22`, `gold +5` / `344->349`, `inventory +0`) with low throttle signal (`rate-limit 1/20`), and live safety probes stayed stable (`GET /api/status` => `level=22`, `gold=359`, `area=talking_island_field`, death-log window `0`). CHANGE was not applied.
- Safety/efficiency policy kept: safest currently available high-efficiency hunt remains field-safe pool under strict threshold movement gate; repeated-defeat avoidance remains active.
- Hard constraints reaffirmed as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated: best-in-slot auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan remains explicit (early safe accumulation/no spam -> mid weapon-first at reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated.
- Live evidence now: smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=combat_start`, `level=22`, `exp=485`, `gold=374`, `code=200`; daemon continuity confirmed (`live-runner-daemon.sh` + `live-strategy-runner.js` active).
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=30%`, and progression `exp +>=100` or `gold +>=15` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (05:46 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-14 17:49:20 -> 2026-03-15 05:20:31`): deltas improved (`level +2` / `20->22`, `gold +35` / `334->369`) with throttle/rate-limit signal `9/20`; immediate safety trend also recovered (`GET /api/logs?action=death&limit=100` shows `recent death delta=0` after latest thinking point), so CHANGE was not applied.
- Safety/efficiency policy kept: safest currently available high-efficiency target remains `talking_island_field` pool (`skeleton`, `rabbit`) over cave options due lower risk gap; strict level-threshold movement gate and repeated-defeat avoidance policy remain unchanged.
- Hard constraints reaffirmed as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated: best-in-slot auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan remains explicit (early safe accumulation/no risky spam -> mid weapon-first at reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated.
- Live evidence now: smoke `node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=combat_start`, `level=22`, `exp=241`, `gold=374`, `code=200`; current status probe shows `level=22`, `exp=267`, `gold=374`, `area=talking_island_field`.
- Live runner continuity evidence: daemon remains active (`live-runner-daemon.sh` + `live-strategy-runner.js` process present) with no secret exposure.
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=30%`, and progression `exp +>=120` or `gold +>=15` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (05:16 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-14 17:18:54 -> 2026-03-15 04:20:01`): nominal improvement existed (`level +1`, `gold +5`), but KEEP was rejected because fresh death pressure re-accelerated right after that window (`GET /api/logs?action=death&limit=100` shows repeated `skeleton_knight` defeats plus mutation-travel deaths at 04:48/04:59/05:09), violating the repeated-defeat safety objective.
- Minimal/reversible CHANGE set committed:
  1) `scripts/live-strategy-runner.js` added a safety-critical mutation-charm reserve override (`BUJU_MUTATION_MIN_GOLD_RESERVE`, default `120`) so mutation-shield prep is not blocked by the high generic reserve floor when travel-death risk is active.
  2) `config/strategy.env` tuned movement threshold `BUJU_MOVE_LEVEL_2 22->30` to enforce safer area1 routing until survivability/equipment improves.
  3) `config/strategy.env` added `BUJU_MUTATION_MIN_GOLD_RESERVE=120`.
- Live evidence now: smoke `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` returned `ok=1/1`, `lastAction=combat_start`, `level=22`, `exp=3`, `gold=369`, `code=200`; follow-up status confirms safe-area alignment `area=talking_island_field`.
- Hard constraints reaffirmed as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated: best-in-slot auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement policy remains explicit (early safe accumulation/no spam -> mid weapon-first with reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`).
- Live runner continuity evidence: daemon remains active (`live-runner-daemon.sh` + `live-strategy-runner.js` present) with no secret exposure.
- KPI target (next 30m): `death delta<=1` (downtrend from current burst), inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=30%`, and progression to `exp>=60` or `gold>=380` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (04:46 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-14 17:18:54 -> 2026-03-15 04:20:01`): `level +1` (`20->21`) and `gold +5` (`324->329`) were positive but insufficient because death pressure stayed extreme in the active cave loop (`GET /api/logs?action=death&limit=100` still dominated by repeated `skeleton_knight` defeats), with residual throttle/rate wording (`10/20`). KEEP was rejected on safety grounds.
- Minimal/reversible logic change committed: `scripts/live-strategy-runner.js` now enforces a **no-armor safety area override** (`!hasArmor => area1`) before normal area progression, so the runner stops lingering in cave while armor slot is empty.
- Live evidence now: smoke run `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` => `ok=1/1`, `lastAction=move`, `level=22`, `exp=1`, `gold=364`, `code=200`; daemon continuity confirmed (`live-runner-daemon.sh` + `live-strategy-runner.js` process present).
- Hard constraints reaffirmed as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated: best-in-slot auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan remains explicit (early safe accumulation/no spam -> mid weapon-first with reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated.
- KPI target (next 30m): force area1 stability while armor is empty, cut fresh death growth (`death delta` downtrend from current burst), keep inventory `<=8`, keep combined combat-start waits `<=35%`, and reach `exp>=60` or `gold>=375` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (04:16 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-14 16:49:02 -> 2026-03-15 03:50:33`): `level +1` (`20->21`) but `gold -85` (`409->324`), `inventory +5` (`2->7`), `exp delta unavailable in thinking payload`, and persistent throttle wording (`rate-limit mention 20/20`) with elevated death-log pressure in-window (`51` entries from `GET /api/logs?action=death&limit=100`), so KEEP was rejected.
- Minimal/reversible parameter change committed: `config/strategy.env` tuned `BUJU_MIN_GOLD_RESERVE 300->340` to protect economy runway and reduce non-critical buy/enhance spend under mixed progression while preserving combat safety gates.
- Live evidence now: `GET /api/status` => `200` (`level=21`, `exp=4209`, `gold=329`, `area=talking_island_field`), `GET /api/areas/talking_island_field/monsters` => safe pool (`rabbit`, `skeleton`), and smoke run `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` => `ok=1/1`, `lastAction=combat_start`, `level=21`, `exp=4221`, `gold=329`, `code=200`.
- Hard constraints reaffirmed as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated: best-in-slot auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan remains explicit (early safe accumulation/no risky spam -> mid weapon-first at reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safe-skips when unsatisfied (`/api/npc/list` currently empty).
- Live runner continuity evidence: daemon remains active (`live-runner-daemon.sh` + `live-strategy-runner.js` process present) with no secret exposure.
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=30%`, and progression to `exp>=4300` or `gold>=340` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (03:46 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-14 16:20:33 -> 2026-03-15 03:19:40`): `level +1` (`20->21`) but `exp +0`, `gold -10` (`324->314`), `inventory +5` (`2->7`), and residual throttle/rate-limit signal `3/20`; KEEP was rejected on economy/throughput drift.
- Minimal/reversible parameter change committed: `config/strategy.env` tuned `BUJU_MIN_MP_POTION_S 4->3` to reduce unnecessary MP-potion restock spend while preserving HP safety floors and combat risk controls.
- Live evidence now: status/smoke confirms stable operation (`level=21`, `exp=3971`, `gold=324`, `area=talking_island_field`, smoke `ok=1/1 code=200`), and currently available monsters remain low-risk/high-efficiency (`rabbit`, `skeleton`) with strict movement threshold gate still enforced (`BUJU_MOVE_LEVEL_2=22`).
- Hard constraints reaffirmed as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated: best-in-slot auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan remains explicit (early safe accumulation/no risky spam -> mid weapon-first at reserve+prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safe-skips when unsatisfied (`/api/npc/list` currently empty).
- Live runner continuity evidence: daemon remains active (`live-runner-daemon.sh` + `live-strategy-runner.js` process present) with no secret exposure.
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=30%`, and progression to `exp>=4050` or `gold>=340` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (03:16 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-14 15:49:56 -> 2026-03-15 01:49:26`): `level +1` (`20->21`) but `gold -65` (`379->314`) with flat inventory (`2->2`) and low throttle signal (`1/20`), so KEEP was rejected due economy-efficiency drift.
- Minimal/reversible parameter change committed: `config/strategy.env` tuned `BUJU_MIN_BUY_QTY 2->1` to stop one-unit overbuy leakage on potion/charm replenishment while keeping safety floors unchanged (`BUJU_MIN_HP_POTION_S=6`, `BUJU_MIN_MP_POTION_S=4`).
- Live evidence now: status probe shows `level=21`, `exp=3719`, `gold=314`, `area=talking_island_field`, inventory `7/30`; safest currently available high-efficiency monsters remain `rabbit` and `skeleton` in area1, with strict movement threshold gate preserved (`BUJU_MOVE_LEVEL_2=22`).
- Hard constraints reaffirmed as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated: best-in-slot auto-equip remains `equipSlot + score(maxDamage+defBonus)`; staged enhancement plan remains explicit (early safe accumulation/no risky spam -> mid weapon-first after reserve+prereqs -> late armor/accessory broadening with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safe-skips when unsatisfied (`/api/npc/list` currently empty).
- Live runner continuity evidence: daemon remains active (`live-runner-daemon.sh` process present) and no secret material was emitted.
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=30%`, and progression to `exp>=3800` or `gold>=330` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (02:46 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-14 15:49:56 -> 2026-03-15 01:49:26`): `level +1` (`20->21`) but `gold -65` (`379->314`) with flat inventory (`2->2`) and low throttle signal (`1/20`), so KEEP was rejected on economy-efficiency drift despite level gain.
- Minimal/reversible parameter change committed: `config/strategy.env` tuned `BUJU_MIN_BUY_QTY 3->2` to cut overbuy gold bleed on potion/charm restocks while keeping safety floors intact (`BUJU_MIN_HP_POTION_S=6`, `BUJU_MIN_MP_POTION_S=4`).
- Safety/efficiency evidence now: live status probe shows `level=21`, `exp=3475`, `gold=344`, `area=talking_island_field`; available monsters remain low-risk/high-efficiency (`rabbit`, `skeleton`) and strict threshold movement gate remains active (`BUJU_MOVE_LEVEL_2=22`).
- Hard constraints reaffirmed unchanged as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements reaffirmed: best-in-slot auto-equip by `equipSlot + score(maxDamage+defBonus)` remains active; staged enhancement plan remains explicit (early safe accumulation/no spam -> mid weapon-first at reserve/prereqs -> late armor/accessory broadening with failure-risk controls); minimal safe enhancement path remains prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`) and skips safely when unsatisfied (`/api/npc/list` currently empty).
- Live runner continuity evidence: daemon and runner processes are both active (`live-runner-daemon.sh`, `live-strategy-runner.js`).
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=30%`, and economy recovery to `gold>=355` or progression to `exp>=3560` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (02:16 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-14 15:49:56 -> 2026-03-15 01:49:26`): `level +1` (`20->21`) but `exp +0`, `gold -65` (`379->314`), and heavy throttle/rate-limit signal (`18/20`), so KEEP was rejected for safety-efficiency balance.
- Minimal/reversible logic change committed: `config/strategy.env` tuned `BUJU_MAX_ACTIONS_PER_CYCLE 2->1` to reduce control-plane churn and repeated cooldown/rate-limit collisions while preserving strict risk-gap and threshold-move policy.
- Hard constraints reaffirmed unchanged as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Safety/efficiency evidence now: monster pool probe `GET /api/areas/talking_island_field/monsters` remains low-risk (`rabbit`, `skeleton`), so safest high-efficiency hunt remains area1 until threshold gate (`BUJU_MOVE_LEVEL_2=22`) is met. Status/NPC endpoints were auth-restricted this cycle (`UNAUTHORIZED`), so live validation used smoke output.
- Equipment progression requirements reaffirmed: best-in-slot auto-equip by `equipSlot + score(maxDamage+defBonus)` remains active; staged enhancement plan remains explicit (early safe accumulation/no risky spam -> mid weapon-first at reserve/prereqs -> late armor/accessory broadening with failure-risk controls); minimal safe enhancement path stays prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`) and safely skips when unsatisfied.
- Validation/continuity evidence: smoke run `node scripts/live-strategy-runner.js` succeeded (`ok=1/1`, `lastAction=combat_start`, `level=21`, `exp=3245`, `gold=339`, `code=200`), and live daemon continuity remains active (`live-runner-daemon.sh` + runner process present).
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=30%`, and progression to `exp>=3325` or `gold>=350` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (01:46 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 delta check: remote `GET /api/agent/thinking/j211y?limit=20` returned empty this cycle, so fallback local trailing 20 thinking posts (`tmp/thinking-post-*.json`) were used and produced `level +1` (`20->21`), `gold -15` (`319->304`), `inventory +3` (`0->3`), with no explicit defeat escalation in the sampled reasoning window and only moderate throttle signal (`8/20`), which still satisfies improvement evidence via level-up.
- Live safety/efficiency evidence validated now: `GET /api/status` => `200` (`level=21`, `exp=2975`, `gold=314`, `area=talking_island_field`), `GET /api/areas/talking_island_field/monsters` => `200` (`rabbit`, `skeleton`), and safest high-efficiency selector remains aligned with strict movement threshold gate (`BUJU_MOVE_LEVEL_2=22`, so no risky area expansion before threshold).
- Hard constraints revalidated as runtime invariants (unchanged): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated and kept explicit: best-in-slot auto-equip by `equipSlot + score(maxDamage+defBonus)` remains active; staged enhancement plan remains (early safe gold/no risky spam -> mid weapon-first when reserve/prereqs met -> late armor/accessory broadening with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safely skipped when unsatisfied this cycle (`GET /api/npc/list` => `200`, `npcs=[]`).
- Validation/continuity evidence: smoke run `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` succeeded (`ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=21`, `exp=2973`, `gold=309`, `code=200`), and daemon continuity remains active (`live-runner-daemon.sh` + runner process both present).
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=35%`, and progression to `exp>=3050` or `gold>=325` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (01:16 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-14 14:49:42 -> 2026-03-15 00:49:26`): `level +1` (`20->21`), `gold -15` (`344->329`), `inventory +8` (`0->8`), `death delta=0`, `rate-limit signal 1/20`; KEEP accepted because level-up + zero-death evidence satisfies improvement while risk controls remain strict.
- Safety/efficiency evidence kept: live probes show current area `talking_island_field` with available low-risk pool (`rabbit`, `skeleton`), so safest high-efficiency selector remains valid under strict movement threshold gate (`BUJU_MOVE_LEVEL_2=22`, current level 21).
- Hard constraints revalidated as invariants (unchanged): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements reconfirmed: best-in-slot auto-equip by `equipSlot + score(maxDamage+defBonus)` remains active; staged enhancement policy remains explicit (early safe accumulation/no spam -> mid weapon-first at reserve/prereqs -> late armor/accessory with failure-risk controls); minimal safe enhancement path remains prerequisite-gated and safely skips when unsatisfied (`GET /api/npc/list => 200`, `npcs=[]`).
- Validation/continuity evidence: smoke run `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` succeeded (`ok=1/1`, `lastAction=combat_start`, `level=21`, `exp=2727`, `gold=344`, `code=200`), and daemon continuity remains active (`live-runner-daemon.sh` + runner process present).
- KPI target (next 30m): `death delta=0`, inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=35%`, and progression to `exp>=2810` or `gold>=320` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (00:46 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check: remote `GET /api/agent/thinking/j211y?limit=20` returned no rows this cycle, so fallback local last-20 posts (`tmp/thinking-post-*.json`) were used; deltas were `level +1` (`20->21`), `gold +15` (`309->324`), `inventory -8` (`8->0`), `death delta=0`, `rate-limit signal 5/20`, which satisfies improvement evidence.
- Safety/efficiency evidence kept: live status `GET /api/status` => `200` (`level=21`, `exp=2461`, `gold=329`, `area=talking_island_field`), and available monsters (`rabbit`, `skeleton`) remain the safest high-efficiency pool under strict threshold movement gate (`BUJU_MOVE_LEVEL_2=22`, current level 21).
- Hard constraints revalidated as invariants (unchanged): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements reconfirmed: best-in-slot auto-equip by `equipSlot + score(maxDamage+defBonus)` remains active; staged enhancement plan remains explicit (early safe gold/no risky spam -> mid weapon-first on reserve/prereqs -> late armor/accessory with failure-risk controls); minimal enhancement path is safely prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`) and skipped this cycle (`GET /api/npc/list => 200`, `npcs=[]`).
- Validation/continuity evidence: smoke run `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` succeeded (`ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=21`, `exp=2461`, `gold=329`, `code=200`), and daemon continuity remains active (`live-runner-daemon.sh` + runner process present).
- KPI target (next 30m): `death delta=0`, inventory `<=8`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=35%`, dangerous-surrender `<=1/8 cycles`, and progression to `exp>=2540` or `gold>=340` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (00:16 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-14 13:50:01 -> 2026-03-14 23:49:41`): `level +1` (`20->21`), `gold +25` (`309->334`), `inventory +3` (`0->3`), `rate_limited 1/20`; improvement evidence is present, so CHANGE was not applied.
- Safety/efficiency evidence: live status confirms safe-area routing (`talking_island_field`) with available low-risk pool (`rabbit`, `skeleton`), and strict level-threshold movement gate remains enforced (`BUJU_MOVE_LEVEL_2=22`, current level 21) to avoid premature risk expansion.
- Hard constraints revalidated as invariants (unchanged): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements revalidated: best-in-slot auto-equip by `equipSlot + score(maxDamage+defBonus)` remains active; staged enhancement plan is retained (early safe gold/no risky spam -> mid weapon-first after reserve/prereqs -> late armor/accessory broadening with failure-risk controls); minimal enhancement path stays prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`) and safely skips when unsatisfied.
- Validation/continuity evidence: smoke run `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` succeeded (`ok=1/1`, `lastAction=surrender_dangerous_combat`, `level=21`, `exp=2213`, `gold=319`, `code=200`), and daemon continuity remains active (`live-runner-daemon.sh` + runner process present).
- KPI target (next 30m): `death delta=0` (no fresh defeats), inventory `<=8`, dangerous-surrender `<=1/8 cycles`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=35%`, and progression to `exp>=2290` or `gold>=330` with smoke `code=200`.

## 2026-03-14
- 30-min STRATEGY DIRECTOR (23:46 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-14 13:19:49 -> 2026-03-14 23:19:35`): `level +1` (`20->21`) but `exp +0` (`sparse/flat`) and `gold -10` (`334->324`) with throttle/rate-limit signal `20/20`; improvement evidence was insufficient, so KEEP was rejected.
- Minimal/reversible safety change applied: `config/strategy.env` tuned `BUJU_LOW_HP_POTION_RATIO 0.15->0.35` so potion-first recovery triggers earlier and reduces low-HP combat exposure without changing movement thresholds or hunt target policy.
- Safety/efficiency constraints revalidated as invariants (unchanged): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Safest-high-efficiency hunt policy kept: current status area remains `talking_island_field` and monster selection stays safety-filtered first; movement remains strict threshold-gated (`BUJU_MOVE_LEVEL_2=22`, `BUJU_MOVE_LEVEL_3=30`) to avoid premature risk expansion.
- Equipment progression requirements reconfirmed and staged plan retained in this document: early safe gold accumulation/no risky enhancement spam -> mid weapon-first enhancement once reserve+prereqs are met -> late armor/accessory broadening with failure-risk controls.
- Minimal safe enhancement path remains implemented and prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`) and safely skips when prerequisites are unsatisfied.
- KPI target (next 30m): `death delta=0`, inventory `<=8`, reduce dangerous-combat churn (`surrender_dangerous_combat<=1/8 cycles`), keep smoke `code=200`, and recover progression (`exp +>=60` or `gold +>=10`).

- 30-min STRATEGY DIRECTOR (23:16 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-14 12:50:18 -> 2026-03-14 22:49:55`): `level +0` (`21->21`), `exp +0` (`1707->1707`), `gold +0` (`324->324`), `inventory +0`, `death +0`, and throttle/rate-limit mentions `5/20`; no measurable progression evidence, so KEEP was rejected.
- Minimal/reversible parameter change applied: `config/strategy.env` tuned `BUJU_MAX_ACTIONS_PER_CYCLE 1->2` to recover progression signal while preserving safety gates (safest-monster selection, level-threshold movement, surrender/risk controls).
- Hard constraints revalidated as invariants (unchanged): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements reconfirmed and staged plan retained: early safe gold accumulation/no risky enhancement spam -> mid weapon-first enhancement after reserve/prerequisites -> late armor/accessory broadening with failure-risk controls.
- Minimal safe enhancement path remains implemented and prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`); this cycle remains safe-skip when prerequisites are missing.
- KPI target (next 30m): achieve at least one measurable progression signal (`exp +>=80` or `gold +>=10`) with `death delta=0`, inventory `<=8`, and smoke `code=200`.

- 30-min STRATEGY DIRECTOR (22:46 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-14 12:19:46 -> 2026-03-14 22:19:31`): `level +1` (`20->21`), `gold +5` (`309->314`), `inventory +3` (`0->3`), with no fresh defeat-burst escalation in current status area; improvement evidence exists so CHANGE was not applied.
- Safety/efficiency policy kept with live evidence: `GET /api/status` (`200`) shows `level=21`, `exp=1461`, `gold=319`, `area=talking_island_field`; `GET /api/areas/talking_island_field/monsters` (`200`) confirms safe available pool (`rabbit`, `skeleton`), so safest high-efficiency hunt remains area1 under strict threshold-move gate (`BUJU_MOVE_LEVEL_2=22`, current level 21).
- Hard constraints revalidated as invariants (unchanged): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements reconfirmed and staged plan retained in this document: early safe gold accumulation/no risky spam -> mid weapon-first enhancement after reserve/prerequisites -> late armor/accessory broadening with failure-risk controls.
- Minimal safe enhancement path remains implemented and prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`); current probe still indicates safe skip path is valid when prerequisites are missing.
- Validation/continuity evidence: smoke run `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` succeeded (`ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=21`, `exp=1467`, `gold=334`, `code=200`), and daemon continuity remains active (`live-runner-daemon.sh` + runner process present).
- KPI target (next 30m): `deaths<=2`, inventory `<=8`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=35%`, dangerous-surrender `<=1/8 cycles`, and progression to `exp>=1540` or `gold>=345` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (22:16 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-14 11:49:24 -> 2026-03-14 21:49:50`): `level +1` (`20->21`), `gold +35` (`304->339`), `exp +0` (thinking-window sparse), throttle/rate-limit wording `18/20`; improvement evidence exists so CHANGE was not applied.
- Safety/efficiency policy kept with live evidence: `GET /api/status` (`200`) shows `level=21`, `exp=1227`, `gold=314`, `area=talking_island_field`; `GET /api/areas/talking_island_field/monsters` (`200`) confirms current safe pool (`rabbit`, `skeleton`), so safest high-efficiency hunt remains area1 with strict level-threshold gate (`BUJU_MOVE_LEVEL_2=22`, current level 21).
- Hard constraints revalidated as invariants (unchanged): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements reconfirmed and staged plan retained in this doc:
  1) Best-in-slot auto-equip remains active by `equipSlot + score(maxDamage+defBonus)`.
  2) Early game: safe gold accumulation and no risky enhancement spam.
  3) Mid game: weapon-first enhancement only after reserve/prerequisites are met.
  4) Late game: broaden to armor/accessory with failure-risk controls.
  5) Minimal safe enhancement path remains prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`); current probe `GET /api/npc/list` => `200` with `npcs=[]`, so safe skip is expected.
- Validation/continuity evidence: smoke run `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` succeeded (`ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=21`, `exp=1221`, `gold=339`, `code=200`), and daemon continuity remains active (`ps -ax` shows `live-runner-daemon.sh`).
- KPI target (next 30m): `deaths<=2`, inventory `<=8`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=35%`, dangerous-surrender `<=1/8 cycles`, and progression to `exp>=1300` or `gold>=345` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (21:46 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-14 11:19:46 -> 2026-03-14 21:18:50`): `level +1` (`20->21`), `gold +0` (`319->319`), `inventory +6` (`0->6`), `death +0` (in-window), `rate-limit signal 4/20`; improvement evidence (`level +1`, no death increase) is present, so CHANGE was not applied.
- Safety/efficiency policy kept with live evidence: current status probe (`GET /api/status`) shows `level=21`, `exp=981`, `gold=339`, `area=talking_island_field`; available monsters remain low-risk/high-efficiency (`rabbit`, `skeleton`), and movement remains strictly threshold-gated (`BUJU_MOVE_LEVEL_2=22`, current level 21).
- Hard constraints revalidated as invariants (unchanged): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements reconfirmed and staged plan retained:
  1) Best-in-slot auto-equip remains active by `equipSlot + score(maxDamage+defBonus)`.
  2) Early game: prioritize safe gold accumulation and avoid risky enhancement spam.
  3) Mid game: weapon-first enhancement only after reserve threshold/prerequisites are met.
  4) Late game: broaden to armor/accessory with failure-risk controls.
  5) Minimal safe enhancement path remains implemented but prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`); this cycle probe shows `/api/npc/list` empty (`npcs=[]`), so safe skip is expected.
- Validation/continuity evidence: smoke run `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` succeeded (`ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=21`, `exp=981`, `gold=339`, `code=200`), and daemon continuity remains active (`live-runner-daemon.sh` + live runner process present).
- KPI target (next 30m): `deaths<=2`, inventory `<=8`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=35%`, dangerous-surrender `<=1/8 cycles`, and progression to `exp>=1060` or `gold>=355` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (21:16 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-14 10:50:05 -> 2026-03-14 20:49:42`): `level +1` (`20->21`), `gold +15` (`309->324`), `inventory +2` (`0->2`); improvement evidence exists, so CHANGE was not applied.
- Safety/efficiency policy kept with live evidence: status probe (`GET /api/status`) shows `level=21`, `exp=727`, `gold=314`, `area=talking_island_field`; available monsters remain low-risk/high-efficiency (`rabbit`, `skeleton`) and strict movement threshold gate stays enforced (`BUJU_MOVE_LEVEL_2=22`, current level 21).
- Hard constraints revalidated as invariants (unchanged): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements reconfirmed and staged plan retained:
  1) Best-in-slot auto-equip remains active by `equipSlot + score(maxDamage+defBonus)`.
  2) Early game: prioritize safe gold accumulation and avoid risky enhancement spam.
  3) Mid game: weapon-first enhancement only after reserve threshold/prerequisites are met.
  4) Late game: broaden to armor/accessory with failure-risk controls.
  5) Minimal safe enhancement path remains prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`); this cycle probe shows `/api/npc/list` empty (`npcs=[]`), so safe skip is expected.
- Validation/continuity evidence: smoke run `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` succeeded (`ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=21`, `exp=729`, `gold=319`, `code=200`), and daemon continuity remains active (`live-runner-daemon.sh` + live runner process present).
- KPI target (next 30m): `deaths<=2`, inventory `<=8`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=35%`, dangerous-surrender `<=1/8 cycles`, and progression to `exp>=820` or `gold>=335` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (20:46 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-14 10:19:49 -> 2026-03-14 19:50:06`): `level +1` (`20->21`), `gold +20` (`309->329`), `inventory +4` (`3->7`), throttle wording `17/20`; improvement evidence exists, so CHANGE was not applied.
- Safety/efficiency policy kept with live evidence: current safe area monster pool remains `rabbit(lv1)` + `skeleton(lv2)` (`GET /api/areas/talking_island_field/monsters`), so safest high-efficiency selection stays in area1 while strict level-threshold movement gate is enforced (`BUJU_MOVE_LEVEL_2=22`, current level 21).
- Hard constraints revalidated as invariants (unchanged): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements reconfirmed and staged plan retained:
  1) Best-in-slot auto-equip remains active by `equipSlot + score(maxDamage+defBonus)`.
  2) Early game: prioritize safe gold accumulation and avoid risky enhancement spam.
  3) Mid game: weapon-first enhancement only after reserve threshold is met.
  4) Late game: broaden to armor/accessory with failure-risk controls.
  5) Minimal safe enhancement path remains implemented but prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`); this cycle probe shows `/api/npc/list` empty, so safe skip is expected.
- Validation/continuity evidence: smoke run `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` succeeded (`ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=21`, `exp=493`, `gold=339`, `code=200`), and daemon continuity remains active (`live-runner-daemon.sh` + live runner process present).
- KPI target (next 30m): `deaths<=3`, inventory `<=8`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=35%`, dangerous-surrender `<=1/8 cycles`, and progression to `exp>=560` or `gold>=350` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (20:16 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-14 10:19:49 -> 2026-03-14 19:50:06`): `level +1` (`20->21`), `gold +20` (`319->339`), `inventory +4` (`3->7`), throttle wording `19/20`; improvement evidence exists, so CHANGE was not applied.
- Safety/efficiency policy kept: safest high-efficiency monster selection + strict level-threshold movement gate + defeat/surrender pressure retreat logic remain active to avoid repeated-defeat loops; no risk-expansion tuning this cycle.
- Hard constraints revalidated as invariants (unchanged): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements reconfirmed and staged plan retained:
  1) Best-in-slot auto-equip remains active by `equipSlot + score(maxDamage+defBonus)`.
  2) Early game: prioritize safe gold accumulation; avoid risky enhancement spam.
  3) Mid game: weapon-first enhancement when reserve threshold/prerequisites are met.
  4) Late game: broaden to armor/accessory with failure-risk controls.
  5) Minimal safe enhancement path remains prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`) and skips safely when unsatisfied.
- Telemetry caveat this cycle: authenticated probes for `/api/status`, `/api/logs?action=death`, `/api/inventory`, and `/api/npc/list` returned `401`; decision evidence therefore used the successful thinking-window delta and live smoke (`ok=1/1`, `lastAction=combat_start`, `level=21`, `exp=241`, `gold=319`, `code=200`).
- KPI target (next 30m): `deaths<=3`, inventory `<=8`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=35%`, dangerous-surrender `<=1/8 cycles`, and progression to `exp>=320` or `gold>=335` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (19:46 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-14 09:48:50 -> 2026-03-14 19:19:06`): `level +0` (`20->20`), `gold +30` (`309->339`), `inventory +1` (`3->4`), throttle wording `18/20`.
- Despite gold improvement, KEEP was rejected due hard safety breach evidence: death feed (`GET /api/logs?action=death&limit=100`) shows repeated defeat burst in current cave loop (`50` deaths in latest ~30m, dominant target `skeleton_knight`), violating the "avoid repeated defeats" objective.
- Minimal/reversible parameter change applied: `config/strategy.env` tuned `BUJU_MOVE_LEVEL_2 21->22` to delay Lv2-area movement by one level and reduce risk-gap exposure while preserving strict threshold movement policy.
- Safety/efficiency evidence: current cave pool includes high-risk `skeleton_knight(lv10, atk22)` under no-armor profile; safest high-efficiency action is to stay/return area1 until survivability improves.
- Hard constraints revalidated as invariants (unchanged): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements reconfirmed and kept staged:
  1) Best-in-slot remains evaluated by `equipSlot + score(maxDamage+defBonus)` and auto-equip is active.
  2) Early game: gold accumulation + no risky enhancement spam.
  3) Mid game: weapon-first enhancement after reserve threshold.
  4) Late game: broaden to armor/accessory with failure-risk controls.
  5) Minimal safe enhancement path remains prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`); this cycle prerequisites are unsatisfied (`/npc/list` empty, no enhancement scroll), so safe skip is expected.
- KPI target (next 30m): `deaths<=5` (downtrend), inventory `<=8`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=35%`, dangerous-surrender `<=1/8 cycles`, and progression to `exp>=80` or `gold>=345` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (19:16 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-14 09:19:06 -> 2026-03-14 18:49:16`): `level +0` (`20->20`), `gold +10` (`309->319`), `inventory +0` (`8->8`), with throttle wording `20/20`; improvement evidence exists (`gold +10`) so CHANGE was not applied.
- Safety/efficiency evidence: currently available monsters in active area `talking_island_field` remain `rabbit(lv1)` + `skeleton(lv2)`, so safest high-efficiency routing stays valid while strict level-threshold movement gate remains enforced (`BUJU_MOVE_LEVEL_2=21`, current level 20).
- Hard constraints revalidated as invariants (unchanged): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements reconfirmed and kept staged:
  1) Best-in-slot remains evaluated by `equipSlot + score(maxDamage+defBonus)` and auto-equip is active.
  2) Early game: gold accumulation + no risky enhancement spam.
  3) Mid game: weapon-first enhancement after reserve threshold.
  4) Late game: expand to armor/accessory with cooldown/failure-risk controls.
  5) Minimal safe enhancement path remains prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`); this cycle prerequisites are unsatisfied (`/npc/list` empty, no enhancement scroll), so safe skip is expected.
- KPI target (next 30m): `deaths=0`, inventory `<=8`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=35%`, dangerous-surrender `<=1/8 cycles`, and progression to `exp>=3990` or `gold>=355` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (18:16 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-14 08:19:25 -> 2026-03-14 17:49:20`): `level +0` (`20->20`), `gold +25` (`309->334`), `inventory -1` (`3->2`), and no explicit in-window death increase evidence (death probe endpoint currently non-200), so improvement evidence is sufficient and CHANGE was not applied.
- Safety/efficiency evidence: currently available monsters in active area `talking_island_field` remain `rabbit(lv1)` + `skeleton(lv2)`; selector keeps safety-first high-efficiency hunt while strict movement threshold gate stays enforced (`BUJU_MOVE_LEVEL_2=21`, current level 20).
- Hard constraints revalidated as invariants (unchanged): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements reconfirmed and documented as staged plan:
  1) Always evaluate best-in-slot by `equipSlot + score(maxDamage+defBonus)` and auto-equip.
  2) Early game: prioritize gold accumulation and avoid risky enhancement spam.
  3) Mid game: enhance main weapon first once reserve threshold is met.
  4) Late game: broaden to armor/accessory with cooldown/failure-risk controls.
  5) Minimal safe enhancement action path remains implemented and prerequisite-gated (`scroll + blacksmith npc + resource reserve + non-combat + rate budget`); this cycle prerequisites are unsatisfied (`/npc/list` empty, no enhance scroll), so safe skip is expected.
- KPI target (next 30m): `deaths=0`, inventory `<=8`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=40%`, dangerous-surrender bursts `<=1/8 cycles`, and progression to `exp>=3520` or `gold>=350` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (17:46 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-13 21:20:10 -> 2026-03-14 07:49:43`): `level +2` (`18->20`) but `gold -20` (`329->309`), `exp +0`, and persistent throttle signal (`rate-limit 13/20`), so KEEP was rejected.
- Minimal/reversible parameter change applied: `config/strategy.env` tuned `BUJU_STALL_429_COOLDOWN_TICKS 12->14` to reduce repeated `combat_start` re-entry collisions under throttle while preserving safe hunt/equip/enhance logic.
- Safety/efficiency evidence: currently available monsters in active safe area remain low-risk (`rabbit`, `skeleton` in `talking_island_field`), and selector still prioritizes safest high-efficiency target with strict level-threshold movement gate (`BUJU_MOVE_LEVEL_2=21`).
- Hard constraints revalidated as invariants (unchanged): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements reconfirmed:
  1) Best-in-slot evaluation remains active each cycle by `equipSlot + score(maxDamage+defBonus)` with auto-equip.
  2) Staged enhancement strategy remains explicit (early safe gold/no-spam -> mid weapon-first at reserve threshold -> late armor/accessory with failure-risk controls).
  3) Minimal safe enhancement API path remains prerequisite-gated (`scroll + blacksmith npc + resource + non-combat + rate budget`) and only executes when satisfiable.
- KPI target (next 30m): `deaths=0`, inventory `<=8`, combined `wait_combat_start_rate_limit+wait_combat_start_cooldown<=40%`, dangerous-surrender bursts `<=1/8 cycles`, and progression to `exp>=3080` or `gold>=360` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (17:16 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-14 07:19:31 -> 2026-03-14 16:49:02`): `level +0` (`20->20`), `gold +100` (`309->409`), `inventory delta -1` (`3->2`), and `rate-limit/throttle signal 8/20`; improvement evidence exists so CHANGE/retune is skipped this cycle.
- Safety/efficiency evidence: current area remains `talking_island_field` with available monsters `rabbit(lv1)` + `skeleton(lv2)`; safest high-efficiency routing remains valid while strict movement threshold gate is preserved (`BUJU_MOVE_LEVEL_2=21`, current level 20) to avoid premature risk-area expansion.
- Hard constraints revalidated as invariants (unchanged): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements reconfirmed:
  1) Best-in-slot evaluation remains active each cycle by `equipSlot + score(maxDamage+defBonus)` with auto-equip.
  2) Staged enhancement strategy remains explicit (early safe gold/no-spam -> mid weapon-first at reserve threshold -> late armor/accessory with failure-risk controls).
  3) Minimal safe enhancement action path remains prerequisite-gated (`scroll + blacksmith npc + resource + non-combat + rate budget`); when prerequisites are unsatisfied, enhancement is skipped safely.
- Validation/continuity evidence: smoke run `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` succeeded (`ok=1/1`, `lastAction=combat_start`, `level=20`, `exp=2957`, `gold=319`, `code=200`) and live runner daemon continuity remains active (`live-runner-daemon.sh` + `live-strategy-runner.js` process present).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=45%`, dangerous-surrender bursts `=0`, and progression to `exp>=3020` or `gold>=360` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (16:46 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, logs window `2026-03-14 06:49:30 -> 2026-03-14 16:20:33`): `level +1` (`19->20`), `gold +5` (`319->324`), `inventory delta -1` (`3->2`), and `rate-limit/throttle signal 9/20`; improvement evidence exists so CHANGE/retune is skipped this cycle.
- Safety/efficiency evidence: current area `talking_island_field` monster pool remains `rabbit(lv1)` + `skeleton(lv2)` (`GET /api/areas/talking_island_field/monsters`), so safest high-efficiency routing remains valid while strict movement threshold gate is preserved (`BUJU_MOVE_LEVEL_2=21`, current level 20) to avoid premature risk-area expansion.
- Hard constraints revalidated as invariants (unchanged): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements reconfirmed:
  1) Best-in-slot evaluation remains active each cycle by `equipSlot + score(maxDamage+defBonus)` with auto-equip.
  2) Staged enhancement strategy remains explicit (early safe gold/no-spam -> mid weapon-first at reserve threshold -> late armor/accessory with failure-risk controls).
  3) Minimal safe enhancement action path remains prerequisite-gated (`scroll + blacksmith npc + resource + non-combat + rate budget`); when prereqs are unsatisfied, enhancement is skipped safely.
- Validation/continuity evidence: smoke run `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` succeeded (`ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=20`, `exp=2717`, `gold=319`, `code=200`) and live runner daemon continuity remains active (`live-runner-daemon.sh` process present).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=45%`, dangerous-surrender bursts `=0`, and progression to `exp>=2780` or `gold>=350` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (16:16 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`): `level +1` (`19->20`), `gold +5` (`309->314`), `inventory delta 0` (`3->3`), and `rate-limit/throttle signal 12/20`; improvement evidence exists so CHANGE/retune is skipped this cycle.
- Safety/efficiency evidence: current area `talking_island_field` monster pool is `rabbit(lv1)` and `skeleton(lv2)`; safest-high-efficiency selector remains active with strict level-threshold movement gate (`BUJU_MOVE_LEVEL_2=21`, current level 20) to avoid premature risk-area expansion.
- Hard constraints revalidated as invariants (unchanged): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements reconfirmed:
  1) Best-in-slot evaluation stays active per `equipSlot + score(maxDamage+defBonus)` with auto-equip.
  2) Staged enhancement strategy remains explicit (early safe gold/no-spam -> mid weapon-first at reserve threshold -> late armor/accessory with failure-risk controls).
  3) Minimal safe enhancement action path remains implemented and prerequisite-gated (`scroll + blacksmith npc + resource + non-combat + rate budget`); current probe shows prerequisites unsatisfied (`npc list empty`, enhancement scroll unavailable), so safe skip is expected.
- Validation/continuity evidence: smoke run `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` succeeded (`ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=20`, `exp=2479`, `gold=324`, `code=200`) and live runner daemon continuity remains active (`live-runner-daemon.sh` + `live-strategy-runner.js`).
- KPI target (next 30m): `deaths=0`, inventory `<=8`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=45%`, no repeated dangerous-surrender bursts, and progression to `exp>=2540` or `gold>=350` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (15:46 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check: remote `GET /api/agent/thinking/j211y?limit=20` returned empty (`count=0`), so local fallback (`tmp/cron-last20-local.json`, 20 posts) was used; deltas were `level +1` (`19->20`), `gold +10` (`334->344`), `exp n/a` (sparse field), `rate-limit signals 19/20`, and `death +0` from `GET /api/logs?action=death&limit=200`.
- Safety/efficiency evidence: currently available monsters in active area `talking_island_field` are `rabbit` and `skeleton`; safest high-efficiency selector remains active with strict movement threshold gate (`BUJU_MOVE_LEVEL_2=21`, current level 20) preventing premature risk expansion.
- Hard constraints preserved as invariants (unchanged): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements reaffirmed:
  1) Best-in-slot remains evaluated by `equipSlot + score(maxDamage+defBonus)` and auto-equip path is active.
  2) Staged enhancement strategy remains explicit in this doc (early safe gold/no-spam -> mid weapon-first when reserve threshold met -> late armor/accessory with failure-risk controls).
  3) Minimal safe enhancement API path remains prerequisite-gated (`scroll + blacksmith npc + resource + non-combat + rate budget`); current probe shows prerequisites unsatisfied (`npc list empty`, enhancement scroll unavailable), so safe skip is expected.
- Validation/continuity evidence: smoke run `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` succeeded (`ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=20`, `exp=2221`, `gold=319`, `code=200`) and live runner daemon continuity remains active.
- KPI target (next 30m): `deaths=0`, inventory `<=8`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=45%`, no repeated dangerous-surrender bursts, and progression to `exp>=2280` or `gold>=350` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (15:16 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-14 04:49:24 -> 2026-03-14 14:49:42`): `level +1` (`19->20`), `exp +0`, `gold +35` (`309->344`), `death +0` (from `GET /api/logs?action=death&limit=200` in-window), but throttle/rate-limit signal remained high (`18/20`), so KEEP was rejected.
- Logic change applied (minimal/reversible): in `scripts/live-strategy-runner.js` tightened safest-monster gate by lowering attack guard (`armor: 1.35x def`, `no-armor: 1.15x def`) and adding pressure-aware hard danger cap before efficiency ranking; this lowers repeat-defeat risk while still picking the safest high-efficiency target.
- Hard constraints preserved as invariants (unchanged): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells unequipped gear worse than equipped first.
- Equipment progression requirements reaffirmed in staged form:
  1) Best-in-slot evaluation per `equipSlot + score(maxDamage+defBonus)` remains auto-equip active.
  2) Enhancement strategy remains staged in this doc: early safe gold/no-spam -> mid weapon-first on reserve threshold -> late armor/accessory broadening with failure-risk controls.
  3) Minimal safe enhancement path remains prerequisite-gated (`scroll + blacksmith npc + resource reserve + rate budget + non-combat`); current probe shows prerequisites unsatisfied (`scrolls=0`, blacksmith not found), so safe skip is expected.
- Validation/continuity evidence: smoke run `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` succeeded (`ok=1/1`, `lastAction=surrender_dangerous_combat`, `level=20`, `exp=1981`, `gold=339`, `code=200`) and daemon continuity remains active.
- KPI target (next 30m): `deaths=0`, inventory `<=8`, `wait_combat_start_rate_limit+wait_combat_start_cooldown<=20%`, no repeated dangerous-surrender bursts, and progression to `exp>=2020` or `gold>=330` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (14:46 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log check: remote `GET /api/agent/thinking/j211y?limit=20` returned empty (`count=0`), so local fallback `tmp/cron-last20-local.json` (20 rows, usable posts=11) was used; deltas were `level +1` (`19->20`), `gold -10` (`339->329`), `exp +0` (sparse field), `rate-limit 1/20`, `death +0` => KEEP rejected.
- Minimal/reversible parameter change applied: `config/strategy.env` tuned `BUJU_STALL_429_COOLDOWN_TICKS 10->12` to reduce repeated `combat_start` re-entry collisions after throttle while preserving current throughput.
- Safety/efficiency evidence: current available monsters in active area (`talking_island_field`) are `rabbit` and `skeleton`; safety-first high-efficiency selector remains active and level-threshold move gate remains strict (`BUJU_MOVE_LEVEL_2=21`, current level 20).
- Hard constraints preserved as invariants (unchanged): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots `>=10`, liquidation still sells unequipped gear worse than equipped first.
- Equipment progression requirements reaffirmed in staged form:
  1) Best-in-slot evaluation per `equipSlot + score(maxDamage+defBonus)` remains auto-equip active.
  2) Enhancement strategy remains staged in this doc: early safe gold/no-spam -> mid weapon-first at reserve threshold -> late armor/accessory broadening with failure-risk controls.
  3) Minimal safe enhancement path remains prerequisite-gated (`scroll + blacksmith npc + resource reserve + rate budget + non-combat`); current probe shows prerequisites unsatisfied (`scrolls=0`, blacksmith not found), so safe skip is expected.
- Validation/continuity evidence: smoke run `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` succeeded (`ok=1/1`, `code=200`) and live runner daemon continuity remains active.
- KPI target (next 30m): `deaths=0`, inventory `<=8`, `wait_combat_start_rate_limit + wait_combat_start_cooldown <=20%`, strict threshold-move compliance `100%`, and progression to `exp>=1780` or `gold>=340` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (14:16 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`GET /api/agent/thinking/j211y?limit=20`, window `2026-03-14 03:49:43 -> 2026-03-14 13:50:01`): `level +1` (`19->20`) but `gold -30` (`339->309`), `exp +0`, and high throttle signal (`18/20`); `death +0` from `GET /api/logs?action=death&limit=200` in-window. KEEP rejected.
- Minimal/reversible parameter change applied: `config/strategy.env` tuned `BUJU_COMBAT_STRATEGY_REFRESH_TICKS 16->24` to reduce control-call churn and lower combat-start collision pressure without altering safety-gated hunt/equip/enhance logic.
- Hard constraints preserved as invariants (unchanged): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still sells unequipped gear worse than equipped first.
- Equipment progression requirements reaffirmed:
  1) Best-in-slot remains evaluated each cycle by `equipSlot + score(maxDamage+defBonus)` and auto-equipped when superior.
  2) Staged enhancement strategy remains explicit: early safe gold/no risky spam -> mid game weapon-first after reserve threshold -> late game armor/accessory broadening with failure-risk controls.
  3) Minimal safe enhancement action path remains prerequisite-gated (`scroll + blacksmith npc + resource reserve + rate budget + non-combat`).
- Validation/continuity evidence: smoke run `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` succeeded (`ok=1/1`, `lastAction=combat_start`, `level=20`, `exp=1489`, `gold=329`, `code=200`), and daemon continuity remains active.
- KPI target (next 30m): `deaths=0`, inventory `<=8`, `wait_combat_start_rate_limit<=20%`, strict level-threshold move compliance `100%`, and progression to `exp>=1520` or `gold>=345` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (13:46 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`2026-03-14 03:18:33 -> 2026-03-14 13:19:49` from `GET /api/agent/thinking/j211y?limit=20`): `level +1` (`19->20`) but `gold -10` (`344->334`) with residual throttle signal (`8/20`), so KEEP was rejected.
- Minimal/reversible parameter change applied: `config/strategy.env` tuned `BUJU_STALL_429_COOLDOWN_TICKS 8->10` to reduce repeated `combat_start` re-entry collisions after 429 and lower wait-churn risk without changing hunt safety policy.
- Safety/efficiency evidence preserved: current active-area monster pool is still low-risk (`rabbit`, `skeleton` in `talking_island_field`), and selector remains safest high-efficiency with strict level-threshold movement gate (`BUJU_MOVE_LEVEL_2=21`).
- Hard constraints preserved unchanged and treated as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still sells unequipped gear worse than equipped first.
- Equipment progression requirements reaffirmed:
  1) Best-in-slot evaluation remains per `equipSlot + score(maxDamage+defBonus)` with auto-equip when superior.
  2) Staged enhancement plan remains explicit in this document (early safe gold/no spam -> mid weapon-first at reserve threshold -> late armor/accessory with failure-risk controls).
  3) Minimal safe enhancement action path stays prerequisite-gated (`scroll + blacksmith npc + resource reserve + rate budget + non-combat`).
- Validation/continuity evidence: smoke run `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` succeeded (`ok=1/1`, `lastAction=combat_start`, `level=20`, `exp=1253`, `gold=339`, `code=200`) and daemon continuity remains active.
- KPI target (next 30m): `deaths=0`, inventory `<=8`, `wait_combat_start_rate_limit<=22%`, strict threshold-move compliance `100%`, and progression to `exp>=1300` or `gold>=345` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (13:16 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`2026-03-13 21:20:10 -> 2026-03-14 07:49:43`): `level +2` (`18->20`) but `gold -20` (`329->309`) with high throttle signal (`13/20`), so KEEP was rejected.
- Minimal/reversible changes applied:
  1) `config/strategy.env` tuned `BUJU_BASE_DELAY_MS 4000->4200` to lower `combat_start` collision/rate-limit pressure.
  2) `scripts/live-strategy-runner.js` fixed move fallback bug (`safeAreaOverride` -> `thresholdAreaOverride`) and clarified fallback action tag to `move_threshold_fallback` so strict level-threshold movement remains deterministic on fallback branch.
- Hard constraints preserved unchanged and treated as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still sells unequipped gear worse than equipped first.
- Equipment progression requirements reaffirmed:
  1) Best-in-slot evaluation remains per `equipSlot + score(maxDamage+defBonus)` with auto-equip when superior.
  2) Staged enhancement plan remains explicit in this document (early safe gold/no spam -> mid weapon-first at reserve threshold -> late armor/accessory with failure-risk controls).
  3) Minimal safe enhancement action path stays prerequisite-gated (`scroll + blacksmith npc + resource reserve + rate budget + non-combat`).
- Validation/continuity evidence: smoke run `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` succeeded (`ok=1/1`, `lastAction=combat_start`, `level=20`, `exp=1003`, `gold=324`, `code=200`) and daemon continuity remains active.
- KPI target (next 30m): `deaths=0`, inventory `<=8`, `wait_combat_start_rate_limit<=25%`, threshold fallback compliance `100%`, and progression to `exp>=1060` or `gold>=335` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (12:46 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (fallback local last-20 thinking posts because `GET /agent/thinking/j211y?limit=20` returned empty list): `level +2` (`18->20`), `gold +5` (`304->309`), `exp +0` (window edge snapshots), and no `rate_limited` decisions (`0/20`).
- Safety/efficiency evidence kept: current available monsters in active area are `rabbit` and `skeleton`; selector remains safety-first/high-efficiency and strict level-threshold movement gate remains active (`level 20 < BUJU_MOVE_LEVEL_2=21` => stay/return to `BUJU_AREA_LV1`).
- Hard constraints locked unchanged in runtime path: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still sells unequipped gear worse than equipped first.
- Equipment progression requirements explicitly retained:
  1) Best-in-slot equip remains evaluated each cycle by `equipSlot + score(maxDamage+defBonus)` and auto-equipped when superior.
  2) Enhancement staged plan remains (early safe accumulation/no spam -> mid weapon-first at reserve threshold -> late armor/accessory with failure-risk controls).
  3) Minimal safe enhancement action path remains prerequisite-gated (`scroll + blacksmith npc + resource reserve + rate budget + non-combat`).
- Live smoke/continuity evidence: `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` => `ok=1/1`, `lastAction=combat_start`, `level=20`, `exp=757`, `gold=329`, `code=200`; daemon kept continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` active).
- KPI target (next 30 min): keep `deaths` trend non-escalating from latest burst window, inventory `<=8`, no cave move below threshold level, and progression to `gold>=340` or `exp>=820` with smoke `code=200`.

- 30-min STRATEGY DIRECTOR (12:16 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`2026-03-14 00:19:41 -> 2026-03-14 10:50:05` from trailing thinking logs): `level +1` (`19->20`) but `exp +0` (thinking window), `gold -25` (`334->309`), and recent death-feed concentration persisted in cave window (supported `logs?action=death` samples), so KEEP was rejected.
- Logic change applied (minimal/reversible): in `scripts/live-strategy-runner.js`, tightened movement policy to strict level-threshold gating — while `level < BUJU_MOVE_LEVEL_2`, runner now forces return/stay to `BUJU_AREA_LV1` regardless of armor state (`thresholdAreaOverride`), preventing cave retention risk drift.
- Hard constraints locked unchanged in runtime path: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still sells unequipped gear worse than equipped first.
- Equipment progression requirements explicitly retained:
  1) Best-in-slot equip remains evaluated each cycle by `equipSlot + score(maxDamage+defBonus)` and auto-equipped when superior.
  2) Enhancement staged plan remains: early game gold accumulation/no risky spam, mid game weapon-first once reserve threshold is met, late game armor/accessory expansion with failure-risk controls.
  3) Minimal safe enhancement action path remains prerequisite-gated (`scroll + blacksmith npc + resource reserve + rate budget + non-combat`).
- Live smoke/continuity evidence: `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` => `ok=1/1`, `lastAction=combat_start`, `level=20`, `exp=499`, `gold=324`, `code=200`; live runner daemon kept continuous.
- KPI target (next 30 min): `deaths` downtrend with no repeat cave-loop burst, inventory `<=8`, strict no-cave-below-threshold compliance, and positive progression (`gold>=330` or `exp>=550`) while keeping smoke `code=200`.

- 30-min STRATEGY DIRECTOR (11:46 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`2026-03-13 21:49:20 -> 2026-03-14 08:19:25` from local trailing logs): `level +2` (`18->20`) but `exp +0`, `gold -25` (`334->309`), `death +0` (from local death-log window check), so KEEP was rejected.
- Parameter change applied (minimal/reversible): `config/strategy.env` `BUJU_COMBAT_STRATEGY_REFRESH_TICKS: 12 -> 16` to further reduce control-call churn under throttle while preserving safest-monster routing and level-threshold movement gates.
- Hard constraints locked unchanged in runtime path: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still sells unequipped gear worse than equipped first.
- Equipment progression requirements explicitly retained:
  1) Best-in-slot equip remains evaluated each cycle by `equipSlot + score(maxDamage+defBonus)` and auto-equipped when superior.
  2) Enhancement staged plan remains: early game gold accumulation without risky spam, mid game weapon-first at reserve threshold, late game armor/accessory expansion with failure-risk controls.
  3) Minimal safe enhancement path remains active only when prerequisites are satisfiable (`scroll + blacksmith npc + resource reserve + rate budget + non-combat`).
- Live smoke/continuity evidence: `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` => `ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=20`, `exp=247`, `gold=304`, `code=200`; daemon remains continuous (`live-runner-daemon.sh` + `live-strategy-runner.js` running).
- KPI target (next 30 min): `deaths=0`, inventory `<=8`, `wait_combat_start_rate_limit<=30%`, and positive economy/progression (`gold>=315` or `exp>=300`) while keeping smoke `code=200`.

- 30-min STRATEGY DIRECTOR (11:16 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`2026-03-13 21:20:10 -> 2026-03-14 07:49:43` from local trailing logs): `level +2` (`18->20`) but `exp +0`, `gold -20` (`329->309`), `death +0`, and rate-limit pressure remained high (`19/20`), so KEEP was rejected.
- Parameter change applied (minimal/reversible): `config/strategy.env` `BUJU_MOVE_LEVEL_2: 20 -> 21` to delay cave transition by one level and lower risk-gap exposure while preserving strict level-threshold movement policy.
- Hard constraints locked unchanged in runtime path: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still sells unequipped gear worse than equipped first.
- Equipment progression requirements explicitly retained:
  1) Best-in-slot equip remains evaluated each cycle by `equipSlot + score(maxDamage+defBonus)` and auto-equipped when superior.
  2) Enhancement staged plan remains: early game gold accumulation without risky spam, mid game weapon-first at reserve threshold, late game armor/accessory expansion with failure-risk controls.
  3) Minimal safe enhancement path remains active only when prerequisites are satisfiable (`scroll + blacksmith npc + resource reserve + rate budget + non-combat`).
- KPI target (next 30 min): `deaths=0`, inventory `<=8`, `wait_combat_start_rate_limit<=35%`, and positive economy drift (`gold>=315` or `exp>=50`) while keeping smoke `code=200` and daemon continuous.

- 30-min STRATEGY DIRECTOR (10:46 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`2026-03-13 23:48:41 -> 2026-03-14 10:19:49`): `level +1` (`19->20`) but `exp +0`, `gold -15` (`324->309`), and persistent throttle/rate-limit signature (`18/20`) with live daemon showing repeated `combat_start -> surrender_dangerous_combat -> rest -> wait_combat_start_rate_limit` loops, so KEEP was rejected.
- Logic changes applied (minimal, reversible):
  1) `scripts/live-strategy-runner.js` now tracks recent dangerous surrenders and feeds that into safety pressure (`pressure=max(defeatPressure,surrenderPressure)`) for monster risk-gap tightening.
  2) Added surrender-pressure retreat gate: when `recentDangerSurrenderCount(8) >= 3` (or defeats pressure is high), desired area is forced to `BUJU_AREA_LV1` (`action=move_safety_retreat`) before normal level-threshold area progression resumes.
- Hard constraints locked unchanged: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements reaffirmed: best-in-slot remains evaluated by `equipSlot + score(maxDamage+defBonus)` and auto-equipped when superior; staged enhancement strategy remains explicit: (a) early safe gold accumulation/no risky spam, (b) mid game weapon-first after reserve threshold, (c) late game armor/accessory expansion with failure-risk controls.
- Minimal safe enhancement action path remains prerequisite-gated and active (`scroll + blacksmith npc + resource reserve + rate budget + non-combat`) and safely skips when unsatisfied.
- KPI target (next 30 min): cut dangerous-surrender loop share to `<=25%` of cycles, enforce safety retreat when surrender pressure spikes, keep inventory `<=8`, keep smoke `code=200`, and recover economy (`gold>=320` or `exp>=50`).

- 30-min STRATEGY DIRECTOR (10:16 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`2026-03-13 23:19:48 -> 2026-03-14 09:48:50`): `level +2` (`18->20`) but `gold -25` (`334->309`) with persistent throttle/rate-limit signature (`20/20`) and active defeat burst risk (latest death feed shows repeated fresh defeats), so KEEP was rejected.
- Logic changes applied (minimal, reversible):
  1) `scripts/live-strategy-runner.js` now hard-picks only the globally least-danger monster when the safety filter is empty (`slice(0,1)`), preventing efficiency re-ranking from reintroducing risky targets.
  2) Added defeat-pressure retreat gate: when recent local defeat pressure reaches threshold (`recentDefeatCount(8) >= 3`), desired area is forced to `BUJU_AREA_LV1` (`action=move_safety_retreat`) before normal level-threshold area progression resumes.
- Hard constraints locked unchanged: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression requirements reaffirmed: best-in-slot remains evaluated by `equipSlot + score(maxDamage+defBonus)` and auto-equipped when superior; staged enhancement strategy remains explicit: (a) early safe gold accumulation/no risky spam, (b) mid game weapon-first after reserve threshold, (c) late game armor/accessory expansion with failure-risk controls.
- Minimal safe enhancement action path remains prerequisite-gated and active (`scroll + blacksmith npc + resource reserve + rate budget + non-combat`) and safely skips when unsatisfied.
- KPI target (next 30 min): `deaths<=2` (downtrend from current burst), inventory `<=8`, avoid `move` to higher-risk areas unless level threshold policy allows, maintain smoke `code=200`, and recover economy (`gold>=320` or `exp>=50`).

- 30-min STRATEGY DIRECTOR (09:46 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`2026-03-13 22:18:53 -> 2026-03-14 08:19:25`): `level +2` (`18->20`) but `gold -20` (`329->309`), `exp` effectively flat at the latest band, and no positive economy lift with repeated throttle-wait signature, so KEEP was rejected.
- Logic change applied (minimal, reversible): in `scripts/live-strategy-runner.js`, when `hunt` action budget is unavailable, the runner now returns `wait_hunt_rate_limit` before attempting `/combat/strategy`, cutting avoidable control-call churn while keeping safest-monster routing and level-threshold movement unchanged.
- Hard constraints locked unchanged: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression staged plan reaffirmed: (a) early game safe gold accumulation/no risky enhancement spam, (b) mid game weapon-first enhancement after reserve/prereqs, (c) late game armor/accessory expansion with cooldown/failure-risk controls.
- Minimal safe enhancement action path remains prerequisite-gated (`scroll + npc + resource + non-combat + rate budget`) and safely skips when prerequisites are not satisfiable.
- KPI target (next 30 min): defeats `=0`, inventory `<=8`, reduce `wait_combat_start_rate_limit` share to `<=25%`, maintain smoke `code=200`, and recover economy (`gold>=320` or `exp>=50`).

- 30-min STRATEGY DIRECTOR (09:16 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`2026-03-13 21:20:10 -> 2026-03-14 07:49:43`): `level +2` (`18->20`) but `gold -20` (`329->309`), `exp +0` (window edge snapshots), `death +0` (death logs in-window), and recurring throttle/cooldown pressure in latest loop signals, so KEEP was rejected.
- Logic change applied (minimal, reversible): in `scripts/live-strategy-runner.js`, `combat_start` `429` now attempts one safe direct `/hunt` fallback in the same tick (`action=hunt_on_combat_start_rate_limit`) before waiting, reusing the already-selected safest high-efficiency monster + skill.
- Hard constraints locked unchanged: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes selling unequipped gear worse than equipped first.
- Equipment progression staged plan (explicitly retained in policy): (a) early game = safe gold accumulation and no risky enhancement spam, (b) mid game = main-weapon-first enhancement only after reserve/prereqs (`scroll+npc+resource+rate-budget+non-combat`), (c) late game = expand to armor/accessory under cooldown/failure-risk controls.
- Minimal safe enhancement action path remains prerequisite-gated and unchanged (`/npc/list` blacksmith discovery -> `/npc/{npc_id}/enhance`) so unsafe/unsatisfied states skip enhancement safely.
- KPI target (next 30 min): defeats `=0`, inventory `<=8`, raise non-wait combat throughput with `hunt_on_combat_start_rate_limit>=1` when 429 appears, reduce pure `wait_combat_start_rate_limit` share to `<=25%`, keep smoke `code=200`, and recover economy (`gold>=320` or `exp>=50`).

- 30-min STRATEGY DIRECTOR (08:46 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`2026-03-13 21:49:20 -> 2026-03-14 08:19:25`): `level +2` (`18->20`), `exp +0` (`1->1`), `gold -25` (`334->309`), `death +0`, with persistent throttle signal (`20/20`), so KEEP was rejected.
- Logic change applied (minimal, reversible): in `scripts/live-strategy-runner.js`, combat strategy refresh interval is now configurable and set to `BUJU_COMBAT_STRATEGY_REFRESH_TICKS=12` (was hardcoded `8`), reducing `POST /combat/strategy` churn while preserving safest-monster routing, level-threshold movement gating, and defeat-risk controls.
- Hard constraints locked unchanged: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes unequipped gear worse than equipped first.
- Equipment progression contract reaffirmed: best-in-slot evaluation by `equipSlot + score(maxDamage+defBonus)` remains active; staged enhancement plan retained (early safe accumulation, mid weapon-first, late armor/accessory with failure-risk controls) with prerequisite-gated minimal enhancement path (`scroll + npc + resource + rate budget + non-combat`).
- KPI target (next 30 min): defeats `=0`, inventory `<=8`, reduce `wait_combat_start_rate_limit` share to `<=25%`, maintain smoke `code=200`, and recover economy (`gold>=320` or `exp>=50`).

- 30-min STRATEGY DIRECTOR (08:16 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`2026-03-13 21:20:10 -> 2026-03-14 07:49:43`): `level +2` (`18->20`) but `gold -20` (`329->309`) with persistent throttle pressure (`rate-limit signal 13/20`), so KEEP was rejected.
- Logic change applied (minimal, reversible): in `scripts/live-strategy-runner.js`, `POST /combat/strategy` is now sent only when strategy payload changes or refresh interval (`8 ticks`) elapses, instead of every combat-start attempt; expected effect is lower action-budget collision while preserving hunt safety behavior.
- Safety/efficiency policy kept: safest high-efficiency monster routing and level-threshold movement gates remain unchanged to avoid defeat loops and premature risk expansion.
- Hard constraints locked unchanged: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes unequipped gear worse than equipped first.
- Equipment progression contract reaffirmed: best-in-slot evaluation by `equipSlot + score(maxDamage+defBonus)` remains active; staged enhancement plan retained (early safe accumulation, mid weapon-first, late armor/accessory with failure-risk controls) with prerequisite-gated minimal enhancement path (`scroll + npc + resource + rate budget + non-combat`).
- KPI target (next 30 min): defeats `=0`, inventory `<=8`, reduce `wait_combat_start_rate_limit` share to `<=30%`, and keep smoke `code=200` while recovering economy (`gold>=320`).

- 30-min STRATEGY DIRECTOR (07:46 KST, adaptive mode + equipment progression) CHANGE decision from mandatory last-20 thinking-log delta check (`2026-03-13 20:49:19 -> 2026-03-14 07:19:31`): `level +2` (`18->20`) but `gold -10` (`319->309`) and rate-limit pressure persisted in window (`rate-limit signal 20/20`), so KEEP was rejected.
- Logic change applied (minimal, reversible): in `scripts/live-strategy-runner.js`, when `combat_start` is in cooldown/skip state the runner now attempts a direct `/hunt` fallback once (`action=hunt_on_combat_cooldown`) instead of pure wait, to preserve progression throughput under throttle while keeping risk controls unchanged.
- Safety/efficiency constraints reaffirmed unchanged: safest high-efficiency monster selection remains active with conservative dynamic risk-gap, and movement remains strictly level-threshold gated (`BUJU_MOVE_LEVEL_2=20`, `BUJU_MOVE_LEVEL_3=30`).
- Hard constraints locked: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes unequipped gear worse than equipped first.
- Equipment progression (staged plan explicitly retained): (a) early game = gold accumulation + no risky enhancement spam, (b) mid game = weapon-first enhancement only after reserve/prereqs, (c) late game = armor/accessory expansion with cooldown/failure-risk controls.
- Minimal safe enhancement action path remains prerequisite-gated (`scroll + blacksmith npc + reserve + rate budget + non-combat`) and only executes when satisfiable.
- KPI target (next 30 min): defeats `=0`, inventory `<=8`, reduce `wait_combat_start_rate_limit` share to `<=30%` with `hunt_on_combat_cooldown>=1` when cooldown appears, and maintain smoke `code=200`.

- 30-min STRATEGY DIRECTOR (06:46 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log delta check (`2026-03-13 19:49:11 -> 2026-03-14 05:49:15`): `level +1` (`18->19`), `gold +0` (`324->324`), `rate_limited 0/20`, and no newer death timestamp than `2026-03-13 17:48:43` from `GET /api/logs?action=death&limit=50`.
- Safety/efficiency remained aligned with constraints in this cycle: safest high-efficiency monster selection is still active under dynamic risk-gap control, and movement remains level-threshold gated (`BUJU_MOVE_LEVEL_2=20`, current level 19) to avoid premature risk expansion.
- Hard constraints reaffirmed as invariants (non-env-overridable in execution path): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation prioritizes unequipped gear worse than equipped before general low-tier cleanup.
- Equipment progression contract remains active: best-in-slot scan by `equipSlot` + `score(maxDamage+defBonus)` with auto-equip, staged enhancement strategy (early safe gold/no-spam -> mid weapon-first at reserve threshold -> late armor/accessory with failure-risk controls), and minimal safe enhancement path gated by `scroll + blacksmith npc + resource reserve + rate budget + non-combat`.
- Live smoke validation this run: `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` => `ok=1/1`, `lastAction=surrender_dangerous_combat`, `level=19`, `exp=3551`, `gold=319`, `code=200`; live runner daemon continuity confirmed active.
- KPI target (next 30 min): defeats `=0`, inventory `<=8`, dangerous-surrender events `<=1` per 6 cycles, and progression to `exp>=3650` or `gold>=340` while maintaining smoke `code=200`.

- 30-min STRATEGY DIRECTOR (06:16 KST, adaptive mode + equipment progression) KEEP decision from mandatory last-20 thinking-log check (`2026-03-13 19:49:11 -> 2026-03-14 05:49:15`): `level +1` (`18->19`), `gold +0` (`324->324`), `rate_limited 0/20`, with no new death signal in current probe window (`/api/logs?action=death&limit=50` returned empty/new-none).
- Safety/efficiency remains conservative and aligned with runtime behavior: safest high-efficiency routing remains active on currently available low-risk pool, and movement remains level-threshold gated (`BUJU_MOVE_LEVEL_2=20`, current level 19) to avoid risk-gap expansion.
- Hard constraints re-locked as invariants (unchanged): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when inventory slots `>=10`, liquidation prioritizes unequipped gear worse than equipped first.
- Equipment progression requirements remain enforced in runtime: best-in-slot scan by `equipSlot` and `score(maxDamage+defBonus)` every cycle with auto-equip when better gear exists.
- Enhancement strategy (staged plan in effect): (a) early game -> safe gold accumulation / no risky enhancement spam, (b) mid game -> weapon-first enhancement only after reserve/prereqs, (c) late game -> armor/accessory broadening with cooldown + failure-risk controls.
- Minimal safe enhancement action path remains implemented and prerequisite-gated (`scroll + blacksmith npc + resource reserve + rate budget + non-combat`), so enhancement is skipped safely when prerequisites are not satisfiable.
- KPI target (next 30 min): defeats `=0`, inventory `<=8`, `wait_combat_start_rate_limit<=35%`, and progression to `exp>=3350` or `gold>=350` at level 19.
- 30-min STRATEGY DIRECTOR (05:46 KST, adaptive mode + equipment progression) KEEP decision with mandatory evidence from latest 20 thinking logs (`2026-03-13 19:21:18 -> 2026-03-14 05:20:07`): `level +1` (`18->19`), `gold +0` (`309->309`), `rate_limited 0/20`; no newer death timestamp than `2026-03-13 17:48:43`.
- Live validation healthy this cycle (`BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` => `ok=1/1`, `lastAction=combat_start`, `level=19`, `exp=3055`, `gold=339`, `code=200`) and live runner daemon continuity remains active.
- Safety/efficiency policy kept with evidence: currently available monsters are low-risk (`rabbit`, `skeleton`) and safest high-efficiency routing stays active; movement remains level-threshold gated (`BUJU_MOVE_LEVEL_2=20`, current level 19) to avoid risk-gap expansion.
- Hard constraints reaffirmed unchanged and code-invariant: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, sell unequipped gear worse than equipped first.
- Equipment progression requirements remain active: best-in-slot evaluation by `equipSlot` + `score(maxDamage+defBonus)` each cycle; staged enhancement plan is explicit (early gold/no-spam -> mid weapon-first at reserve threshold -> late armor/accessory with failure-risk controls); minimal safe enhancement action path remains prerequisite-gated (`scroll+npc+resource+rate-budget+non-combat`).
- KPI target (next 30 min): defeats `=0`, inventory `<=8`, `wait_combat_start_rate_limit<=35%`, smoke `code=200`, and progression to `exp>=3200` or `gold>=350` at level 19.
- 30-min STRATEGY DIRECTOR (05:16 KST, adaptive mode + equipment progression) KEEP decision with hard evidence from latest 20 thinking logs (`2026-03-13 18:49:29 -> 2026-03-14 04:49:24`): `level +1` (`18->19`), `gold +5` (`304->309`), `rate_limited 0/20`; no newer death timestamp than `2026-03-13 17:48:43`.
- Live validation remained healthy this cycle (`BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` => `ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `level=19`, `exp=2813`, `gold=314`, `code=200`) and live runner daemon continuity stayed active.
- Safety/efficiency policy kept with evidence: current available monsters are low-risk (`rabbit`, `skeleton`) and safest high-efficiency routing remains active; movement remains level-threshold gated (`BUJU_MOVE_LEVEL_2=20`, current level 19) to avoid risky relocation before threshold.
- Hard constraints reaffirmed unchanged and code-invariant: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, sell unequipped gear worse than equipped first.
- Equipment progression remains active and explicit: best-in-slot auto-equip by `equipSlot` + `score(maxDamage+defBonus)` each cycle; staged enhancement strategy stays in force (early safe gold/no spam -> mid weapon-first on reserve threshold -> late armor/accessory with failure-risk controls), with minimal safe enhancement path gated by `scroll + npc + resource + rate budget + non-combat`.
- KPI target (next 30 min): `wait_combat_start_rate_limit<=35%`, defeats `=0`, inventory `<=8`, smoke `code=200`, and progression to `exp>=2950` or `gold>=330` at level 19.
- Adaptive step-93 (2026-03-14 04:46 KST): recomputed last-20 thinking deltas (`2026-03-13 18:20:39 -> 2026-03-14 04:20:26`) yielded mixed/insufficient improvement (`level +1`, `gold -25`, `rate_limited 1/20`, no newer death than `2026-03-13 17:48:43`), and one-cycle validation still landed on `wait_combat_start_rate_limit` despite `code=200`.
- CHANGE (reversible): widened pacing by changing `BUJU_BASE_DELAY_MS` from `3800` to `4000` (keeping `BUJU_MAX_ACTIONS_PER_CYCLE=1`) to lower combat-start collision pressure while preserving continuous daemon operation.
- Hard constraints reaffirmed unchanged and code-invariant: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, sell unequipped gear worse than equipped first.
- Equipment progression policy kept explicit and active: best-in-slot auto-equip by `equipSlot` + `score(maxDamage+defBonus)` each cycle; staged enhancement strategy remains (early safe gold/no spam -> mid weapon-first on reserve+scroll+npc gates -> late armor/accessory with cooldown/failure-risk controls), with minimal safe enhancement path prerequisite-gated.
- KPI target (next 30 min): `wait_combat_start_rate_limit<=35%`, defeats `=0`, inventory `<=8`, smoke `code=200`, and progression to `exp>=2650` or `gold>=340` at level 19.
- 30-min STRATEGY DIRECTOR (04:16 KST, adaptive mode + equipment progression) KEEP decision with mandatory evidence from latest 20 thinking logs: `level +1` (`18->19`), `gold +35` (`304->339`), `rate_limited 17/20`; latest smoke/status evidence remained healthy (`BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` => `ok=1/1`, `code=200`, `exp≈2323`) with no newer death timestamp than `2026-03-13 17:48:43`.
- Hard constraints remained unchanged and enforced in code-path priority: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation continues to prioritize unequipped gear worse than equipped first.
- Equipment progression remains active and unchanged: best-in-slot auto-equip by `equipSlot` + `score(maxDamage+defBonus)`, staged enhancement policy (early no-spam gold accumulation -> mid weapon-first at reserve threshold -> late armor/accessory with failure-risk controls), and minimal safe enhancement action path only when `scroll + blacksmith npc + resource + rate-budget + non-combat` prerequisites are all satisfied.
- 30-min STRATEGY DIRECTOR (03:46 KST, adaptive mode + equipment progression) KEEP decision with mandatory evidence from latest 20 thinking logs: `level +17` (`1->18`), `gold +221` (`113->334`), `rate_limited 1/20`; latest live status remained stable (`level=19`, `exp=2071`, `gold=339`, `inventory=3/30`) with no newer death timestamp than `2026-03-13 17:48:43`.
- Live validation remained healthy in this cycle (`BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` => `ok=1/1`, `lastAction=rest`, `level=19`, `exp=2061`, `gold=314`, `code=200`) and runner continuity stayed active (`live-runner-daemon.sh` + `live-strategy-runner.js` processes present).
- Safety/efficiency policy remains KEEP with evidence: safest high-efficiency monster routing + level-threshold movement gate + conservative risk-gap control continued without fresh defeat signals.
- Hard constraints remain invariant and unchanged: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes unequipped gear worse than equipped first.
- Equipment progression requirements remain active and explicit: best-in-slot auto-equip by `equipSlot` + `score(maxDamage+defBonus)` every cycle, staged enhancement plan (early safe gold/no spam -> mid weapon-first with reserve/prereqs -> late armor/accessory with failure-risk controls), and minimal safe enhancement path gated by `scroll + blacksmith npc + resource + rate budget + non-combat`.
- 30-min STRATEGY DIRECTOR (03:16 KST, adaptive mode + equipment progression) KEEP decision with mandatory evidence from latest 20 thinking logs: `level +1` (`18->19`), `gold +30` (`304->334`), `rate_limited 1/20`.
- Live validation remained healthy in this cycle (`BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` => `ok=1/1`, `lastAction=combat_start`, `level=19`, `exp=1813`, `gold=344`, `code=200`) and runner continuity stayed active (`live-runner-daemon.sh` + `live-strategy-runner.js` processes present).
- Safety/efficiency policy remains KEEP with evidence: safest high-efficiency monster routing + level-threshold movement gate + conservative risk-gap control continued without new recent death timestamps.
- Hard constraints remain invariant and unchanged: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes unequipped gear worse than equipped first.
- Equipment progression requirements remain active and explicit: best-in-slot auto-equip by `equipSlot` + `score(maxDamage+defBonus)` every cycle, staged enhancement plan (early safe gold/no spam -> mid weapon-first with reserve/prereqs -> late armor/accessory with failure-risk controls), and minimal safe enhancement path gated by `scroll + blacksmith npc + resource + rate budget + non-combat`.
- 30-min STRATEGY DIRECTOR (02:46 KST, adaptive mode + equipment progression) KEEP decision with mandatory evidence from latest 20 thinking logs: `level +1` (`18->19`), `gold +10` (`304->314`), `rate_limited 1/20`.
- Live validation remained healthy in this cycle (`BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` => `ok=1/1`, `lastAction=combat_start`, `level=19`, `exp=1563`, `gold=329`, `code=200`) and runner continuity stayed active (`live-runner-daemon.sh` + `live-strategy-runner.js` processes present).
- Safety/efficiency policy remains KEEP with evidence: safest high-efficiency monster routing + level-threshold movement gate + conservative risk-gap control continued without new near-term defeat signals.
- Hard constraints remain invariant and unchanged: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes unequipped gear worse than equipped first.
- Equipment progression requirements remain active and explicit: best-in-slot auto-equip by `equipSlot` + `score(maxDamage+defBonus)` every cycle, staged enhancement plan (early safe gold/no spam -> mid weapon-first with reserve/prereqs -> late armor/accessory with failure-risk controls), and minimal safe enhancement path gated by `scroll + blacksmith npc + resource + rate budget + non-combat`.
- 30-min STRATEGY DIRECTOR (02:16 KST, adaptive mode + equipment progression) KEEP decision with mandatory evidence from latest 20 thinking logs: `level +1` (`18->19`), `gold +10` (`304->314`), `rate_limited 1/20`.
- Live validation stayed healthy in this cycle (`BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` => `ok=1/1`, `lastAction=combat_start`, `level=19`, `exp=1303`, `gold=329`, `code=200`) and runner continuity remained active (`live-runner-daemon.sh` + `live-strategy-runner.js` processes present).
- Safety/efficiency policy remains KEEP with evidence: safest high-efficiency monster routing, level-threshold movement gate, conservative risk gap, and inventory control (`8/30`) remain stable; no new repeated defeat pattern surfaced in the latest run window.
- Hard constraints remain invariant and unchanged: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes unequipped gear worse than equipped first.
- Equipment progression requirements remain active and explicit: best-in-slot auto-equip by `equipSlot` + `score(maxDamage+defBonus)` every cycle, staged enhancement plan (early safe gold/no spam -> mid weapon-first with reserve/prereqs -> late armor/accessory with failure-risk controls), and minimal safe enhancement path gated by `scroll + blacksmith npc + resource + rate budget + non-combat`.
- 30-min STRATEGY DIRECTOR (01:46 KST, adaptive mode + equipment progression) KEEP decision with mandatory evidence from latest 20 thinking logs: `level +8` (`11->19`), `gold +10` (`324->334`), `rate_limited 1/20`.
- Live validation stayed healthy in this cycle (`BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` => `ok=1/1`, `lastAction=combat_start`, `code=200`) and current status is stable at `level=19`, `exp=1053`, `gold=314`, inventory `8/30`.
- Safety/efficiency policy is kept with evidence: safest high-efficiency monster routing remains active, level-threshold movement gate is unchanged, and no new recent death timestamp appeared in current death-log probe window.
- Hard constraints remain invariant and unchanged: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes unequipped gear worse than equipped first.
- Equipment progression requirements remain active and explicit: best-in-slot auto-equip by `equipSlot` + `score(maxDamage+defBonus)` every cycle, staged enhancement plan (early safe gold/no spam -> mid weapon-first with reserve/prereqs -> late armor/accessory with failure-risk controls), and minimal safe enhancement path gated by `scroll + blacksmith npc + resource + rate budget + non-combat`.
- 30-min STRATEGY DIRECTOR (01:16 KST, adaptive mode + equipment progression) KEEP decision with mandatory evidence from latest 20 thinking logs: `level +8` (`11->19`), `gold +30` (`304->334`), `rate_limited 1/20`.
- Live validation remained healthy in this cycle (`BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` => `ok=1/1`, `lastAction=wait_combat_start_rate_limit`, `code=200`) and current status is stable at `level=19`, `exp=811`, `gold=329`, inventory `8/30`.
- Safety/defeat pressure remained controlled in the immediate window (no new defeat-indicative churn in latest daemon tail), so safest high-efficiency monster routing + level-threshold movement + conservative risk gap are kept unchanged.
- Hard constraints revalidated unchanged as invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes unequipped gear worse than equipped first.
- Equipment progression requirements remain active and explicit: best-in-slot auto-equip by `equipSlot` + `score(maxDamage+defBonus)` every cycle, staged enhancement plan (early safe gold/no spam -> mid weapon-first with reserve/prereqs -> late armor/accessory with failure-risk controls), and minimal safe enhancement path gated by `scroll + blacksmith npc + resource + rate budget + non-combat`.
- 30-min STRATEGY DIRECTOR (00:46 KST, adaptive mode + equipment progression) KEEP decision with mandatory evidence from latest 20 thinking logs: `level +10` (`9->19`), `gold +20` (`314->334`), `rate_limited 1/20`.
- Live validation stayed healthy in this cycle (`BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` => `ok=1/1`, `code=200`) and status remained stable at `level=19`, `gold=334`, inventory `7/30`.
- Defeat-risk check on supported endpoint `GET /api/logs?action=death&limit=50` showed no new recent death signal in the current window; safety policy remains conservative (safest high-efficiency monster selection + level-threshold movement).
- Hard constraints were revalidated as invariants and kept unchanged: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still prioritizes unequipped gear worse than equipped first.
- Equipment progression requirements remain active and explicit: best-in-slot auto-equip by `equipSlot` + `score(maxDamage+defBonus)` every cycle, staged enhancement plan (early safe gold/no spam -> mid weapon-first with reserve -> late armor/accessory with failure-risk controls), and minimal safe enhancement path gated by `scroll + blacksmith npc + resource + rate budget + non-combat`.

## 2026-03-13
- 30-min STRATEGY DIRECTOR (00:16 KST, adaptive mode + equipment progression) KEEP decision with hard evidence from latest 20 thinking logs: `level +17` (`2->19`), `gold +190` (`134->324`), `rate_limited 1/20`; latest status/smoke kept stability (`level=19`, `exp 299->301`, `gold 329->334`, `ok=1/1`, `code=200`).
- Defeat telemetry now uses supported endpoint `GET /api/logs?action=death&limit=50` (HTTP 200); no new recent death signal observed in the latest cycle window, while unsupported `action=combat` remains `400` and is no longer used for death checks.
- KEEP rationale: improvement criterion is satisfied and near-term risk is controlled (inventory slots 5, safe-area hunting, conservative pacing), so no parameter churn this cycle.
- Hard constraints reaffirmed as execution invariants: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, unequipped gear worse than equipped is liquidated first.
- Equipment progression contract remains active and unchanged: best-in-slot evaluation by `equipSlot` + `score(maxDamage+defBonus)` each cycle, staged enhancement plan (early safe accumulation -> mid weapon-first with reserve/prereqs -> late armor/accessory with cooldown/failure-risk controls), and minimal safe enhancement path gated by `scroll + blacksmith npc + resource + non-combat + rate budget`.
- 30-min STRATEGY DIRECTOR (22:46 KST, adaptive mode + equipment progression) KEEP decision with evidence of improvement: latest thinking-window deltas remained positive (`level +17`, `gold +216`) and current smoke progressed (`exp 2787->2793`, `gold 319->334`, `ok=1/1`, `code=200`).
- Combat-log endpoint currently returns `400` for direct defeat counting, so death delta is treated as unresolved in telemetry while safety policy remains conservative (dynamic risk-gap + safest fallback + level-gated movement).
- 30-min STRATEGY DIRECTOR (adaptive mode) KEEP decision with hard-evidence deltas from latest 20 thinking logs: `level +17`, `gold +221`, `rate_limited 1/20`; smoke remained healthy (`ok=1/1`, `code=200`) with progression on live status (`exp 1529->2543`, deaths delta `0` from combat-log probe).
- Hard constraints are treated as code-level invariants (not env-overridable in execution path): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidate unequipped gear worse than equipped first.
- Equipment progression contract is now explicitly staged for operations:
  - Early game: prioritize gold accumulation + safe hunt; avoid risky enhancement spam.
  - Mid game: when `level>=BUJU_ENHANCE_MID_LEVEL` and gold reserve threshold is met, enhance main weapon first.
  - Late game: expand enhancement to armor/accessory only with risk controls (cooldown, reserve, prerequisite gates).
- Enhancement runtime path remains minimal-safe and prerequisite-gated (`scroll + blacksmith NPC + gold reserve + not in combat + rate budget`), so enhancement is skipped when prerequisites are unsatisfied rather than forcing risk.

## 2026-03-03
- Adopted hot-path deterministic decisioning (FSM + safety gates), with async advisor path for strategy updates.
- Separated periodic autonomous execution from user-facing chat via cron isolated runs.
- Prioritized replayable logs and idempotent action execution before advanced optimization.

## 2026-03-04
- Implemented deterministic 10s worker loop skeleton with explicit tick IDs and serialized JSONL event logging (`tick_started` -> `tick_finished`).
- Added hard safety gates (`maintenance_mode`, `low_health`, `action_queue_saturated`, `cooldown_active`) to block risky actions before FSM evaluation.
- Added retry/backoff action executor with bounded attempts, retryable-error filtering, and idempotency window to reduce duplicate side effects.
- Standardized replay-friendly JSONL schema (`buju.worker.event.v1`) with recursive secret masking to keep logs usable without leaking credentials.
- Added `npm run verify:cycle` smoke verification for retry behavior, idempotency skip path, and secret masking regression coverage.
- Added replay analyzer utility for worker event JSONL logs with per-tick schema/order validation and concise KPI extraction (tick volume, blocked rate, action success/fail/skipped, top safety reasons).
- Added dedicated replay verification script (`npm run verify:replay`) to lock expected analyzer behavior on valid/invalid event streams.
- Added filesystem worker loop lock (`WORKER_LOCK_FILE`, stale TTL) to prevent concurrent loop instances and reclaim stale locks deterministically.
- Added per-tick timeout guard (`WORKER_TICK_TIMEOUT_MS`) so hung ticks emit `tick_error` with timeout code and loop advances to the next tick.
- Added reliability verification script (`npm run verify:worker`) covering live-lock denial, stale lock takeover, timeout recovery, and lock release on shutdown.
- Added in-process action cooldown guard (`ACTION_COOLDOWN_MS`, default `3000`) keyed by `action.type + action.targetId` to block repeated actions across ticks before execution.
- Standardized cooldown-block path as replay-safe JSONL records with `action_executed.status=skipped` and explicit reason `action_cooldown_active` (plus cooldown metadata) followed by `tick_finished`.
- Extended cycle verification (`npm run verify:cycle`) to assert second-tick cooldown blocking, no transport call on blocked tick, and persisted cooldown reason in JSONL.
- Added `scripts/fetch-activity.js` (`npm run activity:fetch`) to produce compact 1h KPI JSON via API-first probing of candidate `/api` activity endpoints, with deterministic fallback to local replay logs (`logs/worker-events.jsonl`) using `analyzeReplayRecords`.
- Standardized hourly activity output contract to `progress_delta`, `action_status_counts`, `known_outcomes`, `source`, `endpoint_statuses`; enforced API key non-disclosure by masking/sanitizing all surfaced strings.
- Added fallback-path verification script (`npm run verify:activity`) with synthetic temp JSONL to lock 1h window filtering and replay-derived KPI correctness.
- Extended replay analyzer summary with deterministic operational counters: cooldown skips (`action_cooldown_active`), tick timeouts (`ETICK_TIMEOUT`), and lock-heartbeat failures (`tick_error.message` contains `lock heartbeat failed`).
- Updated replay CLI summary formatting and verification coverage (`npm run verify:replay`) to keep these counters regression-safe without changing existing summary fields.

## 2026-03-05
- Normalized action-executor idempotency key to battle/action fingerprint (`battleId + (idempotencyKey|id|action.type) + targetId`) instead of including `tickId`, so duplicate action suppression now works across adjacent ticks within the idempotency window.
- Kept deterministic behavior by preferring explicit action-level idempotency identifiers when available, with stable fallback to action type + target.
- Locked regression coverage in `npm run verify:cycle` by asserting a duplicate execute call with a different `tickId` is still skipped and reuses the original `actionKey`.
- Externalized activity probe endpoint order into `config/activity-endpoints.json` and made API probe load this file by default for ops-time reconfiguration without code edits.
- Locked deterministic fallback behavior to built-in endpoint candidates when endpoint config file is missing, unreadable, malformed JSON, empty, or schema-invalid.
- Added dedicated verification (`npm run verify:activity-config`) with a deterministic `data:` endpoint config to assert CLI-provided endpoint config path is honored end-to-end.
- Added deterministic FSM rule `recover-energy-safe-window` so low-energy snapshots with safe health choose `REST` (`RECOVER_ENERGY`) before `hold-default`, preserving higher-priority survival/defense ordering and improving uptime for future attack windows.
- Added per-run activity probe outcome logging to `logs/activity-probe.jsonl` with deterministic endpoint status counters (`total`, `ok`, `http_fail`, `network_fail`, `skipped`, `missing_api_key`) and compact per-endpoint statuses, while preserving API key masking and logging exactly once per `activity:fetch` run.
- Added `npm run verify:activity-log` to assert skip-API mode writes one probe-log record with correct deterministic counters using a temp log path override.
- Added deterministic size guard for `logs/activity-probe.jsonl`: before append, keep only newest complete JSONL tail within configured byte budget and then append the new masked record.
- Added activity probe log byte-budget overrides via env/CLI (`ACTIVITY_PROBE_LOG_MAX_BYTES`, `--activity-probe-log-max-bytes`) and defaulted max size to `256 KiB`.
- Added `npm run verify:activity-log-rotation` to assert oversized probe logs are truncated deterministically, final file size stays within limit, and newest appended record is retained.
- Added deterministic `activity_probe_summary` output for `activity:fetch` by scanning `logs/activity-probe.jsonl` over rolling lookback (default `6h`) and computing per-endpoint trailing consecutive non-`ok` streak + last status.
- Standardized probe-summary output contract to `{ lookback_hours, generated_at, endpoints:[{ endpoint, failure_streak, last_status }] }`, with malformed JSONL line tolerance and compact status normalization (`ok/http_fail/network_fail/skipped/missing_api_key`).
- Added optional probe-summary lookback overrides via env/CLI (`ACTIVITY_PROBE_SUMMARY_LOOKBACK_HOURS`, `--activity-probe-summary-lookback-hours`) while preserving deterministic default behavior.
- Added worker-level deterministic execution-failure circuit breaker with in-process state only (`WORKER_FAILURE_CIRCUIT_STREAK`, default `3`; `WORKER_FAILURE_CIRCUIT_COOLDOWN_MS`, default `30000`).
- Integrated the breaker into cycle/loop safety evaluation so post-streak ticks are safety-blocked with reason `execution_failure_circuit_open`, logged as `safety_evaluated` + `tick_blocked` + `tick_finished(executionStatus=skipped)`, and automatically cleared after cooldown.
- Extended cycle verification to lock the new behavior (`3x failed -> blocked -> cooldown clear`) and adjusted replay validation to accept blocked ticks that terminate with skipped `tick_finished`.
- Extended replay analyzer operational summary with deterministic `executionFailureCircuitOpen` counter for skipped action/tick paths where reason is `execution_failure_circuit_open` (tick-level dedup to avoid double counting).
- Updated replay summary formatter and verification coverage (`npm run verify:replay`, `npm run verify:cycle`) to lock this counter while preserving all existing summary fields.
- Added worker lock-heartbeat retry in loop (`WORKER_LOCK_HEARTBEAT_RETRIES`, default `1`; `WORKER_LOCK_HEARTBEAT_RETRY_DELAY_MS`, default `25`) so transient lock touch failures do not terminate the worker immediately.
- Kept deterministic fail-fast behavior for persistent heartbeat failure: after bounded retries, loop still emits `tick_error` with `lock heartbeat failed` and exits.
- Added lock acquisition dependency injection hook (`acquireLock`) in worker loop to enable deterministic reliability verification without filesystem race dependence.
- Hardened `activity_probe_summary` against test-only/non-production endpoint noise by applying a configured endpoint allowlist sourced from activity endpoint config (with deterministic fallback to built-in candidates).
- Added endpoint-template normalization (`hours=*`, `window=*h`) so allowlist matching remains stable across different runtime hour windows while still excluding unknown schemes like `data:`.
- Wired summary allowlist to the same config path used by API probing to keep probe execution and trailing-streak analytics aligned.
- Added deterministic FSM guard `defend-low-energy-pressure`: when energy is low (`<30`) and an enemy is visible with moderate+ pressure (`enemyThreat >= 50`), choose `RAISE_SHIELD` instead of resting.
- Preserved rule priority: critical health recovery and very-high-threat defense still execute first; low-energy rest remains for safe windows only.
- Locked coverage in `npm run verify:cycle` with explicit low-energy-under-pressure case (`DEFEND_LOW_ENERGY`) plus existing safe-window recovery assertion (`RECOVER_ENERGY`).
- Refined execution-failure circuit semantics so `skipped` executions no longer reset consecutive failure streaks; only `success` clears the streak.
- Rationale: non-execution paths (cooldown/safety skips) should not mask underlying transport instability, improving deterministic failure containment.
- Added verification coverage in `npm run verify:cycle` to assert `failed -> skipped -> failed` opens the circuit when threshold is reached.
- Linked tick-timeout failures to execution-failure circuit accounting in worker loop: when a tick ends with `ETICK_TIMEOUT`, record a failed execution signal in the circuit breaker.
- Rationale: repeated hung ticks indicate execution-path instability and should trigger deterministic safety blocking rather than continuing unrestricted attempts.
- Added worker reliability verification for this path: with circuit threshold `1`, first timeout forces next tick into `execution_failure_circuit_open` block.
- Added deterministic opportunistic attack rule `attack-low-threat-efficient-window`: when enemy is visible, threat is low (`<=25`), and energy is in efficient band (`30-39`), execute `BASIC_ATTACK` instead of falling through to `HOLD`.
- Rationale: increases scoring/action throughput in low-risk windows without weakening existing high-priority survival rules (low-health recovery, high-threat defense, low-energy pressure defense).
- Added cycle verification coverage to lock this behavior with an explicit low-threat, mid-energy scenario.
- Extended replay analyzer with deterministic `topDecisionRules` aggregation from `decision_made.decision.ruleId` to make strategy distribution observable without changing worker hot path.
- Updated replay text summary output to include `top_decision_rules` line (top-5 by count, stable tie-break by ruleId) for quick operator feedback on which deterministic policies are dominating.
- Locked coverage in `npm run verify:replay` with explicit rule-id counts (including `attack-low-threat-efficient-window`) and summary formatting assertion.
- Added deterministic action target validation in worker cycle: actions that require a target (`!= WAIT/REST`) are now skipped when `targetId` is missing, with reason `invalid_action_target`.
- Rationale: prevents avoidable transport attempts/failures from malformed state snapshots and preserves loop stability for next tick decisions.
- Kept circuit semantics stable: invalid-target path is treated as `skipped` (data quality block), not a transport failure.
- Added regression coverage in `npm run verify:cycle` to assert attack decision with missing `enemyId` is blocked pre-transport (`attempts=0`).
- Extended replay operational counters with `invalidActionTarget` to quantify pre-transport target-validation blocks (`reason=invalid_action_target`) introduced in worker safety gate.
- Updated replay summary contract line to include `invalid_target_blocks` so operators can distinguish malformed-action data issues from cooldown/timeouts/circuit blocks.
- Added replay verification coverage with a deterministic invalid-target skipped action record to lock counter and summary formatting behavior.
- Expanded deterministic retryable transport codes in action executor to include transient HTTP failures (`408`, `500`, `502`, `504`) in addition to existing network/time-limit codes.
- Rationale: short-lived upstream gateway/server failures should be retried automatically to preserve action throughput and reduce avoidable failed ticks.
- Added verification coverage in `npm run verify:cycle` asserting `ok:false code=502` retries and succeeds on second attempt with bounded attempts.
- Extended replay operational telemetry with `retriedSuccess` counter (successful `action_executed` where `attempts > 1`) to quantify transient transport instability that was recovered by retries.
- Updated replay summary operational contract to include `retried_success`, enabling fast distinction between hard failures and recovered retry paths.
- Added deterministic verification coverage in `npm run verify:replay` with explicit `attempts:2` success record and summary-line assertion.
- Tightened safety gate threshold for action queue saturation: `pendingActionCount >= maxPendingActions` now blocks tick execution (previously only `>` blocked).
- Rationale: hitting configured queue capacity should be treated as saturated to prevent additional enqueue pressure and reduce cascading latency/failure risk.
- Added cycle verification coverage for edge case equality (`pendingActionCount=2`, `maxPendingActions=2`) asserting deterministic block reason `action_queue_saturated`.
- 30-min strategy director drift-check decision: keep pinned/live spec version at `1.11.1` (no document-version drift), but change live strategy runner behavior to align with newly revalidated official mechanics and endpoint usage.
- Updated live runner to prioritize deterministic score throughput under current rules: cooldown-aware skill selection (`cooldown_remaining`/MP), EXP-first safe monster routing from `GET /api/areas/{area_id}/monsters`, and mutation-shield lifecycle management (`mutation_shield_charm` buy/use at Lv10+).
- Replaced single-shot 429 retry with bounded retry policy honoring `Retry-After` when present, then exponential backoff cap; rationale: maintain near-limit throughput without burst-collisions on action limits (60/min endpoints).
- Externalized new strategy risk controls in `config/strategy.env` (`BUJU_MAX_SAFE_MONSTER_LEVEL_GAP`, `BUJU_MIN_GOLD_RESERVE`, mutation thresholds/stock, backoff windows) to keep future tuning parameter-only when possible.
- Validation: one-cycle live smoke succeeded (`BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` => `ok=1/1`, `hunt`, HTTP 200).
- Priority policy update: added deterministic inventory full-risk liquidation in live runner when used slots approach cap (`>=27` by default), selling only unequipped low-tier/common equipment via `/api/shop/sell` while preserving equipped items.
- Priority policy update: enforced potion-over-rest economics by introducing HP-potion-preferred recovery window (`BUJU_LOW_HP_POTION_RATIO`), so available potions are consumed before `/rest`; rest remains fallback for low-HP/no-potion states.
- Priority policy update: added anti-stall 400 downgrade layer with per-action streak tracking and cooldown skipping (`BUJU_STALL_400_THRESHOLD`, `BUJU_STALL_COOLDOWN_TICKS`), preventing repeated optional-action 400s from blocking hunt throughput.
- Design intent: optional utility actions (sell/buy/equip/move/mutation prep) now soft-fail on 400 and execution proceeds to hunt path in the same tick to maximize continuous scoring actions.
- Validation: one-cycle live smoke after update succeeded (`BUJU_MAX_ACTIONS_PER_CYCLE=1` => `lastAction=use_hp_potion`, HTTP 200).
- Priority policy refinement: upgraded inventory-risk mitigation to batch-first liquidation mode. When inventory usage crosses trigger, runner now performs iterative sell attempts in the same tick toward a target slot watermark, preferring unequipped low-tier/common equipment and preserving equipped gear.
- Priority policy refinement: upgraded potion-over-rest to batch consumption by leveraging `/api/item/use` `quantity` support for consumables; runner computes a bounded quantity plan from HP deficit and available potion tiers.
- Added config knobs to keep batch behavior tunable without code edits: `BUJU_INV_SELL_TARGET_SLOTS`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK`, `BUJU_POTION_USE_MAX_QUANTITY`.
- Kept anti-stall semantics unchanged: repeated HTTP 400 on optional actions still triggers temporary downgrade/skip and immediate continuation to hunt path.
- Live spec drift detected (`1.11.1` pinned vs `1.14.0` live). Adopted compatibility-first runner update without relaxing configured hard constraints.
- Added v1.14 combat-state gating in live runner: skip/defer in-combat actions that are now explicitly blocked (`shop buy/sell`, `equip/unequip`, and move), while continuing allowed combat-safe actions (hunt and consumable use).
- Rationale: avoid predictable `IN_COMBAT` 400 responses on blocked endpoints, reduce wasted action budget, and preserve action throughput under batch-first policy.
- Added anti-stall refinement for low-HP rest path: `POST /rest` now soft-fails on HTTP 400 and execution continues to hunt decision path in the same tick (while preserving immediate return on successful rest).
- Rationale: under live rules/drift conditions, rest can intermittently return 400; treating it as terminal for the tick created avoidable throughput collapse (`ok=0/1`).
- Added configurable 429 retry ceiling for live strategy requests (`BUJU_RETRY_MAX_ATTEMPTS`, default 4) to reduce false-negative failed ticks during short rate-limit bursts.
- Rationale: a fixed low retry cap caused occasional terminal hunt 429 outcomes (`ok=0/1`) even when immediate subsequent retry windows succeeded.
- Adaptive control update (2026-03-07 21:26 KST): repeated bottleneck detected in last 20 thinking logs (`decision_type=rate_limited` 19/19), so keep hard constraints unchanged and reduce request burst pressure by increasing `BUJU_BASE_DELAY_MS` from `250` to `2100`.
- Expected KPI target (next 30 min): lower `wait_hunt_rate_limit` share below 50% of sampled single-tick validations while maintaining HTTP 200 success and no hard-constraint regressions.
- Change is intentionally small/reversible (config-only), with no modifications to sell-threshold logic or rest-first thresholds.
- Adaptive follow-up (2026-03-07 21:49 KST): post-change sample still showed `decision_type=rate_limited` dominance in recent logs, so applied a second-step pacing reduction by changing `BUJU_BASE_DELAY_MS` from `2100` to `2600`.
- Rationale: preserve all hard sell/rest constraints while incrementally reducing burst collisions with hunt/minute windows; avoid broad logic churn.
- KPI target (next 30 min): increase share of `status_check` (non-rate-limited) thinking decisions to >=20% and keep smoke runs at HTTP 200.
- Adaptive step-3 (2026-03-07 22:19 KST): latest 20 thinking logs still reported `rate_limited` in 20/20 entries after the 2600ms pacing change, so delay was raised again from `2600` to `3000`.
- Scope safety: this is a reversible pacing-only change; sell-priority constraints and rest-first thresholds remain untouched.
- KPI target (next 30 min): bring `rate_limited` share in recent 20-log window down to <=70% while preserving smoke-run success (`ok=1/1`, HTTP 200).
- Adaptive step-4 (2026-03-07 22:49 KST): after pacing-only adjustments, recent thinking history still showed persistent `rate_limited` saturation (20/20), so reduced per-cycle burst size by changing `BUJU_MAX_ACTIONS_PER_CYCLE` from `30` to `24`.
- Rationale: lowering loop action volume is reversible and directly targets minute-window pressure without modifying mandatory sell/rest hard constraints.
- KPI target (next 30 min): reduce trailing 20-log `rate_limited` count below 15 while preserving one-tick smoke success and daemon continuity.
- Adaptive step-5 (2026-03-07 23:19 KST): bottleneck persisted (`rate_limited` still 20/20 in latest window) after action-cap 24, so reduced `BUJU_MAX_ACTIONS_PER_CYCLE` further from `24` to `20`.
- Rationale: keep constraints intact while reducing per-cycle quota pressure; this is reversible and narrower than logic-level edits.
- KPI target (next 30 min): trailing 20-log `rate_limited` count <=12 and at least one `status_check` entry, with smoke validation still `ok=1/1`.
- Adaptive step-6 (2026-03-07 23:49 KST): even after reducing cycle quota to 20, latest 20-log sample still showed `rate_limited` at 20/20, so pacing was tightened again by changing `BUJU_BASE_DELAY_MS` from `3000` to `3400`.
- Rationale: this keeps hard constraints intact while further smoothing request cadence; reversible config-only change.
- KPI target (next 30 min): achieve at least 2 `status_check` decisions in trailing 20 thinking logs and keep smoke result at HTTP 200.
- Adaptive step-7 (2026-03-08 00:19 KST): recent window still showed `rate_limited` saturation (20/20) after delay 3400 and cycle cap 20, so per-cycle quota was reduced again from `20` to `16`.
- Rationale: this is a minimal, reversible pressure cut that leaves mandatory sell-priority and rest-first hard constraints untouched.
- KPI target (next 30 min): trailing 20-log `rate_limited` <=10 and at least 3 `status_check` entries, while keeping smoke checks green (`ok=1/1`, HTTP 200).
- Adaptive step-8 (2026-03-08 00:50 KST): with `BUJU_MAX_ACTIONS_PER_CYCLE=16`, the latest trailing window still remained `rate_limited` 20/20, so cadence was further relaxed by changing `BUJU_BASE_DELAY_MS` from `3400` to `3800`.
- Rationale: maintain all hard constraints while reducing minute-window contention through additional inter-action spacing; reversible and config-only.
- KPI target (next 30 min): trailing-20 `rate_limited` <=8 and at least 4 `status_check` entries, with smoke validation still `ok=1/1` and HTTP 200.
- Adaptive step-9 (2026-03-08 01:20 KST): after delay 3800 and action cap 16, the trailing 20-log sample still stayed `rate_limited` 20/20, so the per-cycle action budget was cut again from `16` to `12`.
- Rationale: this trims burst pressure using a reversible config-only change while keeping all hard inventory and rest constraints untouched.
- KPI target (next 30 min): trailing-20 `rate_limited` <=6 and at least 5 `status_check` entries, with smoke run staying `ok=1/1` (HTTP 200).
- Adaptive step-10 (2026-03-08 01:50 KST): after delay 3800 plus action cap 12, the latest trailing history still showed `rate_limited` 20/20, so retry pressure was reduced by changing `BUJU_RETRY_MAX_ATTEMPTS` from `4` to `2`.
- Rationale: fewer immediate retry bursts during throttled windows may cut contention without altering protected sell-priority or rest-first constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=5 with >=6 `status_check` entries, while preserving smoke success (`ok=1/1`, HTTP 200).
- Adaptive step-11 (2026-03-08 02:20 KST): the latest trailing window remained fully rate-limited (20/20) even after retry-attempt reduction, so cycle quota was trimmed again from `12` to `10`.
- Rationale: this is a reversible throughput cut intended to reduce minute-window contention while keeping all hard inventory and recovery constraints untouched.
- KPI target (next 30 min): trailing-20 `rate_limited` <=4 with >=7 `status_check` entries, and smoke check stays `ok=1/1` (HTTP 200).
- Adaptive step-12 (2026-03-08 02:53 KST): with action cap 10 and reduced retries, the trailing log sample still reported `rate_limited` 20/20, so pacing was widened further by changing `BUJU_BASE_DELAY_MS` from `3800` to `4200`.
- Rationale: this keeps all mandatory constraints intact and applies a reversible timing-only pressure reduction to reduce hunt-budget collisions.
- KPI target (next 30 min): trailing-20 `rate_limited` <=3 with >=8 `status_check` entries while keeping smoke checks green (`ok=1/1`, HTTP 200).
- Adaptive step-13 (2026-03-08 03:22 KST): trailing history again remained `rate_limited` 20/20 after delay 4200 and cap 10, so base pacing was widened from `4200` to `4600`.
- Rationale: keep mandatory constraints untouched and apply a reversible timing-only reduction in request density to probe for rate-window alignment.
- KPI target (next 30 min): trailing-20 `rate_limited` <=2 with >=9 `status_check` entries, while preserving smoke success (`ok=1/1`, HTTP 200).
- Adaptive step-14 (2026-03-08 03:50 KST): after widening delay to 4600, the latest 20-log window still remained `rate_limited` 20/20, so cycle quota was reduced from `10` to `8`.
- Rationale: reduce per-cycle request density further with a reversible config-only change while preserving all protected sell and rest hard constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=1 with >=10 `status_check` entries and smoke checks still `ok=1/1` (HTTP 200).
- Adaptive step-15 (2026-03-08 04:22 KST): despite quota reduction to 8, trailing 20 thinking logs remained `rate_limited` 20/20 across consecutive runs, so cadence was widened from `BUJU_BASE_DELAY_MS=4600` to `5200`.
- Rationale: repeated bottleneck persisted while progression continued (`level 35 유지, exp/gold 증가`), so KEEP was disallowed and a minimal reversible pacing-only change was selected without touching mandatory sell/rest constraints.
- KPI target (next 30 min): reduce trailing-20 `rate_limited` to <=18, secure >=2 `status_check` entries, and keep smoke validation at `ok=1/1` with HTTP 200.
- Adaptive step-16 (2026-03-08 04:49 KST): after delay 5200 and cap 8, trailing thinking distribution was still pinned at `rate_limited` 20/20, so per-cycle quota was tightened once more from `BUJU_MAX_ACTIONS_PER_CYCLE=8` to `6`.
- Rationale: maintain all hard inventory liquidation/rest-economy constraints and apply a small reversible throughput reduction because the same bottleneck repeated across at least two consecutive runs.
- KPI target (next 30 min): trailing-20 `rate_limited` <=16 with >=3 `status_check` decisions while preserving smoke result `ok=1/1` (HTTP 200).
- Adaptive step-17 (2026-03-08 05:18 KST): with cycle cap already at 6, the trailing 20-log window still stayed `rate_limited` 20/20, so pacing was widened from `BUJU_BASE_DELAY_MS=5200` to `5600`.
- Rationale: progression exists (EXP +416) but repeated throttle bottleneck persisted; selected a minimal reversible timing-only change that does not touch mandatory sell-priority and rest-first hard constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=15 with >=4 `status_check` decisions, while keeping smoke run `ok=1/1` (HTTP 200).
- Adaptive step-18 (2026-03-08 05:48 KST): even after widening delay to 5600, trailing thinking remained `rate_limited` 20/20, so per-cycle quota was reduced from `BUJU_MAX_ACTIONS_PER_CYCLE=6` to `5`.
- Rationale: same bottleneck repeated across runs despite positive EXP/gold progress, so KEEP is disallowed; applied a small reversible throughput cut while preserving all protected inventory liquidation and rest-economy constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=14 with >=5 `status_check` decisions, while preserving smoke validation `ok=1/1` (HTTP 200).
- Adaptive step-19 (2026-03-08 06:18 KST): with action cap already at 5, trailing logs still showed `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=5600` to `6000`.
- Rationale: repeated bottleneck remained dominant across consecutive runs; chose a minimal reversible pacing change while preserving all hard sell-priority and rest-first economy constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=13 with >=6 `status_check` decisions, while keeping smoke run `ok=1/1` (HTTP 200).
- Adaptive step-20 (2026-03-08 06:48 KST): after increasing delay to 6000, the trailing 20 thinking logs still remained `rate_limited` 20/20, so cycle quota was reduced again from `BUJU_MAX_ACTIONS_PER_CYCLE=5` to `4`.
- Rationale: repeated bottleneck persists across consecutive runs despite EXP/gold growth; applied a small reversible throughput cut while preserving mandatory inventory liquidation and rest-first constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=12 with >=7 `status_check` decisions, while maintaining smoke check `ok=1/1` (HTTP 200).
- Adaptive step-21 (2026-03-08 07:18 KST): with action cap fixed at 4, trailing 20 thinking logs still stayed `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=6000` to `6400`.
- Rationale: repeated bottleneck remains dominant over multiple runs; selected a minimal reversible pacing adjustment while preserving all mandatory sell-priority and rest-first hard constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=11 with >=8 `status_check` decisions, while keeping smoke run `ok=1/1` (HTTP 200).
- Adaptive step-22 (2026-03-08 07:48 KST): after widening delay to 6400, trailing 20 thinking logs still reported `rate_limited` 20/20, so cycle quota was further reduced from `BUJU_MAX_ACTIONS_PER_CYCLE=4` to `3`.
- Rationale: repeated bottleneck persisted across consecutive runs despite continued EXP/gold gain; applied a small reversible throughput reduction while preserving all hard inventory and rest-first constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=10 with >=9 `status_check` decisions, while preserving smoke validation `ok=1/1` (HTTP 200).
- Adaptive step-23 (2026-03-08 08:18 KST): with cycle quota already at 3, trailing 20 thinking logs remained `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=6400` to `6800`.
- Rationale: repeated bottleneck continues across consecutive runs even as EXP/gold advance; selected a minimal reversible pacing-only change while preserving mandatory sell-priority and rest-first hard constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=9 with >=10 `status_check` decisions, while keeping smoke run `ok=1/1` (HTTP 200).
- Adaptive step-24 (2026-03-08 08:48 KST): after widening delay to 6800, trailing 20 thinking logs still showed `rate_limited` 20/20, so cycle quota was reduced from `BUJU_MAX_ACTIONS_PER_CYCLE=3` to `2`.
- Rationale: repeated bottleneck persisted across consecutive runs despite continued progress in exp/gold; selected a minimal reversible throughput cut while preserving all mandatory inventory and rest-first hard constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=8 with >=11 `status_check` decisions, while preserving smoke validation `ok=1/1` (HTTP 200).
- Adaptive step-25 (2026-03-08 09:18 KST): with cycle quota already constrained to 2, trailing 20 thinking logs still remained `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=6800` to `7200`.
- Rationale: repeated bottleneck persisted across consecutive runs even with reduced cycle quota; applied a small reversible pacing-only adjustment while preserving mandatory sell-priority and rest-first constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=7 with >=12 `status_check` decisions, while keeping smoke run `ok=1/1` (HTTP 200).
- Adaptive step-26 (2026-03-08 09:48 KST): after widening delay to 7200, trailing 20 thinking logs still reported `rate_limited` 20/20, so cycle quota was reduced from `BUJU_MAX_ACTIONS_PER_CYCLE=2` to `1`.
- Rationale: repeated bottleneck remained unchanged across consecutive runs despite incremental exp progression; selected a minimal reversible throughput cut while preserving all mandatory inventory and rest-first hard constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=6 with >=13 `status_check` decisions, while preserving smoke validation `ok=1/1` (HTTP 200).
- Adaptive step-27 (2026-03-08 10:18 KST): with cycle quota at the minimum practical value (`BUJU_MAX_ACTIONS_PER_CYCLE=1`), trailing 20 thinking logs still remained `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=7200` to `7600`.
- Rationale: repeated bottleneck persisted across consecutive runs while hard constraints were already tight; applied a small reversible pacing-only change without altering mandatory inventory liquidation or rest-first thresholds.
- KPI target (next 30 min): trailing-20 `rate_limited` <=5 with >=14 `status_check` decisions, while keeping smoke run `ok=1/1` (HTTP 200).
- Adaptive step-28 (2026-03-08 10:48 KST): after widening delay to 7600 with cycle quota fixed at 1, trailing 20 thinking logs still showed `rate_limited` 20/20, so delay was widened again from `BUJU_BASE_DELAY_MS=7600` to `8000`.
- Rationale: even with level-up progress (35→36), repeated bottleneck remained unchanged across consecutive runs; selected a minimal reversible pacing-only change while preserving all hard inventory/rest constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=4 with >=15 `status_check` decisions, while preserving smoke validation `ok=1/1` (HTTP 200).
- Adaptive step-29 (2026-03-08 11:18 KST): with cycle quota locked at 1 and delay at 8000, trailing 20 thinking logs still remained `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=8000` to `8400`.
- Rationale: repeated bottleneck persisted across consecutive runs despite ongoing exp progression at level 36; applied a minimal reversible pacing-only adjustment while preserving all mandatory inventory liquidation/rest-first constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=3 with >=16 `status_check` decisions, while keeping smoke run `ok=1/1` (HTTP 200).
- Adaptive step-30 (2026-03-08 11:48 KST): with cycle quota still fixed at 1, trailing 20 thinking logs again stayed `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=8400` to `8800`.
- Rationale: same bottleneck persisted over consecutive runs even with strict throughput limits; applied a minimal reversible pacing-only change while preserving all mandatory inventory and rest-first hard constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=2 with >=17 `status_check` decisions, while preserving smoke validation `ok=1/1` (HTTP 200).
- Adaptive step-31 (2026-03-08 12:18 KST): with quota pinned at 1, trailing 20 thinking logs still reported `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=8800` to `9200`.
- Rationale: repeated bottleneck remained unchanged across consecutive runs despite ongoing exp progression; applied a minimal reversible pacing-only adjustment while preserving all mandatory inventory liquidation and rest-first constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=1 with >=18 `status_check` decisions, while keeping smoke run `ok=1/1` (HTTP 200).
- Adaptive step-32 (2026-03-08 12:49 KST): with cycle quota still fixed at 1, trailing 20 thinking logs remained `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=9200` to `9600`.
- Rationale: repeated bottleneck persisted across consecutive runs despite continued exp progression; applied a minimal reversible pacing-only change while preserving all mandatory inventory/rest-first hard constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=1 with >=18 `status_check` decisions, while preserving smoke validation `ok=1/1` (HTTP 200).
- Adaptive step-33 (2026-03-08 13:18 KST): with cycle quota pinned at 1, trailing 20 thinking logs still remained `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=9600` to `10000`.
- Rationale: repeated bottleneck persisted across consecutive runs despite ongoing exp/gold progression; applied a minimal reversible pacing-only change while preserving all mandatory inventory liquidation and rest-first constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=1 with >=18 `status_check` decisions, while keeping smoke run `ok=1/1` (HTTP 200).
- Adaptive step-34 (2026-03-08 13:49 KST): with cycle quota still fixed at 1, trailing 20 thinking logs remained `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=10000` to `10400`.
- Rationale: repeated bottleneck persisted across consecutive runs despite continued exp/gold progression; applied a minimal reversible pacing-only adjustment while preserving all mandatory inventory liquidation/rest-first constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=1 with >=18 `status_check` decisions, while preserving smoke validation `ok=1/1` (HTTP 200).
- Adaptive step-35 (2026-03-08 14:18 KST): with cycle quota pinned at 1, trailing 20 thinking logs still remained `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=10400` to `10800`.
- Rationale: repeated bottleneck persisted across consecutive runs despite ongoing exp progression; applied a minimal reversible pacing-only change while preserving all mandatory inventory liquidation/rest-first hard constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=1 with >=18 `status_check` decisions, while keeping smoke run `ok=1/1` (HTTP 200).
- Adaptive step-36 (2026-03-08 14:48 KST): with cycle quota fixed at 1, trailing 20 thinking logs still remained `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=10800` to `11200`.
- Rationale: repeated bottleneck persisted across consecutive runs despite continued exp progression; applied a minimal reversible pacing-only adjustment while preserving all mandatory inventory liquidation/rest-first constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=1 with >=18 `status_check` decisions, while preserving smoke validation `ok=1/1` (HTTP 200).
- Adaptive step-37 (2026-03-08 15:18 KST): with cycle quota pinned at 1, trailing 20 thinking logs still remained `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=11200` to `11600`.
- Rationale: repeated bottleneck persisted across consecutive runs despite ongoing exp/gold progression; applied a minimal reversible pacing-only change while preserving all mandatory inventory liquidation/rest-first constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=1 with >=19 `status_check` decisions, while keeping smoke run `ok=1/1` (HTTP 200).
- Adaptive step-38 (2026-03-08 15:48 KST): with cycle quota fixed at 1, trailing 20 thinking logs remained `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=11600` to `12000`.
- Rationale: repeated bottleneck persisted across consecutive runs despite continued exp/gold progression; applied a minimal reversible pacing-only adjustment while preserving all mandatory inventory liquidation/rest-first constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=1 with >=19 `status_check` decisions, while preserving smoke validation `ok=1/1` (HTTP 200).
- Adaptive step-39 (2026-03-08 16:18 KST): with cycle quota fixed at 1, trailing 20 thinking logs again remained `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=12000` to `12400`.
- Rationale: repeated bottleneck persisted across consecutive runs despite ongoing exp progression; applied a minimal reversible pacing-only change while preserving all mandatory inventory liquidation/rest-first hard constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=1 with >=19 `status_check` decisions, while keeping smoke run `ok=1/1` (HTTP 200).
- Adaptive step-40 (2026-03-08 16:48 KST): with cycle quota fixed at 1, trailing 20 thinking logs still remained `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=12400` to `12800`.
- Rationale: repeated bottleneck persisted across consecutive runs despite continued exp/gold progression; applied a minimal reversible pacing-only adjustment while preserving all mandatory inventory liquidation/rest-first constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=1 with >=19 `status_check` decisions, while preserving smoke validation `ok=1/1` (HTTP 200).
- Adaptive step-41 (2026-03-08 17:18 KST): with cycle quota fixed at 1, trailing 20 thinking logs still reported `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=12800` to `13200`.
- Rationale: repeated bottleneck persisted across consecutive runs despite continued exp progression; applied a minimal reversible pacing-only change while preserving all mandatory inventory liquidation/rest-first hard constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=1 with >=19 `status_check` decisions, while keeping smoke run `ok=1/1` (HTTP 200).
- Adaptive step-42 (2026-03-08 17:48 KST): with cycle quota fixed at 1, trailing 20 thinking logs again remained `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=13200` to `13600`.
- Rationale: repeated bottleneck persisted across consecutive runs despite ongoing exp/gold progression; applied a minimal reversible pacing-only adjustment while preserving all mandatory inventory liquidation/rest-first constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=1 with >=19 `status_check` decisions, while preserving smoke validation `ok=1/1` (HTTP 200).
- Adaptive step-43 (2026-03-08 18:18 KST): with cycle quota fixed at 1, trailing 20 thinking logs remained `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=13600` to `14000`.
- Rationale: repeated bottleneck persisted across consecutive runs despite ongoing exp progression; applied a minimal reversible pacing-only change while preserving all mandatory inventory liquidation/rest-first hard constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=1 with >=19 `status_check` decisions, while keeping smoke run `ok=1/1` (HTTP 200).
- Adaptive step-44 (2026-03-08 18:48 KST): with cycle quota fixed at 1, trailing 20 thinking logs still remained `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=14000` to `14400`.
- Rationale: repeated bottleneck persisted across consecutive runs despite continued exp/gold progression; applied a minimal reversible pacing-only adjustment while preserving all mandatory inventory liquidation/rest-first constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=1 with >=19 `status_check` decisions, while preserving smoke validation `ok=1/1` (HTTP 200).
- Adaptive step-45 (2026-03-08 19:18 KST): with cycle quota fixed at 1, trailing 20 thinking logs still remained `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=14400` to `14800`.
- Rationale: repeated bottleneck persisted across consecutive runs despite ongoing exp/gold progression; applied a minimal reversible pacing-only adjustment while preserving all mandatory inventory liquidation/rest-first constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=1 with >=19 `status_check` decisions, while keeping smoke run `ok=1/1` (HTTP 200).
- Adaptive step-46 (2026-03-08 19:48 KST): with cycle quota fixed at 1, trailing 20 thinking logs still remained `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=14800` to `15200`.
- Rationale: repeated bottleneck persisted across consecutive runs despite ongoing exp/gold progression; applied a minimal reversible pacing-only adjustment while preserving all mandatory inventory liquidation/rest-first constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=1 with >=19 `status_check` decisions, while preserving smoke validation `ok=1/1` (HTTP 200).
- Adaptive step-47 (2026-03-08 20:19 KST): with cycle quota fixed at 1, trailing 20 thinking logs still remained `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=15200` to `15600`.
- Rationale: repeated bottleneck persisted across consecutive runs despite continued progression; applied a minimal reversible pacing-only adjustment while preserving all mandatory inventory liquidation/rest-first constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=1 with >=19 `status_check` decisions, while keeping smoke run `ok=1/1` (HTTP 200).
- Adaptive step-48 (2026-03-08 20:48 KST): with cycle quota fixed at 1, trailing 20 thinking logs still remained `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=16000` to `16400`.
- Rationale: repeated bottleneck persisted across consecutive runs despite ongoing progression; applied a minimal reversible pacing-only adjustment while preserving all mandatory inventory liquidation/rest-first constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=1 with >=19 `status_check` decisions, while preserving smoke validation `ok=1/1` (HTTP 200).
- Adaptive step-49 (2026-03-08 21:19 KST): with cycle quota fixed at 1, trailing 20 thinking logs still remained `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=16400` to `16800`.
- Rationale: repeated bottleneck persisted across consecutive runs despite ongoing progression; applied a minimal reversible pacing-only adjustment while preserving all mandatory inventory liquidation/rest-first constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=1 with >=19 `status_check` decisions, while preserving smoke validation `ok=1/1` (HTTP 200).
- Adaptive step-50 (2026-03-08 21:48 KST): with cycle quota fixed at 1, trailing 20 thinking logs still remained `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=16800` to `17200`.
- Rationale: repeated bottleneck persisted across consecutive runs despite ongoing progression; applied a minimal reversible pacing-only adjustment while preserving all mandatory inventory liquidation/rest-first constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=1 with >=19 `status_check` decisions, while preserving smoke validation `ok=1/1` (HTTP 200).
- Adaptive step-51 (2026-03-08 22:18 KST): with cycle quota fixed at 1, trailing 20 thinking logs still remained `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=17200` to `17600`.
- Rationale: repeated bottleneck persisted across consecutive runs despite ongoing progression; applied a minimal reversible pacing-only adjustment while preserving all mandatory inventory liquidation/rest-first constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=1 with >=19 `status_check` decisions, while preserving smoke validation `ok=1/1` (HTTP 200).
- Adaptive step-52 (2026-03-08 22:48 KST): with cycle quota fixed at 1, trailing 20 thinking logs still remained `rate_limited` 20/20, so base delay was widened from `BUJU_BASE_DELAY_MS=17600` to `18000`.
- Rationale: repeated bottleneck persisted across consecutive runs despite ongoing progression; applied a minimal reversible pacing-only adjustment while preserving all mandatory inventory liquidation/rest-first constraints.
- KPI target (next 30 min): trailing-20 `rate_limited` <=1 with >=19 `status_check` decisions, while preserving smoke validation `ok=1/1` (HTTP 200).
- Adaptive step-53 (2026-03-08 23:48 KST): bootstrap reset state persisted (`level=1`, sparse trailing thinking window), and hunt smoke continued returning `HTTP 400`, so cadence was adjusted from `BUJU_BASE_DELAY_MS=12000` down to `8000`.
- Rationale: at low-level bootstrap, overly wide delay was not helping recover from repeated hunt-400 stalls; apply a reversible cadence reduction while keeping all mandatory sell/rest constraints untouched.
- KPI target (next 30 min): recover smoke to `ok=1/1` with HTTP 200 and move progression beyond bootstrap (`level>=2` or `exp>0`).
- Note: superseded intermediate note removed; current adaptive step for this cycle is the delay increase to `BUJU_BASE_DELAY_MS=18000` with standard KPI tracking.
- Adaptive step-54 (2026-03-09 00:18 KST): bootstrap stagnation persisted (`level=1`, `exp=0`, `gold=100`) and repeated hunt smoke still returned `HTTP 400`, so delay was reduced from `BUJU_BASE_DELAY_MS=8000` to `6000` for recovery.
- Rationale: repeated bottleneck changed from rate-limit saturation to hunt-400 at low-level reset; tighter reversible cadence was selected to unblock bootstrap progression while preserving all mandatory sell/rest constraints.
- KPI target (next 30 min): recover smoke to `ok=1/1` with HTTP 200 and show progression beyond reset (`level>=2` or `exp>0`).
- Adaptive step-55 (2026-03-09 00:48 KST): bootstrap stagnation remained (`level=1`, `exp=0`, `gold=100`) and hunt smoke continued returning `HTTP 400`, so delay was reduced from `BUJU_BASE_DELAY_MS=6000` to `5000`.
- Rationale: repeated hunt-400 bottleneck persisted across consecutive bootstrap runs; applied a minimal reversible cadence reduction while preserving all mandatory inventory liquidation/rest-first hard constraints.
- KPI target (next 30 min): recover smoke to `ok=1/1` with HTTP 200 and produce first positive progression signal (`level>=2` or `exp>0`).
- Adaptive step-56 (2026-03-09 00:48 KST, late-cycle correction): bootstrap stagnation remained with repeated hunt `HTTP 400`, so cadence was tightened further from `BUJU_BASE_DELAY_MS=5000` to `4000`.
- Rationale: repeated identical blocker persisted within the same cycle window; selected a small reversible cadence cut while preserving all mandatory inventory liquidation/rest-first constraints.
- KPI target (next 30 min): achieve first smoke `ok=1/1` HTTP 200 and progression signal (`exp>0` or `level>=2`).
- Adaptive step-57 (2026-03-09 01:18 KST): bootstrap stagnation still held (`level=1`, `exp=0`, `gold=100`) and hunt smoke remained `HTTP 400`, so cadence was tightened from `BUJU_BASE_DELAY_MS=4000` to `3000`.
- Rationale: repeated bootstrap hunt-400 bottleneck persisted across consecutive cycles; applied a minimal reversible cadence reduction while preserving all mandatory inventory liquidation/rest-first constraints.
- KPI target (next 30 min): obtain first smoke `ok=1/1` with HTTP 200 and any positive progression signal (`exp>0` or `level>=2`).
- Adaptive step-58 (2026-03-09 01:48 KST): bootstrap stagnation persisted (`level=1`, `exp=0`, `gold=100`) with repeated hunt `HTTP 400`, so cadence was tightened from `BUJU_BASE_DELAY_MS=3000` to `2500`.
- Rationale: same non-rate-limit bottleneck repeated across consecutive cycles; selected a minimal reversible timing cut while preserving all mandatory inventory liquidation/rest-first hard constraints.
- KPI target (next 30 min): get first smoke `ok=1/1` HTTP 200 and any progression signal (`exp>0` or `level>=2`).
- Adaptive step-59 (2026-03-09 02:18 KST): bootstrap remained stuck (`level=1`, `exp=0`, `gold=100`) and hunt smoke continued failing with `HTTP 400`, so cadence was reduced from `BUJU_BASE_DELAY_MS=2500` to `2000`.
- Rationale: repeated non-rate-limit bottleneck persisted across consecutive runs; applied a minimal reversible timing cut while preserving all mandatory inventory liquidation/rest-first hard constraints.
- KPI target (next 30 min): achieve first smoke `ok=1/1` with HTTP 200 and first progression event (`exp>0` or `level>=2`).
- Adaptive step-60 (2026-03-09 02:48 KST): bootstrap state remained unchanged (`level=1`, `exp=0`, `gold=100`) and hunt smoke still failed with `HTTP 400`, so cadence was reduced from `BUJU_BASE_DELAY_MS=2000` to `1500`.
- Rationale: same repeated non-rate-limit bottleneck persisted across consecutive runs; selected a minimal reversible timing cut while preserving all mandatory inventory liquidation/rest-first constraints.
- KPI target (next 30 min): first smoke `ok=1/1` with HTTP 200 and any progression signal (`exp>0` or `level>=2`).
- Adaptive step-61 (2026-03-09 03:18 KST): bootstrap remained fully flat (`level=1`, `exp=0`, `gold=100`) and hunt smoke continued failing with `HTTP 400`, so cadence was reduced from `BUJU_BASE_DELAY_MS=1500` to `1000`.
- Rationale: repeated same-cause bottleneck persisted across consecutive cycles with zero forward movement; applied a minimal reversible timing cut while preserving all mandatory inventory liquidation/rest-first constraints.
- KPI target (next 30 min): first smoke success `ok=1/1` HTTP 200 and first progression event (`exp>0` or `level>=2`).
- Adaptive step-62 (2026-03-09 03:48 KST): bootstrap state remained unchanged (`level=1`, `exp=0`, `gold=100`) and hunt smoke still failed with `HTTP 400`, so cadence was reduced from `BUJU_BASE_DELAY_MS=1000` to `800`.
- Rationale: the same non-rate-limit bottleneck repeated again across consecutive cycles without metric lift; selected a minimal reversible timing cut while preserving all mandatory inventory liquidation/rest-first constraints.
- KPI target (next 30 min): obtain first smoke `ok=1/1` with HTTP 200 and first progression evidence (`exp>0` or `level>=2`).
- Adaptive step-63 (2026-03-09 04:20 KST): repeated bottleneck root cause was moved from timing to API compatibility — live spec drift (`1.11.1` pinned vs `1.17.0` live) requires combat initiation through `POST /api/combat/start` with `{monster_id, area}`; `/hunt` path remained the repeating `HTTP 400` cause.
- CHANGE (reversible): in `scripts/live-strategy-runner.js`, switched primary combat call to `/combat/start` with required payload and kept guarded `/hunt` fallback only for compatibility (`404` or `API_DEPRECATED`). Added `BUJU_USE_COMBAT_START=1` feature toggle in `config/strategy.env` for quick rollback.
- Outcome evidence: one-tick validation recovered from `ok=0/1 code=400` to `ok=1/1 code=200`, with first measurable bootstrap progress (`exp 0→3`, `gold 100→103`).
- Constraint integrity: all mandatory inventory sell constraints and rest-first economy thresholds unchanged.
- KPI target (next 30 min): sustain smoke `ok=1/1` HTTP 200 and increase to `exp>=15`, `gold>=115` while keeping inventory slots `<=8`.
- Adaptive step-64 (2026-03-09 04:48 KST): after switching to `/combat/start`, the 30-minute interval still showed flat progression (`level 1`, `exp 3`, `gold 103`, `inv 3`, same area), so KEEP remained disallowed due to stagnation even though HTTP 400 failures were removed.
- CHANGE (reversible): raised `BUJU_MAX_ACTIONS_PER_CYCLE` from `1` to `2` in `config/strategy.env` to restore minimal throughput while retaining the new `/combat/start` primary path.
- Constraint integrity: all mandatory inventory sell constraints and rest-first economy thresholds were kept unchanged.
- KPI target (next 30 min): reach `exp>=12` and `gold>=112` from current `exp=3/gold=103`, maintain smoke `>=2/2` HTTP 200, and keep inventory slots `<=8`.
- Adaptive step-65 (2026-03-09 05:48 KST): the subsequent interval remained flat (`level 1->1`, `exp 3->3`, `gold 113->113`, inventory/area unchanged), and the trailing thinking window was sparse (`1` record), so repeated-error evidence was inconclusive but stagnation alone still forced CHANGE.
- CHANGE (reversible): increased `BUJU_MAX_ACTIONS_PER_CYCLE` from `2` to `3` to probe whether slightly higher per-cycle throughput yields measurable growth under stable `/combat/start` execution.
- Constraint integrity: hard inventory sell constraints and rest-first economy thresholds remain unchanged.
- KPI target (next 30 min): achieve `exp>=6` and `gold>=116` with smoke `ok>=3/3` HTTP 200, while keeping inventory slots `<=8`.
- Adaptive step-66 (2026-03-09 06:18 KST): progression was still stagnant (`level 1`, `exp 3`, `gold 113`, same area/inventory), and a temporary 4-action probe surfaced rate pressure (`ok=3/4`, `code=429`).
- CHANGE (reversible): shifted from throughput increase to collision mitigation — set `BUJU_MAX_ACTIONS_PER_CYCLE` back to `3` (from 4 test) and widened `BUJU_BASE_DELAY_MS` from `800` to `1000`.
- Rationale: preserve combat-start stability while lowering minute-window burst collisions; keep all hard inventory/rest constraints untouched.
- KPI target (next 30 min): maintain smoke `ok>=3/3` with `code=200` (no 429), and lift to `exp>=6`, `gold>=116`, inventory slots `<=8`.
- Adaptive step-67 (2026-03-09 06:48 KST): despite stabilized 3-action operation, progression remained flat for another interval (`level 1`, `exp 3`, `gold 113`, inventory/area unchanged), so CHANGE remained mandatory by stagnation rule.
- CHANGE (reversible): increased timing buffer `BUJU_BASE_DELAY_MS` from `1000` to `1200` while keeping `BUJU_MAX_ACTIONS_PER_CYCLE=3`.
- Rationale: apply timing-side mitigation (not quota churn) to reduce burst-collision risk after the recent 429 signal, while preserving all hard inventory/rest constraints.
- KPI target (next 30 min): keep smoke `ok>=3/3` with `code=200` and no 429, and reach `exp>=6`, `gold>=116`, inventory slots `<=8`.
- Adaptive step-68 (2026-03-09 07:19 KST): stagnation repeated again (`level 1`, `exp 3`, `gold 113`, inventory 3, same area) across consecutive runs, while smoke remained healthy (`ok=3/3`, `code=200`).
- CHANGE (reversible): widened timing guardrail further by setting `BUJU_BASE_DELAY_MS` from `1200` to `1400` and keeping `BUJU_MAX_ACTIONS_PER_CYCLE=3` unchanged.
- Rationale: with repeated flat KPI and no hard error signature, favor incremental pacing dilation to test progression recovery without touching mandatory inventory/rest constraints.
- KPI target (next 30 min): break flatline to `exp>=6` or `gold>=116`, while sustaining smoke `ok>=3/3` at `code=200` and inventory slots `<=8`.
- Adaptive step-69 (2026-03-09 07:49 KST): after another flat interval (`level 1`, `exp 3`, `gold 113`, inventory/area unchanged), repeated stagnation persisted, but no new rate-limit spike appeared.
- CHANGE (reversible, logic-level): updated rest gate in runner from `hpRatio < BUJU_LOW_HP_RATIO` to `hpRatio <= BUJU_LOW_HP_RATIO` so 50% HP boundary enters rest-first path.
- Rationale: switch from timing-only tuning to boundary-condition correction to reduce borderline low-HP combat churn while preserving all mandated sell/rest numeric constraints.
- KPI target (next 30 min): observe `rest` activation at HP<=50% at least once and achieve `exp>=6` or `gold>=116` with smoke `ok>=3/3`, `code=200`.
- Adaptive step-70 (2026-03-09 08:19 KST): even after boundary-rest correction, core progression stayed flat (`level 1`, `exp 3`, `gold 113`, inventory 3, same area), though HP state improved to 70% and smoke remained stable.
- CHANGE (reversible): reduced retry burst overhead by changing `BUJU_RETRY_MAX_ATTEMPTS` from `2` to `1`.
- Rationale: with no fresh rate-limit spike and persistent KPI flatline, shift from pacing/threshold changes to retry-pressure reduction while preserving all mandatory inventory/rest constraints.
- KPI target (next 30 min): maintain smoke `ok>=3/3` with `code=200` and break stagnation to `exp>=6` or `gold>=116`, inventory slots `<=8`.
- Adaptive step-71 (2026-03-09 08:48 KST): the next interval still showed repeated stagnation (`level 1`, `exp 3`, `gold 113`, inventory 3, same area), with no new 429 signature.
- CHANGE (reversible): reduced cycle breadth by setting `BUJU_MAX_ACTIONS_PER_CYCLE` from `3` to `2` while keeping `BUJU_BASE_DELAY_MS=1400` and `BUJU_RETRY_MAX_ATTEMPTS=1`.
- Rationale: with rate-limit pressure quiet but KPI flatline persisting, reduce per-cycle action fan-out to favor cleaner deterministic rest/combat turnover.
- KPI target (next 30 min): maintain smoke `ok>=2/2` at `code=200` and break stagnation to `exp>=6` or `gold>=116`, inventory slots `<=8`.
- Adaptive step-72 (2026-03-09 09:18 KST): stagnation repeated again (`level 1`, `exp 3`, `gold 113`, inventory 3, same area) after cycle-width reduction, with no new rate-limit spike observed.
- CHANGE (reversible): widened pacing further by changing `BUJU_BASE_DELAY_MS` from `1400` to `1600` while keeping `BUJU_MAX_ACTIONS_PER_CYCLE=2` and `BUJU_RETRY_MAX_ATTEMPTS=1`.
- Rationale: when flat KPI persists without fresh error signatures, apply incremental cadence dilation to test progression recovery while preserving all hard inventory/rest constraints.
- KPI target (next 30 min): sustain smoke `ok>=2/2` with `code=200` and break flatline to `exp>=6` or `gold>=116`, inventory slots `<=8`.
- Adaptive step-73 (2026-03-09 09:49 KST): metrics remained flat again (`level 1`, `exp 3`, `gold 113`, inventory 3, same area) despite delay widening, while HP/rate-limit state stayed healthy.
- CHANGE (reversible): nudged cadence back up by changing `BUJU_BASE_DELAY_MS` from `1600` to `1500` and keeping `BUJU_MAX_ACTIONS_PER_CYCLE=2`, `BUJU_RETRY_MAX_ATTEMPTS=1`.
- Rationale: with repeated under-production and no fresh throttle signal, test a small re-acceleration rather than further delay widening.
- KPI target (next 30 min): keep smoke `ok>=2/2` at `code=200` and break stagnation to `exp>=6` or `gold>=116`, inventory slots `<=8`.
- Adaptive step-74 (2026-03-09 10:18 KST): despite re-acceleration, progression remained flat (`level 1`, `exp 3`, `gold 113`, inventory 3, same area), but health/throttle signals were stable (HP high, no new 429).
- CHANGE (reversible): increased cycle breadth from `BUJU_MAX_ACTIONS_PER_CYCLE=2` to `3` while keeping `BUJU_BASE_DELAY_MS=1500` and `BUJU_RETRY_MAX_ATTEMPTS=1`.
- Rationale: with repeated flat KPI and no instability signature, test controlled throughput restoration by undoing prior cycle-width reduction.
- KPI target (next 30 min): sustain smoke `ok>=3/3` at `code=200` and break stagnation to `exp>=6` or `gold>=116`, inventory slots `<=8`.
- Adaptive step-75 (2026-03-09 10:48 KST): stagnation persisted again (`level 1`, `exp 3`, `gold 113`, inventory 3, same area), while stability headroom remained positive (HP high, no fresh throttle errors).
- CHANGE (reversible): raised `BUJU_MAX_ACTIONS_PER_CYCLE` from `3` to `4`, keeping `BUJU_BASE_DELAY_MS=1500` and `BUJU_RETRY_MAX_ATTEMPTS=1`.
- Rationale: with repeated flat outputs but stable execution, test one-step throughput expansion before further logic churn.
- KPI target (next 30 min): maintain smoke `ok>=4/4` at `code=200` and break stagnation to `exp>=6` or `gold>=116`, inventory slots `<=8`.
- Adaptive step-76 (2026-03-09 11:19 KST): after cycle-width expansion, metrics remained flat (`level 1`, `exp 3`, `gold 113`, inventory 3, same area), with smoke stable and no new throttle signal.
- CHANGE (reversible): increased action cadence by reducing `BUJU_BASE_DELAY_MS` from `1500` to `1300` while keeping `BUJU_MAX_ACTIONS_PER_CYCLE=4` and `BUJU_RETRY_MAX_ATTEMPTS=1`.
- Rationale: repeated flat outputs under stable runtime indicate under-frequency risk; apply timing acceleration before deeper logic rewrites, while preserving all mandatory inventory/rest constraints.
- KPI target (next 30 min): sustain smoke `ok>=4/4` with `code=200` and break stagnation to `exp>=6` or `gold>=116`, inventory slots `<=8`.
- Adaptive step-77 (2026-03-09 11:50 KST): even with faster cadence, progression stayed flat (`level 1`, `exp 3`, `gold 113`, inventory 3, same area), while execution remained stable and unthrottled.
- CHANGE (reversible): increased cycle throughput from `BUJU_MAX_ACTIONS_PER_CYCLE=4` to `5`, keeping `BUJU_BASE_DELAY_MS=1300` and `BUJU_RETRY_MAX_ATTEMPTS=1`.
- Rationale: repeated stagnation with healthy runtime signals supports another bounded throughput step before considering heavier logic changes.
- KPI target (next 30 min): maintain smoke `ok>=5/5` at `code=200` and break stagnation to `exp>=6` or `gold>=116`, inventory slots `<=8`.
- Adaptive step-78 (2026-03-09 12:18 KST): after widening to 5-action cycles, metrics were still pinned (`level 1`, `exp 3`, `gold 113`, inventory 3, same area), but execution quality stayed high (`ok=5/5`, no fresh throttle).
- CHANGE (reversible): reduced `BUJU_BASE_DELAY_MS` from `1300` to `1200` while keeping `BUJU_MAX_ACTIONS_PER_CYCLE=5` and `BUJU_RETRY_MAX_ATTEMPTS=1`.
- Rationale: repeated flat outputs under stable runtime justify one more timing-density increase before deeper strategy rewrites.
- KPI target (next 30 min): keep smoke `ok>=5/5` with `code=200` and produce first progression movement (`exp>=6` or `gold>=116`), inventory slots `<=8`.

## Equipment Progression Plan (staged)
- Early game (Lv1~9): prioritize safe EXP/gold accumulation, always auto-equip best-in-slot by `equipSlot` and `score=maxDamage+defBonus`, avoid enhancement spam.
- Mid game (Lv10+): enhance main weapon first only when prerequisites are satisfied (weapon equipped, enhancement scroll available, blacksmith NPC reachable, gold reserve met).
- Late game: expand enhancement to armor/accessory after weapon baseline, with cooldown/risk controls and reserve preservation.

- Adaptive step-79 (2026-03-09 12:49 KST): recovery appeared (`level 1->2`, `exp 3->14`, `gold 113->134`), but trailing repeated-stagnation history and missing enhancement path completion required CHANGE.
- CHANGE (reversible): added minimal safe enhancement path in runner (weapon-first, prerequisite checks, cooldown gate) and explicit `combat/start` payload (`monster_id`, `area`) for compatibility safety.
- Constraint integrity: mandatory inventory liquidation + rest-first numeric constraints unchanged.
- KPI target (next 30 min): maintain smoke `ok>=5/5` code=200, deaths=0, and reach `exp>=20`, `gold>=150`, inventory slots `<=8`.
- Adaptive step-80 (2026-03-13 16:16 KST): last-20 thinking baseline (19 logs) showed prior checkpoint around `level=11`, `gold=324`, `area=talking_island_field`; current state moved to `level=18`, `gold=304`, `area=talking_island_cave`, inventory `8->5`.
- CHANGE (reversible): a fresh smoke probe surfaced `combat_start` HTTP `429`, so cadence was de-bursted by changing `BUJU_BASE_DELAY_MS` from `1200` to `1600` and `BUJU_MAX_ACTIONS_PER_CYCLE` from `5` to `3`.
- Rationale: progression exists, but a new immediate rate-limit signal violates KEEP condition; lower burst pressure first before deeper logic changes.
- Constraint integrity: hard inventory policy kept exactly (`BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`) and worse-than-equipped liquidation priority unchanged.
- Enhancement progression status: staged plan remains active (early safe farm, mid weapon-first with prereqs, late broadening with risk controls); minimal safe enhancement path remains prerequisite-gated.
- KPI target (next 30 min): smoke `ok>=1/1` at `code=200`, new `429` probes `=0`, defeats `=0`, level `>=19`, inventory slots `<=8`.

- Adaptive step-81 (2026-03-13 16:46 KST): last-20 thinking logs show net progression but mixed stability; latest run improved level band (`11->18`) while introducing fresh 429 pressure.
- CHANGE (reversible): lowered safe monster gap from `1` to `0`, widened base delay from `1600` to `2200`, and updated monster selector to prioritize safety-filtered high-efficiency targets (`exp/(1+danger)`) with defeat-aware dynamic gap tightening.
- Rationale: enforce safest available farm target first, reduce repeated defeat risk, and keep movement strictly level-threshold gated.
- Equipment progression (staged, active):
  - Early game: prioritize gold accumulation and safe hunting; no enhancement spam.
  - Mid game: weapon-first enhancement when reserve/prereqs are satisfied.
  - Late game: expand to armor/accessory with cooldown/failure-risk guardrails.
- Enhancement action path: minimal safe path remains implemented with prerequisite gates (`weapon equipped + scroll + blacksmith NPC + gold reserve + enhance rate budget`).
- KPI target (next 30 min): smoke `ok>=1/1` code `200`, defeats `=0`, rate-limit probes `<=1`, level `>=19`, inventory slots `<=8`.
- Adaptive step-82 (2026-03-13 17:16 KST): last-20 thinking logs (latest checkpoint 16:20 KST) and daemon tail showed no KPI lift after step-81 (`level=18`, `exp=1`, `gold=304`, inventory `5`), while `combat_start` 429 recurred and an in-combat surrender loop appeared when armor slot was empty.
- CHANGE (reversible): added action-level 429 cooldown gating (`BUJU_STALL_429_COOLDOWN_TICKS`, default 6) and tightened `surrender_for_equip` precondition to require a real emergency equip candidate (prevents repeated surrender when no equippable armor exists).
- Evidence: one full-cycle smoke after patch returned `ok=2/3`, `code=200`, with no new 429 in that sample; daemon continuity preserved.
- Constraint integrity: hard inventory constraints unchanged and still enforced (`trigger=10`, `target=8`, `max-iterations=10`), with worse-than-equipped liquidation priority preserved.
- Equipment progression plan remains active in docs and runtime (early safe farm, mid weapon-first enhancement on prerequisite gates, late armor/accessory broadening with cooldown/risk controls).
- KPI target (next 30 min): reduce `combat_start` 429 appearances in daemon tail to <=1 per 5 cycles, keep defeats `=0`, hold inventory slots `<=8`, and reach `level>=19` or `gold>=330`.

- Adaptive step-83 (2026-03-13 17:46 KST): last-20 thinking logs still show historical growth (`level 1->18`, `gold 113->304`) but current daemon window is flat (`level=18`, `exp=1`, `gold=304`) with repeating `combat_start 429` and `surrender_dangerous_combat` churn.
- CHANGE (reversible): reduced per-cycle burst from `BUJU_MAX_ACTIONS_PER_CYCLE=3` to `2`, and tightened area progression gate by raising `BUJU_MOVE_LEVEL_2` from `18` to `20` to stay in safer field routing until threshold is actually met.
- Rationale: KEEP rejected due no near-term KPI lift + repeated risk/throttle pattern; this change lowers request pressure and risk gap without touching mandatory inventory constraints.
- Constraint integrity: hard inventory policy preserved exactly (`BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`) and worse-than-equipped liquidation priority unchanged.
- Equipment progression policy (explicit): (a) early game = gold accumulation, no enhancement spam; (b) mid game = weapon-first enhancement when reserve+scroll+NPC prerequisites are satisfied; (c) late game = widen to armor/accessory with cooldown/failure-risk controls.
- KPI target (next 30 min): daemon tail `combat_start 429 <=1` per 6 cycles, dangerous-surrender events `<=1` per 6 cycles, defeats `=0`, inventory slots `<=8`, and progression to `level>=19` or `gold>=320`.

- Adaptive step-84 (2026-03-13 18:16 KST): last-20 thinking logs still show long-window improvement (`level 1->18`, `gold 113->304`, `deaths=0`, `rate_limited=0/20`), but fresh smoke probe under current settings produced a new throttle failure (`live-strategy ok=0/2`, `lastAction=combat_start`, `code=429`) despite short-term gains (`exp 0->309`, `gold 304->334`).
- CHANGE (reversible): increased pacing delay from `BUJU_BASE_DELAY_MS=2200` to `2600` while keeping `BUJU_MAX_ACTIONS_PER_CYCLE=2` to reduce burst pressure without collapsing throughput.
- Safety/efficiency intent kept: safest high-efficiency monster selector (safety-filter + efficiency score), level-threshold area movement, and defeat-aware dynamic risk gap remain active.
- Hard constraints preserved exactly: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; if slots>=10, liquidation still prioritizes unequipped gear worse than equipped first.
- Equipment progression status kept: best-in-slot auto-equip by `equipSlot` + `maxDamage+defBonus`; staged enhancement plan (early safe gold, mid weapon-first with reserve, late broaden with risk control) remains active; minimal safe enhancement path stays prerequisite-gated (`weapon+scroll+npc+reserve+cooldown`).
- KPI target (next 30 min): smoke `ok>=1/2` with `code=200` and `429<=1`, defeats `=0`, maintain `inventory<=8`, progress to `level>=19` or `gold>=340`.

- Adaptive step-85 (2026-03-13 18:46 KST): last-20 thinking deltas remain positive (`level 1->18`, `gold 113->304`) but near-term daemon pattern still repeats (`combat_start` 429 bursts + `surrender_dangerous_combat` churn), so KEEP rejected for efficiency/risk reasons.
- CHANGE (reversible): increased pacing from `BUJU_BASE_DELAY_MS=2600` to `3000`, added no-armor safety override (force move to safe area1 while below move-Lv2 threshold), and tightened monster safety filter when armor is missing.
- Constraint integrity: hard inventory policy preserved exactly (`BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`) with "worse-than-equipped first" liquidation unchanged.
- Equipment progression status: best-in-slot equip evaluation by `equipSlot` + `(maxDamage+defBonus)` remains active; staged enhancement plan kept (early safe gold/no spam -> mid weapon-first with reserve/prereq gates -> late armor/accessory expansion with cooldown/failure-risk controls); minimal safe enhancement path remains prerequisite-gated (`weapon+scroll+npc+reserve+rate budget`).
- Validation evidence: one-cycle smoke after change returned `live-strategy ok=1/2`, `lastAction=combat_start`, `code=200`, daemon continuity preserved.
- KPI target (next 30 min): `combat_start 429 <=1` per 8 cycles, `surrender_dangerous_combat <=1` per 8 cycles, defeats `=0`, inventory slots `<=8`, and progress to `level>=19` or `gold>=340`.

- Adaptive step-86 (2026-03-13 19:16 KST): parsed trailing thinking window (`/api/agent/thinking/j211y?limit=20`) and computed deltas across window edges: `level 1->18`, `gold 113->304`, `deaths 0->0` (no defeat increase), while immediate smoke still repeated `combat_start` throttle (`code=429`) before adaptation.
- CHANGE (reversible):
  - Enforced hard inventory constraints as code invariants (`trigger=10`, `target=8`, `maxIterations=10`) to avoid env drift.
  - Tightened safest-target selection fallback: if no monster passes strict safety filter, choose least-danger candidates first (no blind fallback to high-risk mobs).
  - Enhancement progression implementation extended to staged runtime path: mid-game weapon-first safe enhancement, late-game optional armor/accessory safe enhancement only when `level>=20` and reserve margin is met.
  - Added combat-start throttle guard: `combat_start=429` now yields `wait_combat_start_rate_limit` (graceful wait, avoids fail-loop churn) instead of hard failing the tick.
  - Pacing tuned `BUJU_BASE_DELAY_MS 3000->3200` to reduce burst pressure.
- Enhancement strategy plan (staged, explicit):
  - Early game: prioritize safe gold/EXP accumulation; no risky enhancement spam.
  - Mid game: enhance main weapon first after reserve threshold + scroll + blacksmith + rate budget prerequisites.
  - Late game: broaden to armor/accessory only with extra reserve and cooldown/failure-risk controls.
- Validation evidence: `BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` => `ok=1/1 lastAction=wait_combat_start_rate_limit level=18 exp=919 gold=309 code=200`.
- KPI target (next 30 min): keep defeats `=0`, keep inventory slots `<=8`, reduce `wait_combat_start_rate_limit` share to `<=50%` of ticks, and reach `level>=19` or `gold>=360`.

- Adaptive step-87 (2026-03-13 19:46 KST): last-20 thinking logs show long-window progression (`level 1->18`, `gold 113->309`, `exp 3->~1195`) with stable no-defeat operation, but rate-limit pressure still appears (`decision_type=rate_limited` present and daemon tail dominated by `wait_combat_start_rate_limit`), so KEEP was rejected.
- CHANGE (reversible): reduced per-cycle burst from `BUJU_MAX_ACTIONS_PER_CYCLE=2` to `1` and widened pacing from `BUJU_BASE_DELAY_MS=3200` to `3400` to lower combat-start collision pressure while preserving continuous daemon operation.
- Constraint integrity: hard inventory constraints remain invariant (`BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`), and `slots>=10` still prioritizes selling unequipped gear worse than equipped before general low-tier cleanup.
- Equipment/enhancement policy status: best-in-slot auto-equip by `equipSlot` + `score(maxDamage+defBonus)` remains active; staged enhancement plan preserved (early safe gold/no spam -> mid weapon-first on reserve+scroll+npc gates -> late armor/accessory with cooldown/risk controls) with minimal safe enhancement path still prerequisite-gated.
- KPI target (next 30 min): `wait_combat_start_rate_limit` share `<=40%`, `surrender_dangerous_combat <=1` per 10 cycles, defeats `=0`, inventory slots `<=8`, and progression to `level>=19` or `gold>=360`.

- Adaptive step-88 (2026-03-13 20:16 KST): fetched trailing 20 thinking logs (`/api/agent/thinking/j211y?limit=20`) and daemon last-20 cycle outputs. Window deltas stayed positive (`level 1->18`, `gold 113->324`, `rate_limited 1/20`) but near-term efficiency remained bottlenecked (`exp +8`, `gold +20`, `wait_combat_start_rate_limit 12/20`, no level-up), so KEEP was rejected.
- CHANGE (reversible): added explicit `combat_start` cooldown gate in runner (`shouldSkipAction('combat_start') => wait_combat_start_cooldown`) to honor existing 429 stall state before issuing new start calls; tuned `BUJU_STALL_429_COOLDOWN_TICKS` from `6` to `8`; added daemon singleton lockfile guard (`logs/live-runner-daemon.lock`) to prevent duplicate live-runner loops.
- Rationale: reduce repeated 429-collision churn and keep the runner continuous-but-single-instance without touching hard inventory liquidation or enhancement safety prerequisites.
- Hard constraints preserved exactly: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots `>=10`, liquidation still sells unequipped worse-than-equipped gear first.
- Equipment progression status: best-in-slot evaluation/equip remains inventory-wide by `equipSlot` and `score(maxDamage+defBonus)` each cycle; staged enhancement plan remains active (early safe gold/no spam, mid weapon-first on reserve+scroll+npc, late armor/accessory with reserve+cooldown/failure controls).
- KPI target (next 30 min): reduce combined combat throttle waits (`wait_combat_start_rate_limit + wait_combat_start_cooldown`) to `<=50%` of cycles, keep defeats `=0`, keep inventory slots `<=8`, and reach `level>=19` or `gold>=350`.

- Adaptive step-89 (2026-03-13 20:47 KST): parsed last-20 thinking logs (`/api/agent/thinking/j211y?limit=20`) and recomputed deltas (`level 1->18`, `gold 113->334`, `death delta 0`, `rate_limited 1/20`). Long-window growth remains, but immediate KPI was still unmet (`level=18`, `gold<350`) and combat-start wait pressure persisted, so KEEP was rejected.
- CHANGE (reversible): widened pacing from `BUJU_BASE_DELAY_MS=3600` to `3800` in `config/strategy.env` to lower repeated combat-start collision risk without reducing safety controls or daemon continuity.
- Validation evidence: `BUJU_MAX_ACTIONS_PER_CYCLE=2 node scripts/live-strategy-runner.js` => `live-strategy ok=2/2 lastAction=wait_combat_start_rate_limit level=18 exp=1781 gold=314 code=200`; follow-up `GET /api/status` => `level=18`, `exp=1783`, `gold=319`, no combat lock.
- Constraint integrity: hard inventory rules preserved exactly (`BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`) with `slots>=10` worse-than-equipped liquidation priority unchanged.
- Equipment progression status: best-in-slot equip by `equipSlot + (maxDamage+defBonus)` remains active; staged enhancement plan remains explicit (early safe gold/no spam -> mid weapon-first on reserve+scroll+npc -> late armor/accessory with cooldown/failure controls); minimal safe enhancement path remains prerequisite-gated.
- KPI target (next 30 min): keep combined combat-start wait share `<=45%`, defeats `=0`, inventory slots `<=8`, and progress to `level>=19` or `gold>=350` with smoke `code=200`.

- Adaptive step-90 (2026-03-13 21:18 KST): parsed last-20 thinking logs (`GET /api/agent/thinking/j211y?limit=20`) and computed deltas `level 1->18 (+17)`, `gold 113->319 (+206)`, `rate_limited 1/20`; current status check showed `level=18`, `exp=2035`, `gold=329`, `inventory=8`, `area=talking_island_field`.
- KEEP (evidence-based): improvement is still present in the mandatory window, so no pacing/config churn this cycle; keep conservative cadence and monitor immediate `combat_start` 429 wait pressure.
- Hard constraints re-validated in runtime code path (env-override immune): `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`, with slots>=10 liquidation priority = unequipped worse-than-equipped first.
- Equipment progression plan detail (staged, explicit):
  - Early game (Lv1~9): safe gold/EXP farming only, auto-equip best-in-slot each cycle by `equipSlot` and `score=maxDamage+defBonus`, no enhancement spam.
  - Mid game (Lv10+): weapon-first enhancement only when all prerequisites are satisfied (`weapon equipped + scroll available + blacksmith npc + gold reserve >= BUJU_ENHANCE_GOLD_RESERVE + action budget`).
  - Late game (Lv20+): expand to armor/accessory enhancement only after weapon baseline, with reserve margin (`+400`), cooldown gate (`BUJU_ENHANCE_COOLDOWN_TICKS`), and stall/rate-limit controls.
- Enhancement action path status: minimal safe API path remains active and prerequisite-gated (`/npc/list` -> select blacksmith -> `/npc/{npc_id}/enhance`), skipped when any prerequisite is missing.
- KPI target (next 30 min): keep defeats `=0`, maintain inventory slots `<=8`, reduce `wait_combat_start_rate_limit` share to `<=45%`, and reach `level>=19` or `gold>=350` with smoke `code=200`.

- Adaptive step-91 (2026-03-13 21:47 KST): read `GET /api/agent/thinking/j211y?limit=20` and recomputed deltas: `level 1->18 (+17)`, `gold 113->334 (+221)`, no new death-event increase in the recent window, and `rate_limited=1/20`.
- KEEP (evidence-based): latest smoke stayed healthy (`BUJU_MAX_ACTIONS_PER_CYCLE=1 node scripts/live-strategy-runner.js` => `ok=1/1`, `code=200`, `exp 2283->2285`, `gold 329->334`) and daemon tail wait-pressure was acceptable (`wait_combat_start_rate_limit` share `7/20=0.35`).
- Constraint integrity preserved exactly: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`, with slots>=10 selling unequipped worse-than-equipped first.
- Equipment progression policy kept active: every tick best-in-slot by `equipSlot` + `score(maxDamage+defBonus)`; staged enhancement plan (early safe accumulation/no spam -> mid weapon-first on prereqs -> late armor/accessory with reserve+cooldown risk controls).
- Enhancement action path remains minimal and safe (`/npc/list` blacksmith discovery -> `/npc/{npc_id}/enhance`) and executes only when prerequisites are simultaneously satisfied (scroll+npc+resource+budget).
- KPI target (next 30 min): `wait_combat_start_rate_limit<=40%`, deaths `=0`, inventory `<=8`, smoke `code=200`, and progression to `level>=19` or `gold>=350`.

- Adaptive step-92 (2026-03-13 23:16 KST): mandatory check on `GET /api/agent/thinking/j211y?limit=20` yielded positive deltas (`level 1->18`, `gold 113->319`, `rate_limited 1/20`) with no new death-signal increase in current probes, so KEEP selected.
- KEEP (evidence-valid): no parameter churn this cycle; preserve continuous daemon cadence and monitor immediate wait-pressure trend before any future reversible tuning.
- Constraint lock preserved: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots>=10, sell unequipped worse-than-equipped gear first.
- Equipment progression (explicit staged plan):
  - Early game: prioritize safe gold/EXP accumulation; avoid risky enhancement spam.
  - Mid game: weapon-first enhancement only after reserve + scroll + blacksmith + action-budget prerequisites are all satisfied.
  - Late game: broaden to armor/accessory with reserve margin, cooldown, and failure-risk controls.
- Minimal safe enhancement path remains implemented and gated (`/npc/list` blacksmith discovery -> `/npc/{npc_id}/enhance` only when scroll+npc+resource prerequisites are satisfiable).
- 2026-03-14 04:32 KST: For hourly gameplay feedback, treat paginated `GET /api/logs` as the primary outage fallback when `/api/*/recent` probes return `404`, instead of relying on local replay-only summaries.
  - Justification: live evidence this hour showed `/api/logs` healthy (`200`) with sufficient 60m signal (359 events, 251 wins, 0 defeats) while `activity:fetch` recent-endpoint probes stayed `404` and underreported gameplay.
  - Scope: feedback/telemetry path only; no gameplay policy/action-loop behavior change.

- Adaptive step-93 (2026-03-14 07:16 KST): read `GET /api/agent/thinking/j211y?limit=20` and recomputed deltas `level +1`, `gold -15`, `death +0`, with persistent throttle/wait pressure in latest daemon tail.
- CHANGE (evidence-based): to reduce repeated `combat_start -> surrender_dangerous_combat -> rest` churn, tuned dangerous-combat surrender HP gate from fixed `0.55` to dynamic `max(0.4, BUJU_LOW_HP_RATIO + 0.05)` while preserving high-risk monster/atk override surrender conditions.
- Constraint integrity preserved exactly: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when slots>=10, liquidate unequipped worse-than-equipped first before any other inventory cleanup.
- Equipment progression requirements remain active:
  - Early game: safe gold accumulation, no risky enhancement spam.
  - Mid game: weapon-first enhancement only when scroll+npc+resource prerequisites are satisfiable.
  - Late game: expand to armor/accessory with cooldown/failure-risk controls.
- Minimal safe enhancement action path remains implemented (`/npc/list` blacksmith discovery -> `/npc/{npc_id}/enhance`) and executes only under prerequisite gates.

## 2026-03-14 18:46 KST — Equipment Progression Plan (staged, enforced)
- Early game: prioritize gold accumulation and safe hunting; no risky enhancement spam.
- Mid game: enhance main weapon first only when prerequisites are satisfiable (`weapon equipped + enhancement scroll + blacksmith NPC + gold reserve + action budget`).
- Late game: broaden to armor/accessory only with failure-risk controls (reserve margin, cooldown, stall/rate-limit guards).
- Runtime path remains minimal-safe: if any prerequisite is missing, enhancement is skipped (never forced).
- KPI target (next 30 min): `surrender_dangerous_combat<=1/6 cycles`, `wait_combat_start_rate_limit<=40%`, `deaths=0`, `inventory<=8`, smoke `code=200`.

- 2026-03-14 08:28 KST: Adopt `death_loop_breaker` as a mandatory safety-policy gate in hourly feedback/runner orchestration when last-hour defeat concentration is abnormal.
  - Trigger baseline: `death_count_last_60m >= 5` or `death_share >= 10%` from paginated `/api/logs` fallback.
  - Enforced behavior under trigger: freeze aggression increases, force conservative hunt profile + pre-combat HP floor + cooldown backoff until recovery criteria are met.
  - Justification: live 1-hour evidence showed pathological loop (`death=136`, `surrender=136`, `rest=136`, `hunt=0`) with no progression signal, so normal adaptive tuning is unsafe.
  - Scope: orchestration safety policy only; does not change secret handling or external API auth flow.

- 2026-03-14 09:28 KST: Escalate death-loop handling to mandatory circuit-breaker mode when loop persists across consecutive hours.
  - Policy change: If last-hour signals satisfy `hunt_count==0` and `death_count>=20`, runner must enter 15-minute non-combat recovery mode (`rest/buy only`, block `combat_start`) before any further combat attempts.
  - Roll-forward criteria: Exit recovery mode only after cooldown expiry, then resume with conservative hunt constraints and telemetry tagging (`guard_hit=true`, `recovery_window_id`).
  - Justification: Two consecutive hourly windows showed severe loop persistence (`death/surrender/rest` triad, no hunts, no progression) despite healthy status/log telemetry, so soft tuning is insufficient.
  - Scope: Runtime safety policy/orchestration only; no secret handling or external API contract changes.

## 2026-03-16 01:17 KST — Strategy Director (30m adaptive, economy stabilization)
- Adaptive loop evidence: remote `GET /api/agent/thinking/j211y?limit=20` returned `count=0`, so the mandated 20-window fallback used local trailing telemetry (`tmp/thinking-post-*.json`, latest 20; `thinking-post-1416.json -> thinking-post-0050.json`) with deltas `level +1`, `gold +0`, `inventory +3`, `rate/cooldown mentions 19/20`.
- CHANGE (mandatory loop): despite level gain, economy/inventory quality regressed (flat gold + inventory drift under high cooldown pressure), so KEEP was rejected.
- Minimal reversible parameter change committed: `BUJU_MUTATION_MIN_GOLD_RESERVE 120 -> 260` to block low-reserve mutation spend spikes and preserve safe-farm gold buffer while keeping combat risk policy unchanged.
- Safety/efficiency evidence: live `GET /api/status` shows `level=23`, `area=talking_island_field`, ongoing combat target `skeleton`; `GET /api/areas/talking_island_field/monsters` exposes (`rabbit`,`skeleton`) so safest high-efficiency target remains `skeleton` under strict move gate (`BUJU_MOVE_LEVEL_2=30`).
- Hard constraints remain invariant in runtime: `BUJU_INV_SELL_TRIGGER_SLOTS=10`, `BUJU_INV_SELL_TARGET_SLOTS=8`, `BUJU_INV_SELL_MAX_ITERATIONS_PER_TICK=10`; when `slots>=10`, liquidation priority stays `unequipped worse-than-equipped first`.
- Equipment progression policy remains staged and explicit:
  - Early game: safe gold accumulation, no risky enhancement spam.
  - Mid game: enhance main weapon first only when `scroll + npc + resource + reserve` prerequisites are satisfiable.
  - Late game: broaden to armor/accessory only with cooldown and failure-risk controls.
- Minimal safe enhancement path remains implemented and prerequisite-gated (`/npc/list -> /npc/{npc_id}/enhance`), skipped automatically when prerequisites are missing.

## 2026-03-15 23:19 KST — Strategy Director (30m adaptive, keep-with-evidence)
- Adaptive loop evidence (`GET /api/agent/thinking/j211y?limit=20`, ordered 20-window): `level +1` (`22->23`), `gold +5`, `inventory -1`, throttle/rate-limit wording `14/20`.
- KEEP decision is valid this cycle (measurable progression + no defeat regression in window); no aggression increase applied.
- Hard inventory constraints remain fixed and enforced in runtime code: trigger/target/max-iter = `10/8/10` with strict precedence `slots>=10 => sell unequipped gear worse than equipped first`.
- Equipment progression policy (staged, enforced):
  - Early: gold accumulation + no risky enhancement spam.
  - Mid: weapon-first enhancement only when `scroll + blacksmith NPC + reserve + action budget + non-combat` prerequisites are all satisfied.
  - Late: expand to armor/accessory only with cooldown/stall/rate-limit guards.
- Minimal safe enhancement action path remains active: `/npc/list`(blacksmith discovery) -> `/npc/{npc_id}/enhance` with prerequisite gating; skipped safely when any prerequisite is missing.

## [2026-03-16 02:17 KST] Adaptive change — combat_start 429 streak cooldown scaling
- Trigger evidence (mandatory loop): `GET /api/agent/thinking/j211y?limit=20` ordered window (`2026-03-15 15:20:05 -> 2026-03-16 01:49:55`, `count=20`) showed no measurable improvement (`Δlevel=0`, `Δexp=0`, `Δgold=0`, `Δdeath=0`) and persistent throttle wording (`rate/429 mentions=20/20`).
- Decision: **CHANGE** (KEEP rejected due to zero progression evidence).
- Runtime logic update: in `scripts/live-strategy-runner.js`, added `combatStart429Streak` and scaled `combat_start` cooldown on repeated `429` (`stall429CooldownTicks + min(6, streak)`), while preserving safe hunt fallback and hard safety contracts.
- Expected effect: reduce repeated `wait_combat_start_rate_limit` churn, allow rate budget recovery between combat-start attempts, and keep runner continuous without aggression increase.
- Invariants preserved: inventory hard constraints (`10/8/10` + worse-than-equipped-first), BiS equip scoring (`equipSlot`, `maxDamage+defBonus`), strict level-threshold movement gate, and prerequisite-gated minimal enhancement path.
